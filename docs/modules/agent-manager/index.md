# Agent Manager

Pipeline agent lifecycle orchestration — drain loop, worktree management, watchdog, completion handling.
Source: `src/main/agent-manager/`

| Module | Purpose | Key Exports |
|--------|---------|-------------|
| [`opencode-wire.ts`](opencode-wire.md) | Pure translation layer: opencode `--format json` stdout lines → Anthropic SDK wire message objects. Event type mapping: `text`→assistant text, `tool`→assistant tool_use + user tool_result, `step_finish(stop)`→result, `error`→assistant error text. No I/O, no side effects — every function is a pure value-in/value-out transform. | `translateOpencodeEvent`, `extractOpencodeSessionId` |
| `sdk-message-protocol.ts` | SDK wire protocol type guards and field accessors. `asSDKMessage` rejects messages whose outer shape contradicts the declared type — `message` must be an object if present, and `message.content` must be an array if present — so consumers never receive a value that lies about its container shape. | `SDKWireMessage`, `asSDKMessage`, `getNumericField`, `getSessionId`, `isRateLimitMessage` |
| `spawn-sdk.ts` | SDK-based agent spawn — session ID extraction, abort wiring, steer stub. Exports `MAX_TURNS = 20` used by both SDK config and message consumer. **T-79:** the SDK-mode steer warn log reports only `message length: <n>` — the message body is never logged, since steer content is user-supplied and project policy keeps prompts/specs out of `~/.bde/bde.log`. **EP-12:** emits a structured `logger.event('agent.spawn', { taskId, tickId, agentType, model, maxBudgetUsd, cwd })` at spawn time. **EP-6 (T-78):** `agent.spawn` event now includes `backend: strategy.type` from the `SpawnStrategy` parameter — callers (sdk-adapter) resolve the strategy before calling `spawnViaSdk` and pass it explicitly. | `spawnViaSdk`, `MAX_TURNS` |
| `spawn-cli.ts` | CLI fallback spawn — stream-json protocol, V8 heap cap, stdin steer. Accepts optional `maxBudgetUsd` and passes it via `CLAUDE_MAX_COST_USD` env var to the CLI process. | `spawnViaCli`, `withMaxOldSpaceOption`, `AGENT_PROCESS_MAX_OLD_SPACE_MB` |
| `opencode-session-mcp.ts` | Per-session MCP HTTP server for opencode agents. Binds to 127.0.0.1 on a random port with a per-session token; exposes the same tasks/epics/meta tools as the in-process planner server used by Claude agents. Torn down when the session closes. | `startOpencodeSessionMcp`, `OpencodeSessionMcpHandle` |
| `opencode-worktree-config.ts` | Writes `.opencode/opencode.json` into the worktree with the per-session MCP server URL + token, and builds the opencode-specific first-turn prompt (branch name + commit rules prepended to the raw user task). | `writeOpencodeWorktreeConfig`, `buildOpencodeFirstTurnPrompt` |
| `spawn-opencode.ts` | opencode CLI spawn adapter — spawns `opencode run --format json`, pipes stdout line-by-line through `translateOpencodeEvent`, captures `sessionID` from the first event, and returns an `AgentHandle`. Steer is intentionally unsupported (one-shot CLI). | `spawnOpencode`, `OpencodeSpawnOptions` |
| `sdk-adapter.ts` | Thin facade over spawn-sdk, spawn-cli, spawn-opencode, and local-adapter. `spawnAgent` reads `loadBackendSettings()`+`resolveAgentRuntime()` and routes to the correct backend. The opencode branch: starts a per-session MCP server (`startOpencodeSessionMcp`), writes worktree config (`writeOpencodeWorktreeConfig`), builds a lightweight branch-prefixed first-turn prompt (`buildOpencodeFirstTurnPrompt`) when `branch` is provided, spawns opencode, and wraps the messages iterable to close the MCP server on exhaustion. If `spawnOpencode` throws before returning a handle, the MCP server is closed immediately in a catch block to prevent port leaks. `spawnWithTimeout` wraps `spawnAgent` with a `SPAWN_TIMEOUT_MS` deadline. **T-77:** `isInsideAllowedWorktreeBase` now resolves both the `cwd` and the `worktreeBase` via `realpathSync` before the prefix compare, so a symlink whose target points outside the base cannot pass the lexical-prefix check. A `realpathSync` failure (e.g. ENOENT for a non-existent cwd) fails closed — the spawn is refused and a warn log records the failure context. **EP-6 (T-73/74):** `resolveSpawnStrategy()` (private to the module) resolves SDK availability at spawn time and returns a `SpawnStrategy` discriminated union (`{ type: 'sdk' }` or `{ type: 'cli'; claudePath }`). The resolved strategy is passed to `spawnViaSdk`, which includes `backend: strategy.type` in the existing `agent.spawn` structured log event. `SpawnStrategy` re-exported from `types.ts` for callers. | `spawnAgent`, `spawnWithTimeout`, `SpawnStrategy`, `SDKWireMessage`, `asSDKMessage`, `getNumericField`, `getSessionId`, `isRateLimitMessage`, `AGENT_PROCESS_MAX_OLD_SPACE_MB`, `withMaxOldSpaceOption` |
| `pipeline-abort-error.ts` | Typed sentinel thrown by Phase 1 / Phase 2 helpers after completing their own recovery. `runAgent` catch blocks check `instanceof PipelineAbortError` to distinguish an expected early exit (no double cleanup) from an unexpected error that needs claim-release and terminal notification. | `PipelineAbortError` |
| `prompt-assembly.ts` | Task validation + prompt context prep — upstream context, scratchpad, prompt build | `validateTaskForRun`, `assembleRunContext`, `fetchUpstreamContext`, `readPriorScratchpad` |
| `message-consumer.ts` | SDK message stream iteration, OAuth refresh on auth error, playground path accumulation. Hard turn limit via `maxTurns` param (aborts with `max_turns_exceeded`); per-turn budget enforcement aborts if `agent.costUsd >= agent.maxCostUsd`. `pendingPlaygroundPaths` typed as `PlaygroundWriteResult[]` (includes `contentType`). **EP-12:** stream-error path emits `logger.event('agent.stream.error', { taskId, messagesConsumed, lastEventType, error })` (replaces the T-20 string warn). **EP-6 (T-70):** per-message stream deadline via `nextOrStall()` — if no message arrives within `MESSAGE_STALL_TIMEOUT_MS` (120 s) the loop exits with `streamError.message === 'stream_stalled'` and emits `logger.event('agent.stream.error', { reason: 'stalled', ... })`. Uses `node:timers` `setTimeout` (bypasses Vitest fake-timer patching so drain-loop tests that advance fake time do not inadvertently trigger the stall path). Test-only override: `__setStallTimeoutMsForTesting` / `__resetStallTimeoutForTesting`. **T-55 (Phase A):** `handleOAuthRefresh` accepts optional `onRefreshStarted?(p: Promise<unknown>): void`; calls it with the refresh promise so the drain loop can await token availability before the next spawn. `consumeMessages` accepts optional `onOAuthRefreshStart` 8th param and forwards it to `handleOAuthRefresh`. | `consumeMessages`, `ConsumeMessagesResult`, `MESSAGE_STALL_TIMEOUT_MS`, `__setStallTimeoutMsForTesting`, `__resetStallTimeoutForTesting` |
| `agent-telemetry.ts` | Cost/token tracking from SDK messages, SQL persistence of run telemetry. `computeTokenCost` estimates cost from token totals when the SDK subprocess exits before yielding the `result` message (e.g. maxTurns abort or OS signal). `persistAgentRunTelemetry` uses the SDK-reported cost when non-zero and falls back to `computeTokenCost` otherwise. | `trackAgentCosts`, `persistAgentRunTelemetry`, `computeTokenCost` |
| `metrics.ts` | Closure-based `MetricsCollector` implementation. Counters: `agentsCompleted`, `agentsFailed`, `watchdogKills`, and `duration_<bucket>` (five time buckets: <30s, <2m, <10m, <30m, >30m). `snapshot()` returns a plain object copy; `reset()` zeros all counters. `recordAgentDuration(durationMs)` buckets finished-agent runtime; called from `run-agent.ts` finalizeAgentRun after every completion. | `createMetricsCollector`, `MetricsCollector` |
| `agent-initialization.ts` | Agent record creation, stderr wiring, activeAgents registration, agent:started event, and structured `agent.spawned` JSON log. `createAgentRecord` failure is logged at `error` level (fire-and-forget: function is synchronous; untracked run is surfaced loudly so operators can investigate). `task.max_runtime_ms` is clamped to `[60_000, 86_400_000]` before being stored in `ActiveAgent.maxRuntimeMs` — zero or negative values fall back to `null` (uses config default); values above 24 hours are capped. | `initializeAgentTracking` |
| `spawn-and-wire.ts` | Spawn orchestration and error recovery — calls `credentialService.refreshCredential('claude')` immediately before `spawnWithTimeout` (V0.6 race fix) and surfaces the `CREDENTIAL_GUIDANCE` actionable message in the task's `notes` on failure, then `initializeAgentTracking` on success. Calls `deps.onAgentRegistered?.()` after `initializeAgentTracking` so `AgentManagerImpl` can decrement `_pendingSpawns` the moment the agent enters `activeAgents`. Passes `worktree.branch` to `spawnWithTimeout` so the opencode backend can build a lightweight branch-prefixed first-turn prompt. **EP-5 (T-55):** `handleSpawnFailure` calls `deps.onSpawnFailure(task.id, errReason)` with the task ID and error message so callers receive structured context for circuit-breaker accounting. | `spawnAndWireAgent`, `handleSpawnFailure` |
| `playground-sanitize.ts` (`src/main/`) | DOMPurify-based HTML sanitizer. `<meta>` and `<link>` removed from allowlist (prevented HTTP-refresh redirect and resource-fetch vectors). `afterSanitizeAttributes` hook strips `url(...)` from inline `style` attributes to block CSS-based data exfiltration. | `sanitizePlaygroundHtml` |
| [`playground-handler.ts`](playground-handler.md) | Detects playground file writes from agents (.html, .svg, .md, .markdown, .json), reads and sanitizes the file via DOMPurify, emits `agent:playground` events through `emitAgentEvent` (so they persist to SQLite AND reach the renderer via the batched broadcast path — the old direct `broadcast('agent:event', …)` call dropped events because the renderer only listens to `agent:event:batch`). Detection is session-scoped: `createPlaygroundDetector().onMessage(msg)` pairs each Write `tool_use` block with its matching `tool_result` block (by `tool_use_id`) so hits only fire after the file is confirmed written. Supports three tool naming conventions: Claude SDK `Write` (snake_case `file_path`), opencode `edit` (camelCase `filePath`), and opencode `apply_patch` (`*** Add File:` lines in `patchText`). `tryEmitPlaygroundEvent` takes a `PlaygroundEmitRequest` object; pass `allowAnyPath: true` for adhoc/assistant sessions where the user may render files outside the worktree. Uses `realpath()` (not `resolve()`) to follow symlinks before containment check, preventing traversal via symlinks. | `createPlaygroundDetector`, `PlaygroundDetector`, `detectPlaygroundWrite` (legacy top-level format only), `detectHtmlWrite` (backward-compat alias), `tryEmitPlaygroundEvent`, `PlaygroundEmitRequest`, `PlaygroundWriteResult`, `PlaygroundContentType` |
| `oauth-checker.ts` | Thin boolean adapter over `CredentialService` — delegates to `getDefaultCredentialService(...).getCredential('claude')`. Returns `true` on `status: 'ok'`, `false` otherwise, logging the service's canonical `actionable` message ("Run: claude login" etc.) on the warning. All caching, file reads, and proactive Keychain refresh logic now lives inside the service. TTL constants stay exported for test expectations. | `checkOAuthToken`, `invalidateCheckOAuthTokenCache`, `OAUTH_CHECK_CACHE_TTL_MS`, `OAUTH_CHECK_FAIL_CACHE_TTL_MS` |
| `prompt-sections.ts` | Shared prompt section builders and constants. `CODING_AGENT_PREAMBLE` includes a "Data vs. Instructions" directive listing all XML boundary tags as user-provided data. `escapeXmlContent` escapes `<` before any ASCII letter or `/` (opening-tag and closing-tag prefixes) to block tag construction by agent-controlled content; `<` before digits/spaces is left untouched to preserve diff output. `buildRetryContext` auto-detects structured `RevisionFeedback` JSON in `previousNotes` via `parseRevisionFeedback` and renders a `<revision_feedback>` block; falls back to legacy `<failure_notes>` for freeform strings. Accepts optional `revisionFeedback` array — renders a `## Human Revision Request` sub-section when present. `PLANNER_TOOLS_INSTRUCTIONS` advertises the in-process BDE MCP tools and explicitly forbids direct edits to `~/.bde/bde.db`. | `CODING_AGENT_PREAMBLE`, `SPEC_DRAFTING_PREAMBLE`, `PLANNER_TOOLS_INSTRUCTIONS`, `PLAYGROUND_INSTRUCTIONS`, `buildPersonalitySection`, `buildUpstreamContextSection`, `buildCrossRepoContractSection`, `buildBranchAppendix`, `buildRetryContext`, `buildScratchpadSection`, `truncateSpec`, `escapeXmlContent` |
| `prompt-pipeline.ts` | Pipeline agent prompt builder. Section ordering: upstream context → task spec → prior scratchpad → retry context. Prior scratchpad content (from `progress.md`) is wrapped in `<prior_scratchpad>…</prior_scratchpad>` boundary tags with `escapeXmlContent` applied before insertion — prevents a malicious `progress.md` from breaking XML containment. `MUST_COMMIT_BEFORE_EXIT` directive injected before `DEFINITION_OF_DONE` — instructs agents to run `git add -A && git commit` as their final action and verify with `git log -1`. `PUSH_FAILURE_GUIDANCE` injected for all task classes; `PIPELINE_JUDGMENT_RULES` (test failure rules) only for fix/refactor. Passes `revisionFeedback` from `BuildPromptInput` to `buildRetryContext` so re-spawned agents see human revision requests. | `buildPipelinePrompt`, `classifyTask`, `TaskClass` |
| `prompt-assistant.ts` | Assistant and adhoc agent prompt builders. `renderTaskSection` wraps truncated content in `escapeXmlContent()` before inserting it into `<user_task>` tags — prevents boundary-tag injection from user-supplied task strings. | `buildAssistantPrompt`, `buildAdhocPrompt` |
| _moved → `services/planner-mcp-server.ts`_ | T-19 (Phase 2) relocated this file out of `agent-manager/` because its sole responsibility is service orchestration (sprint + epic services), not pipeline lifecycle. See `docs/modules/services/index.md`. | — |
| `prompt-synthesizer.ts` | Synthesizer agent prompt builder (single-turn spec generation) | `buildSynthesizerPrompt` |
| `prompt-copilot.ts` | Copilot agent prompt builder. All user-supplied interpolation sites (`title`, `repo`, `spec`, `msg.content`) are now wrapped in `escapeXmlContent()` before insertion into XML boundary tags. | `buildCopilotPrompt` |
| `prompt-constants.ts` | Truncation limits for all prompt builders. Also exports `MAX_NO_COMMITS_RETRIES=3` — hard cap on the `no_commits` retry branch so agents that never commit are not requeued forever. | `PROMPT_TRUNCATION`, `MAX_NO_COMMITS_RETRIES` |
| `prompt-composer-reviewer.ts` | Reviewer-specific prompt builders for Code Review Station. `buildStructuredReviewPrompt` emits a single JSON object; `buildInteractiveReviewPrompt` builds multi-turn conversational prompt. All user-supplied content (`taskContent`, `diff`, `messages[].content`) is wrapped with `escapeXmlContent` to prevent boundary-tag injection. | `buildStructuredReviewPrompt`, `buildInteractiveReviewPrompt`, `buildReviewerPrompt` |
| `pr-operations.ts` | PR find/create operations via gh CLI — check existing, create new with retry/backoff, sanitize titles. `createNewPr` now calls `ensureGithubCredential()` before invoking `gh` so tasks fail fast with the canonical "Run: gh auth login" guidance (or the opted-out message) instead of burning through three retries against a missing CLI. Broadcasts `manager:warning` toast when PR creation fails after all retry attempts. | `generatePrBody`, `sanitizeForGit`, `checkExistingPr`, `createNewPr`, `findOrCreatePR` |
| `worktree.ts` | Worktree setup and cleanup orchestration for pipeline agents. `setupWorktree` acquires a per-repo lock, asserts main-repo cleanliness, fast-forwards local `main` to `origin/main` (skipped with a warning if the main repo is not on branch `main` — prevents ff-merging into an unrelated branch), creates the worktree and branch, symlinks `node_modules`, and reserves disk space. `cleanupWorktree` removes the worktree, deletes the branch, and releases the reservation. `pruneStaleWorktrees` removes orphaned entries. `TASK_ID_HEX_PATTERN` (`/^[0-9a-f]{32}$/i`) is the guard constant — matches BDE's 32-char hex task IDs (`lower(hex(randomblob(16)))`); directories whose names don't match are never pruned. **T-68:** Optional `logger` parameters now default to `createLogger('worktree')` (never to `console`); helper signatures narrowed from `Logger \| Console` to `Logger`. **T-33:** every cleanup-failure log line includes `worktreePath=<path>` so operators can diagnose orphaned trees without scraping surrounding context. **EP-4:** `SetupWorktreeOpts` accepts an optional `appendToNotes` callback; when `git fetch origin main` fails the failure message is appended to the task's notes field via this callback so operators see the context on retry. | `setupWorktree`, `SetupWorktreeOpts`, `SetupWorktreeResult`, `cleanupWorktree`, `CleanupWorktreeOpts`, `pruneStaleWorktrees`, `branchNameForTask`, `branchNameForTaskId`, `branchNameForTaskGroup` |
| `disk-space.ts` | Pre-spawn disk-space gating for worktree setup. `ensureFreeDiskSpace` throws `InsufficientDiskSpaceError` when free bytes are below the threshold and `DiskSpaceProbeError` when the underlying `statfs` call itself fails (ENOENT, EACCES, ENOSYS). **T-99:** probe failures used to be swallowed and treated as a pass — now they log at WARN with the underlying error and propagate so callers can refuse the spawn instead of silently over-committing on an unknown filesystem. `reserveDisk`/`releaseDisk` track in-flight reservations so concurrent spawns do not all race past the same "5 GB free" check. | `ensureFreeDiskSpace`, `InsufficientDiskSpaceError`, `DiskSpaceProbeError`, `reserveDisk`, `releaseDisk`, `getPendingReservation`, `MIN_FREE_DISK_BYTES`, `DISK_RESERVATION_BYTES` |
| `file-lock.ts` | Per-repo PID-based advisory lock used by `setupWorktree` to serialize the conflict-sensitive worktree-add/branch-create/ff-merge operations. `acquireLock` checks liveness via `process.kill(pid, 0)` and reclaims stale locks using a write-then-verify pattern: after `rename` claims the lock it re-reads the file and throws `LockContestedError` if another process won the race (closes the TOCTOU window). `releaseLock` is non-throwing — fires `rm().catch()` fire-and-forget; ENOENT and all other errors log WARN. Both functions accept an optional `logger`; when omitted they fall back to `createLogger('file-lock')` so warnings always reach `~/.bde/bde.log`. **T-77 (Phase C):** `acquireLock` is now `async`, using `readFile`/`writeFile`/`rename`/`rm` from `node:fs/promises` — stale-lock I/O no longer blocks the main thread. | `acquireLock`, `releaseLock`, `LockContestedError` |
| `worktree-lifecycle.ts` | Worktree add/remove/prune and branch delete operations | `listWorktrees`, `removeWorktreeForce`, `pruneWorktrees`, `deleteBranch`, `forceDeleteBranchRef`, `addWorktree`, `cleanupWorktreeAndBranch` |
| `lifecycle-controller.ts` | Owns the four periodic `setInterval` handles that drive the agent-manager loop family. Single responsibility: `startTimers(pollIntervalMs, callbacks, stagger?)` and `stopTimers()`. Extracted from `AgentManagerImpl` so timer management has its own SRP boundary. **T-83 (EP-2):** `startTimers` accepts an optional `TimerStaggerOptions` argument with per-timer `initialDelayMs` values. When set, a `setTimeout` defers the interval start so drain/watchdog/orphan/prune don't all fire at t=0 on startup. `stopTimers()` cancels pending stagger handles. `AgentManagerImpl.start()` passes staggered offsets: drain=0ms, watchdog=100ms, orphan=175ms, prune=250ms. | `LifecycleController`, `LoopCallbacks`, `TimerStaggerOptions` |
| `circuit-breaker.ts` | Spawn-phase circuit breaker. After `SPAWN_CIRCUIT_FAILURE_THRESHOLD` (5) consecutive spawn-phase failures the drain loop pauses for `SPAWN_CIRCUIT_PAUSE_MS` (5min). `recordFailure(taskId, reason)` accumulates a `recentFailures` history; `recordSuccess()` resets both counter and history. **EP-5:** scope tightened to spawn phase only — stream/cleanup failures must not call `recordFailure`. The `circuit-breaker.open` structured event includes `triggeringTask`, `failureCount`, and the full `recentFailures` array for diagnostics. | `CircuitBreaker`, `SPAWN_CIRCUIT_FAILURE_THRESHOLD`, `SPAWN_CIRCUIT_PAUSE_MS` |
| `error-registry.ts` | Groups circuit-breaker state, per-task drain-failure counts, and fast-fail sliding-window tracking behind a single named collaborator. **T-2 (EP-2):** `ErrorRegistry` wraps `CircuitBreaker` + `drainFailureCounts` map. `AgentManagerImpl` holds one instance and delegates all spawn-failure and drain-failure accounting through it. Methods: `isCircuitOpen()`, `circuitOpenUntil`, `recordSpawnSuccess/Failure(taskId?, reason?)`, `getDrainFailureCount/setDrainFailureCount/clearDrainFailureCount()`. **EP-5 (T-57):** also holds `fastFailTracker: FastFailTracker` — timestamped per-task failure window. `FastFailTracker.record(taskId, reason, now?)` stores a failure entry; `recentCount(taskId)` evicts stale entries (older than `FAST_FAIL_THRESHOLD_MS`) and returns the live count; `isExhausted(taskId)` returns `true` at `MAX_FAST_FAILS` threshold. | `ErrorRegistry`, `FastFailTracker` |
| `agent-manager-test-internals.ts` | Stable typed view over the underscore-prefixed members of `AgentManagerImpl` that tests reach into. Tests import via `mgr.__testInternals.<name>` rather than `mgr._<name>`; renames inside `AgentManagerImpl` only need to update the mapping in this file instead of 35+ test sites. **Spying caveat**: `vi.spyOn(manager, '_processQueuedTask')` still uses the underscore name and the assertion needs to use the captured spy variable (the view's delegating wrapper isn't the spy). Use the seam for state read/write and direct method invocation; capture spies as variables for assertion. **T-2/T-6 (EP-2):** exposes `wipTracker`, `errorRegistry`, and `started` via the test seam. | `AgentManagerTestInternals` |
| `index.ts` | `AgentManagerImpl` class — drain loop, watchdog, shutdown, `_validateAndClaimTask` (fresh-status guard), `onTaskTerminal`. Timer handles delegated to `LifecycleController`. `_drainFailureCounts` accessor delegates to `_errorRegistry.drainFailureCounts`. `_drainPausedUntil: number | undefined` marks when the drain is paused; pause events broadcast on `agentManager:drainPaused`. `_consecutiveDrainErrors` counts consecutive drain-tick-level errors; resets on any successful tick; emits `manager:warning` after 3 consecutive failures. **T-6 (EP-2):** `_started` flag guards against duplicate `start()` calls — second call logs WARN and returns immediately; `stop()` resets the flag. **T-2 (EP-2):** holds `_wipTracker: WipTracker` (active-agent count) and `_errorRegistry: ErrorRegistry` (circuit-breaker + drain-failure counts); delegates to both collaborators. `_drainFailureCounts` is now an accessor that returns `_errorRegistry.drainFailureCounts`. **T-34 (EP-2):** `waitForAgentsToSettle(gracePeriodMs)` polls until `_activeAgents.size === 0` or the grace period elapses. **T-83 (EP-2):** `start()` passes `TimerStaggerOptions` to `lifecycle.startTimers()` so drain/watchdog/orphan/prune don't all fire at t=0. **T-21 (write-failure guard):** the last-resort `_spawnAgent` catch block now has a two-level fallback — attempts `{ status: 'error', claimed_by: null }` first; on transition-guard rejection retries with a claim-only `{ claimed_by: null, notes }` patch (no `status` field) so `claimed_by` is always cleared. The status-write failure is logged at WARN (expected race); only the claim-release failure is logged at ERROR. `broadcastToRenderer` is wired into `WatchdogLoopDeps` using the module-level `broadcast` function. **T-10 (Phase A):** `AgentManager` interface adds `cancelAgent(taskId): Promise<void>` — aborts the running agent (delegates to `killAgent`) and logs when no agent is found (no-op). **T-55 (Phase A):** `AgentManager` interface adds `awaitOAuthRefresh(): Promise<void>` — resolves when `_oauthRefreshPromise` settles (both ok and error paths), or immediately if no refresh is in flight. `_oauthRefreshPromise` is set by `onOAuthRefreshStart` injected into `runAgentDeps` and cleared in `.finally()`. | `AgentManagerImpl`, `createAgentManager`, `AgentManager`, `AgentManagerStatus` |
| `run-agent.ts` | Orchestrator — delegates to prompt-assembly, message-consumer, agent-telemetry, spawn-and-wire; re-exports public symbols from extracted modules for backward compat. `cleanupWorktreeWithRetry` retries up to 4 times (100ms/500ms/2000ms backoff). **T-17:** `RunAgentDataDeps` requires `unitOfWork: IUnitOfWork`. **PR-3:** `resolveNormalExit` calls `detectMissingSpecFiles` for re-queue on missing spec paths. **EP-12:** `finalizeAgentRun` emits `logger.event('agent.completed', { taskId, status, durationMs, model, costUsd })` replacing the T-19 string log. `RunAgentSpawnDeps` now carries optional `tickId?: string` threaded to spawn call. **T-16 (EP-2):** Phase 3 (consumeMessages + playground event awaiting + stream error logging) extracted into `runStreamingPhase(StreamingContext): Promise<StreamResult>`. `runAgent` is now a pure orchestrator: validate → pre-spawn tripwire → spawn → stream → finalize. **EP-5 (T-55/57):** `RunAgentSpawnDeps.onSpawnFailure` now receives `(taskId, reason)` so callers can pass structured context to circuit-breaker accounting. `RunAgentFastFailDeps` adds optional `onFastFailRecorded(taskId, reason)` and `isFastFailExhausted(taskId)` callbacks; both are wired to `ErrorRegistry.fastFailTracker` in `AgentManagerImpl`. When supplied, `resolveAgentExit` uses the sliding-window exhaustion check instead of the DB-backed `fast_fail_count` integer. **T-92 (write-failure guard):** `handleIncompleteFiles` and `handleResolveSuccessFailure` both check `result.writeFailed` from `resolveFailure` and skip `onTaskTerminal` when true — prevents dependency resolution against a task whose status was not actually written. **T-55 (Phase A):** `RunAgentSpawnDeps` adds optional `onOAuthRefreshStart?(p): void`; `runStreamingPhase` passes it to `consumeMessages` so auth errors surface the refresh promise to the drain loop. **Phase A (single-terminal-chokepoint):** `abortPhaseUnexpectedly` now uses `deps.taskStateService.transition(taskId, 'error', ...)` instead of `deps.repo.updateTask` so Phase 1/2 abort recovery routes through `TaskStateService`. | `runAgent`, `cleanupWorktreeWithRetry`, `consumeMessages`, `validateTaskForRun`, `assembleRunContext`, `fetchUpstreamContext`, `RunAgentDeps`, `RunAgentFastFailDeps`, `AgentRunClaim`, `ConsumeMessagesResult` |
| `revision-feedback-builder.ts` | Parses raw verification tool output (typecheck stderr, test runner output) into structured `RevisionFeedback` objects. `buildVerificationRevisionFeedback(kind, stderr)` handles `'compilation'` (modern + legacy tsc formats) and `'test_failure'` (FAIL suite headers, vitest ● blocks). `buildMissingFilesRevisionFeedback` and `buildNoCommitsRevisionFeedback` cover the other retry paths. `parseRevisionFeedback(notes)` safely deserialises `task.notes` — returns `null` for legacy freeform strings. `renderRevisionFeedbackBlock(feedback)` formats a `RevisionFeedback` as a `<revision_feedback>` XML block for injection into retry prompts, escaping `</` sequences to prevent boundary-tag injection. | `buildVerificationRevisionFeedback`, `buildMissingFilesRevisionFeedback`, `buildNoCommitsRevisionFeedback`, `parseRevisionFeedback`, `renderRevisionFeedbackBlock` |
| `turn-tracker.ts` | Per-agent token/turn tracking — writes turn records via injected `TurnTrackerDeps.insertTurn(record)` callback. Default deps use `insertAgentRunTurn(getDb(), record)`; tests pass a spy directly, no db/module mocking required. **T-147:** `TurnTrackerDeps` accepts an optional `logger`; default falls back to `createLogger('turn-tracker')` so DB-write failures land in `~/.bde/bde.log`. | `TurnTracker`, `TurnTrackerDeps`, `InsertTurnFn` |
| `agent-event-mapper.ts` (`src/main/`) | Maps SDK wire-protocol messages to AgentEvents and batches them to SQLite. Broadcasts via `broadcastCoalesced` (agent:event:batch channel only — no separate unbatched emit). `flushAgentEventBatcher(db?)` accepts optional db for injection. `tool_result` output field capped at `TOOL_RESULT_OUTPUT_MAX_CHARS` (50 000 chars) before persisting. Recognizes two SDK tool-result formats: (a) the modern `user` message with a `tool_result` content block keyed by `tool_use_id` — the tool name is resolved from the preceding assistant `tool_use` block via a per-agent, bounded (`MAX_TRACKED_TOOL_USE_IDS_PER_AGENT = 1000`) id→name map (EP-15); and (b) legacy top-level `tool_result` messages. `mapRawMessage(raw, agentId?)` accepts an optional `agentId` so concurrent runs do not share tool-name state — callers omitting it fall back to a shared bucket. **EP-15 DLQ sentinel:** when the batch circuit breaker hits `MAX_CONSECUTIVE_FAILURES`, instead of silently dropping the batch the mapper logs a structured WARN (`event-batcher: permanent failure — dropping events {droppedCount, sampleAgentIds, reason}`), increments `getDroppedEventCount()`, and resets the failure counter so a later recovery is not blocked. **EP-15 per-run isolation:** `emitAgentEvent` clears that agent's tool-name map on `agent:started` so stale tool-use IDs from a prior run cannot mis-label the new run. **T-88:** each unrecognized `msgType` is logged at most once per process lifetime via a module-scoped sentinel set — prevents log flooding when the SDK introduces a new control frame. `__resetUnknownMessageTypeSentinel`, `__resetAllToolNameTracking`, and `__resetDroppedEventCount` are exported for test-only fixture reset. | `mapRawMessage`, `emitAgentEvent`, `flushAgentEventBatcher`, `resetAgentToolNames`, `getDroppedEventCount`, `__resetUnknownMessageTypeSentinel`, `__resetAllToolNameTracking`, `__resetDroppedEventCount` |
| `terminal-handler.ts` | Metrics recording, structured event log, and dependency resolution on task terminal events. **T-17 (Phase 1b):** Transactional atomicity comes from an injected `IUnitOfWork`; `wrapTransactionWithLogging` adds the failure-logging wrapper. **EP-12:** emits `logger.event('agent.terminal', { taskId, status, source: 'terminal-handler' })`. **EP-1:** `createAgentTerminalDispatcher(onTaskTerminal, deps)` wraps `handleTaskTerminal` as a `TerminalDispatcher` for injection into `TaskStateService`. | `handleTaskTerminal`, `TerminalHandlerDeps`, `createAgentTerminalDispatcher` |
| `orphan-recovery.ts` | Detects tasks stuck in `active` status without a live agent. Re-queues tasks under `MAX_ORPHAN_RECOVERY_COUNT` (3) with the counter incremented; transitions tasks at the cap to `error` with `failure_reason = 'exhausted: orphan recovery cap reached'` via `TaskStateService.transition` (Phase A invariant `orphan-recovery-via-state-service`). Returns `OrphanRecoveryResult { recovered: string[], exhausted: string[] }`. Suppresses duplicate "has PR, clearing claimed_by" log after first per session. **EP-3** | `recoverOrphans`, `MAX_ORPHAN_RECOVERY_COUNT`, `OrphanRecoveryResult` |
| `commit-message.ts` | Pure function that builds a BDE-convention commit message from a `SprintTask`. Format: `${type}(${scope}): ${subject}\n\nTask-Id: ${task.id}`. Type is mapped from `spec_type`; scope is the basename (no extension, lowercase) of the first path in `## Files to Change`, falling back to `agent`; subject is the task title with any leading `T-N [PN]` / `PR-N [PN]` prefix stripped. | `buildCommitMessage` |
| `completion.ts` | Thin dispatcher for agent task completion. Orchestrates success path (via `resolve-success-phases.ts`) and failure path (via `resolve-failure-phases.ts`). Auto-merge delegated to `auto-merge-coordinator.ts`. **EP-7:** `resolveSuccess` now iterates a `SuccessPhase[]` pipeline — each named phase handles its own error reporting and throws `PipelineAbortError` to abort the pipeline cleanly. Pre-review advisory scans are pluggable via `PreReviewAdvisor { name; advise(ctx): Promise<string\|null> }` — the `preReviewAdvisors` array holds `untouchedTestsAdvisor` and `unverifiedFactsAdvisor`; extend by appending. Decision logs: `completion.noop`, `completion.decision`, `completion.review_transition`. All `execFileAsync('git', …)` calls carry `timeout: GIT_EXEC_TIMEOUT_MS` (30s). **T-92 (write-failure guard):** exported `resolveFailure` wrapper now returns `ResolveFailureResult` instead of `boolean`; re-exports `ResolveFailureResult` type. Internal call sites (`detectNoOpAndFailIfSo`, `verifyWorktreeOrFail`) check `writeFailed` before calling `onTaskTerminal`. | `resolveSuccess`, `resolveFailure`, `findOrCreatePR`, `ResolveSuccessContext`, `ResolveFailureContext`, `ResolveFailureResult` |
| `unverified-facts-scanner.ts` | Pure heuristic scan of a `git diff HEAD~1 HEAD` output for concrete-looking values agents may have invented. Flags: brew tap installs (slash in name), `npm install -g` packages absent from `package.json`, URLs with unapproved hostnames, and pipe-to-shell patterns. Returns `string[]` of warning messages; never throws; no I/O. Advisory only — `completion.ts` appends each warning to `task.notes`. | `scanForUnverifiedFacts` |
| `resolve-success-phases.ts` | Success-path phase functions: worktree verification, branch detection, auto-commit, rebase, commit check, review transition. `autoCommitPendingChanges` takes a `SprintTask` and calls `buildCommitMessage` for a BDE-convention message. When `hasCommitsAheadOfMain` detects zero commits it calls `logUncommittedWorktreeState` (runs `git diff HEAD` + `git status --porcelain` at WARN level under the task id) before requeuing; notes use `NO_COMMITS_NOTE` from `failure-messages.ts`. `hasCommitsAheadOfMain` caps the `no_commits` retry loop at `MAX_NO_COMMITS_RETRIES` — once the cap is reached the task short-circuits to `status='failed'` with `failure_reason='no-commits-exhausted'`. `extractTaskIdFromBranch(branch)` parses the `<idSlug>` from a `agent/t-<idSlug>-<titleSlug>-<groupHash>` branch. `branchMatchesTask` checks both legacy `t-N` task-id tail (e.g. `endsWith('t-13')`) and UUID task-id prefix via the 8-char hex hash at the end of the branch name — covers UUID task ids like `9f04f0d0…`. `assertBranchTipMatches` uses branch-name match as primary signal and falls back to commit-message match; throws `BranchTipMismatchError` only when neither matches. **EP-7:** all `execFileAsync('git', …)` calls carry `timeout: GIT_EXEC_TIMEOUT_MS` (30s). **EP-1:** `CommitCheckContext.resolveFailure` typed as `Promise<ResolveFailureResult>` (was synchronous). `failTaskExhaustedNoCommits` accepts an optional `taskStateService?: TaskStateService` and routes the `failed` write through `TaskStateService.transition(..., caller: 'resolve-success:no-commits-exhausted')` when supplied; falls back to `repo.updateTask` otherwise. `hasCommitsAheadOfMain` passes `opts.taskStateService` through to `failTaskExhaustedNoCommits`. | `verifyWorktreeExists`, `detectAgentBranch`, `autoCommitPendingChanges`, `performRebaseOntoMain`, `hasCommitsAheadOfMain`, `transitionTaskToReview`, `failTaskWithError`, `extractTaskIdFromBranch`, `branchMatchesTask`, `assertBranchTipMatches`, `BranchTipMismatchError`, `RebaseOutcome`, `ReadTipCommit` |
| `resolve-failure-phases.ts` | Failure-path phase: requeue with exponential backoff or mark task permanently failed. Tail-truncates notes to `NOTES_TAIL_CHARS=1500` before DB write so root causes (at end of stack traces) are preserved. **T-92 (write-failure guard):** `resolveFailure` returns `ResolveFailureResult` (a discriminated union) — never throws. Callers must check `writeFailed` and skip `onTaskTerminal` when true to prevent unblocking dependents against a task still `active` in SQLite. **EP-1:** `resolveFailure` is now `async` and routes all status writes through `TaskStateService.transition()` when `ResolveFailureContext.taskStateService` is supplied (falls back to `repo.updateTask` when absent for backward compat). Both the requeue (`queued`) and terminal (`failed`) writes use the callers `'resolve-failure:requeue'` and `'resolve-failure:terminal'`. All callers must `await` the result. | `resolveFailure`, `calculateRetryBackoff`, `ResolveFailureContext`, `ResolveFailureResult` |
| `auto-merge-coordinator.ts` | Evaluates and executes automatic merges after agent completion. Best-effort — failures leave task in `review` for human action. **T-17 (Phase 1b):** Post-merge atomicity now comes from `AutoMergeContext.unitOfWork` (an injected `IUnitOfWork`) rather than a direct `getDb` import — `finalizeAutoMergeStatus` wraps the task status write in `unitOfWork.runInTransaction`, preserving the loud `[auto-merge] COMMIT LANDED ON MAIN but status update failed` banner on DB failure after a successful merge. Tested via `auto-merge-coordinator.test.ts`. | `evaluateAutoMerge`, `AutoMergeContext` |
| `auto-merge-policy.ts` | Pure rule evaluation for auto-merge. Computes the CSS-only flag (every changed path is `.css`/`.scss`) so downstream callers can skip the full pre-push test suite. Returns `AutoMergeDecision` (merge with matched rule name) or `AutoMergeSkip`. | `evaluateAutoMergePolicy`, `isCssOnlyChange`, `AutoMergePolicyResult`, `AutoMergeDecision`, `AutoMergeSkip` |
| `partial-diff-capture.ts` | Captures partial diffs from failed/cancelled agents for diagnostic notes | `capturePartialDiff` |
| `review-transition.ts` | Transitions a completed task to `review` status, preserving worktree path and branch. Resets `fast_fail_count` to 0 on every successful review transition so a task that fast-failed on earlier attempts does not carry a stale counter into the next retry. **EP-7:** emits `logger.event('completion.review_transition', { taskId, rebaseSucceeded, hasRebaseNote, commitsAhead })` before writing the status update — `commitsAhead` is the count of agent commits ahead of `origin/main` (or `null` when the count cannot be obtained). All `execFileAsync('git', …)` calls carry `timeout: GIT_EXEC_TIMEOUT_MS` (30s). | `transitionToReview`, `TransitionToReviewOpts` |
| `task-mapper.ts` | Maps raw sprint task rows to `MappedTask` shape (used as `AgentRunClaim`) and evaluates hard-dependency blocking. Parses `revision_feedback` JSON from DB rows. `checkAndBlockDeps` routes `rawDeps` through the shared `sanitizeDependsOn` helper, which tolerates string-or-array JSONB shapes and silently discards malformed entries — an unparseable string or fully-invalid array is treated as "no deps" rather than marking the task `error`. | `mapQueuedTask`, `checkAndBlockDeps`, `MappedTask` |
| `dependency-refresher.ts` | Rebuilds the in-memory dependency index from SQLite; debounced on task mutations. **EP-9:** accepts an optional `dirtyTaskIds?: Set<string>` hint — when provided, tasks outside the set whose fingerprint is already cached skip the per-task fingerprint recompute. Deletion sweep and terminal eviction always run over the full set so a caller-supplied dirty hint can never leak stale fingerprints. | `refreshDependencyIndex`, `computeDepsFingerprint` |
| `types.ts` | Shared type definitions for agent manager internals. `ActiveAgent` now includes `worktreePath` and `branch` fields populated at spawn time. `DRAIN_PAUSE_ON_ENV_ERROR_MS` (30s) configures how long the drain pauses after an environmental failure. **EP-6 (T-73):** `SpawnStrategy` discriminated union added — `{ type: 'sdk' }` or `{ type: 'cli'; claudePath: string }`. | `ActiveAgent`, `AgentHandle`, `AgentManagerConfig`, `ResolveDependentsParams`, `SpawnStrategy`, `DRAIN_PAUSE_ON_ENV_ERROR_MS` |
| `drain-loop.ts` | Polling orchestration — precondition checks, dep-index refresh, queued-task fetching and processing. Preconditions: shutdown flag, circuit breaker, no configured repos (skips with warning), OAuth token validity, drain-pause gate (`drainPausedUntil`). Slot check uses `activeAgents.size + getPendingSpawns()` to prevent over-claim during async spawn window. Full dep-index rebuild wrapped in try/catch. Environmental failures classified via `classifyFailureReason`. Spec-level failures quarantine after `DRAIN_QUARANTINE_THRESHOLD` (3) throws. **EP-12:** `runDrain` generates a `tickId` (short random hex) at the top of each invocation and stores it on `DrainLoopDeps.tickId`; when no tasks are available, emits `logger.event('drain.tick.idle', { tickId, queuedCount: 0 })` at DEBUG level instead of a noisy INFO heartbeat. `tickId` threads to spawn calls via `processQueuedTask` closure for cross-event correlation. **T-1 (EP-2):** `drainQueuedTasks` wraps `repo.getQueuedTasks()` in `Promise.race` with `DRAIN_TICK_TIMEOUT_MS` (10s). On `DrainTimeoutError`, the tick is skipped and `logger.event('drain.tick.timeout', { tickId })` is logged so the drain loop can never hang forever on a locked WAL or stalled filesystem. **T-55 (Phase A):** `DrainLoopDeps` adds optional `awaitOAuthRefresh?(): Promise<void>`; `drainQueuedTasks` awaits it before each spawn so the refreshed token is on disk before the next `node` process reads `~/.bde/oauth-token`. | `runDrain`, `validateDrainPreconditions`, `buildTaskStatusMap`, `drainQueuedTasks`, `DrainLoopDeps`, `DRAIN_QUARANTINE_THRESHOLD`, `DRAIN_TICK_TIMEOUT_MS`, `DrainTimeoutError`. **EP-9:** `DrainLoopDeps.recentlyProcessedTaskIds: Set<string>` carries IDs of tasks claimed in the previous tick into the next tick's `buildTaskStatusMap`, which forwards them as the dirty-set hint to `refreshDependencyIndex` and clears the set |
| `watchdog-loop.ts` | Agent health checks — idle/timeout/rate-limit/cost verdicts, kill helpers. **T-32:** `softKillAgent` issues `handle.abort()`; `forceKillAgent` escalates via `handle.forceKill()` / subprocess SIGKILL / abort fallback with explicit "forceKill applied" log; `killAgentWithEscalation` wires both with `FORCE_KILL_DELAY_MS` (5s) grace window. `removeAgentFromMap` does map removal. `runWatchdog` (async): escalation kill → flush → DB write → remove → flush → notify terminal. **EP-12:** kill path emits `logger.event('agent.watchdog.kill', { taskId, runtimeMs, limitMs, agentType, verdict })` replacing the T-30 string warn. **EP-5 (T-29):** Idempotency guard at the top of the kill loop: if `activeAgents.get(taskId)?.agentRunId !== agent.agentRunId` the agent was already removed or replaced (e.g. by orphan recovery racing in), the watchdog logs DEBUG and skips the terminal dispatch to prevent double-notify. **T-35 (write-failure guard):** DB write is wrapped in `withRetryAsync` (from `sqlite-retry.ts`) for SQLITE_BUSY resilience. On persistent failure, `deps.broadcastToRenderer?.('manager:warning', { message })` is called and the function returns early — `onTaskTerminal` is NOT called when the write did not land. `WatchdogLoopDeps` gains an optional `broadcastToRenderer?: (channel, payload) => void` field. | `runWatchdog`, `killActiveAgent`, `abortAgent`, `softKillAgent`, `forceKillAgent`, `killAgentWithEscalation`, `FORCE_KILL_DELAY_MS`, `removeAgentFromMap`, `WatchdogLoopDeps` |
| `already-done-check.ts` | Pre-claim guard helper for the drain loop. `taskHasMatchingCommitOnMain(task, repoPath, logger)` scans the last 200 commits on `origin/main` and returns the first SHA whose subject or body references the task via any of three criteria (OR'd): `(T-<taskId>)` token in subject or body, subject equal to `task.title`, or `agent-run-id: <runId>` trailer in subject or body. Returns `{ sha, matchedOn }` or `null`. The `git log` invocation passes `maxBuffer: 16 MiB` so verbose monorepo history can never silently overflow the default 1 MB cap. Parsed commits are memoized per `repoPath` for `ALREADY_DONE_CACHE_TTL_MS` (5 s) so a single drain tick scanning N queued tasks against the same repo collapses to one git invocation. Errors from `git log` are swallowed (empty list, not cached) so a transient repo problem never blocks the drain tick. `__resetAlreadyDoneCache` is test-only. | `taskHasMatchingCommitOnMain`, `AlreadyDoneTask`, `AlreadyDoneMatch`, `ALREADY_DONE_CACHE_TTL_MS`, `__resetAlreadyDoneCache` |
| `task-claimer.ts` | Task claim pipeline — fresh-status guard, dep blocking, repo path resolution, pre-claim already-done check (via `already-done-check.ts`), worktree setup, agent spawn. When `taskHasMatchingCommitOnMain` returns a match, the task is auto-completed (`status='done'`) via `taskStateService.transition(..., caller: 'task-claimer:auto-complete')` rather than being claimed — prevents re-execution of work that has already landed. Passes `config.maxConcurrent` to `claimTask` for atomic DB-level WIP enforcement. `spawnAgent` callback typed `Promise<void>` (fire-and-forget; call site awaits for accurate slot accounting). **EP-9:** post-claim status-map refresh now uses `repo.getTask(claimedId)` (single PK lookup) instead of a full-catalog `getTasksWithDependencies` rescan; successfully claimed task IDs are recorded into the optional `recentlyProcessedTaskIds` set so the next drain tick's dependency refresh can use them as a dirty-set hint. **T-95 (write-failure guard):** `skipIfAlreadyOnMain` returns `false` on transition failure without calling `onTaskTerminal`, preventing false dependency resolution against a task still in its prior status. **EP-1:** `TaskClaimerDeps` requires `taskStateService: TaskStateService` (required, not optional); the composition root (`index.ts`) wires `this._taskStateService`. The auto-complete path uses the `queued → done` edge added to `VALID_TRANSITIONS` by EP-1. | `validateAndClaimTask`, `prepareWorktreeForTask`, `processQueuedTask`, `resolveRepoPath` |
| `worktree-manager.ts` | Worktree prune pass and review-status check helper | `runPruneLoop`, `checkIsReviewTask`, `WorktreeManagerDeps` |
| `shutdown-coordinator.ts` | Graceful shutdown — waits for drain, aborts agents, re-queues active tasks, flushes event batcher. **T-34 (EP-2):** re-queue step now checks task status before writing; tasks already in `review` status are skipped (their agent work is complete and awaiting human review — a shutdown must not discard that). | `executeShutdown`, `ShutdownCoordinatorDeps` |
| `config-manager.ts` | Hot-reload settings — updates maxConcurrent, maxRuntimeMs, defaultModel in place; flags worktreeBase as restart-required | `reloadConfiguration`, `ConfigManagerDeps` |
| `wip-tracker.ts` | Thin facade over ConcurrencyState for readable slot queries, plus `WipTracker` class. **T-2 (EP-2):** `WipTracker` class wraps the active-agent count behind named operations: `count` (live read via callback), `isFull(max)`. Used by `AgentManagerImpl` as `_wipTracker`. | `createConcurrencyState`, `getAvailableSlots`, `updateMaxSlots`, `WipTracker` |
| `failure-classifier.ts` | Maps agent error notes to a `FailureReason` via a keyword-based pattern registry. Patterns cover auth, timeout, test_failure, compilation, spawn, no_commits, incomplete_files, and unknown. The `environmental` pattern includes Ollama-specific errors (`model not found`, `failed to connect to ollama`, `failed to pull model`). **EP-5 (T-58):** `classifyFailureReason` now accepts optional `logger` and `taskId` parameters; when a pattern matches it emits `logger.debug('[failure-classifier] matched pattern "<type>" verdict=<type> taskId=<id>')` for operator diagnostics. `FailurePattern` gains an optional `name` field for a human-readable pattern label. **EP-16:** Added `resetRegistryToBuiltins()` for test isolation — call in `afterEach` when tests register custom patterns. | `classifyFailureReason`, `registerFailurePattern`, `resetRegistryToBuiltins`, `FailurePattern` |
| `failure-messages.ts` | Centralised user-visible failure notes written to the task `notes` column. `FAST_FAIL_EXHAUSTED_NOTE` surfaces on triple fast-fail (directs users to `~/.bde/bde.log` and the Retry button in the Task Detail drawer). `NO_COMMITS_NOTE` surfaces when the agent exits without committing. `NOOP_RUN_NOTE` surfaces when the agent committed only scratch files. | `FAST_FAIL_EXHAUSTED_NOTE`, `NO_COMMITS_NOTE`, `NOOP_RUN_NOTE` |
| `spec-parser.ts` | Pure parser for sprint task spec markdown. Extracts the list of file paths declared in the `## Files to Change` section, handling backtick-quoted, bare, parenthetical-annotated, and em-dash-suffixed bullet variants. Returns `[]` when the section is absent. No I/O — safe to import anywhere. | `extractFilesToChange` |
| `resolve-node.ts` | Resolves an absolute path to a usable `node` binary for packaged `.app` bundles whose PATH lacks fnm/nvm/Homebrew install locations. Probes Electron's bundled node, fnm default alias, highest nvm version, then Homebrew — used by `sdk-adapter.ts` to augment PATH before the SDK spawns its cli.js. | `resolveNodeExecutable` |
| `turn-budget.ts` | Pipeline agent SDK tuning: returns `maxTurns` (75 default; 100 when the spec opts in via `## Multi-File: true`) and exports the `PIPELINE_DISALLOWED_TOOLS` list. The 30-turn default and the 50-turn mixed-stack auto-detection were removed in the Phase-B 2026-04-24 change; the multi-file ceiling was raised from 75 to 100 in the follow-up after measurement showed 75 was the binding operating point, not a margin. Recon blocks: `Bash(git log:*)`, `Bash(git status:*)`, `Bash(git ls-remote:*)`, `Bash(git diff:*)`, `Bash(git reflog:*)`, `Bash(git log --grep:*)`. Network/exfil blocks: `Bash(curl:*)`, `Bash(wget:*)`, `Bash(ssh:*)`, `Bash(scp:*)`, `Bash(nc:*)`, `Bash(gh:*)`, `WebFetch`, `WebSearch`. Pipeline-only — other agent types use their own spawn paths. | `computeMaxTurns`, `PIPELINE_DISALLOWED_TOOLS` |
| `test-touch-check.ts` | Advisory pre-review heuristic — lists files changed between the agent branch and `origin/main`, then flags any changed `.ts/.tsx/.js/.jsx` source file whose sibling test (`<name>.test.<ext>` next to the file or in `__tests__/`) exists but was not updated. Never blocks the review transition; the warning is appended to the task's `notes` field so the human reviewer sees it in Code Review Station. | `detectUntouchedTests`, `listChangedFiles`, `formatAdvisory` |
| `backend-selector.ts` | Per-agent-type runtime resolution. `resolveAgentRuntime(agentType, settings) → { backend, model }` reads from a SQLite-backed JSON setting under `agents.backendConfig`. Every agent type (Pipeline, Synthesizer, Copilot, Assistant, Adhoc, Reviewer) routes through this helper; backends are `claude`, `local`, or `opencode`. `DEFAULT_SETTINGS.opencodeExecutable` is `'opencode'` (PATH lookup); `mergeWithDefaults` fills the field from defaults when the stored value is absent. | `BackendSettings`, `AgentBackendConfig`, `BackendKind`, `loadBackendSettings`, `saveBackendSettings`, `resolveAgentRuntime`, `DEFAULT_SETTINGS`, `SETTING_BACKEND_CONFIG` |
| `local-adapter.ts` | Thin pass-through to the `rbt-coding-agent` framework's BDE adapter (`rbt-coding-agent/adapters/bde`). Dynamic `import()` keeps the file-dependency out of Electron's build-time bundle graph. Scopes `OPENAI_API_BASE` around the spawn and restores the previous value on exit (including on error). The returned handle's `messages` iterable emits `SDKWireMessage`-shaped objects, so the existing drain loop / event mapper / cost tracker / playground detection all work unchanged. | `spawnLocalAgent`, `LocalSpawnOptions` |
| `noop-detection.ts` | Post-commit guard that classifies an agent run as a no-op when the diff vs `origin/main` contains only Aider scratch artefacts (`.aider*` files and a `.gitignore` whose lines all start with `.aider`). Called from `completion.resolveSuccess` after `hasCommitsAheadOfMain`; on `true`, routes the task through `resolveFailure` with notes `"produced only scratch files"` rather than transitioning to `review`. Empty change lists are treated as not-a-no-op so `hasCommitsAheadOfMain` remains the single authority for the empty-commit path. | `detectNoOpRun`, `NoOpDetectionDeps` |
| `verify-worktree.ts` | Pre-review verification gate. Runs `npm run typecheck` then `npm test --run` inside the worktree after the agent commits. On failure, returns a `VerificationFailure` classified as either `compilation` or `test_failure`, with the tool's stderr tail-truncated to 2000 chars. `completion.resolveSuccess` routes failures through `resolveFailurePhase` (requeue with notes) instead of promoting to `review`. Accepts an injectable `runCommand` so the module can be unit-tested without spawning real npm processes. | `verifyWorktreeBuildsAndTests`, `VerificationResult`, `VerificationFailure`, `RunCommand`, `VERIFICATION_STDERR_LIMIT` |
| `worktree-isolation-hook.ts` | SDK `CanUseTool` gate used by pipeline + adhoc agents. Checks absolute paths in Bash command tokens and now also checks paths that follow flag tokens (`-o`, `--output`, `-f`, `--file`, `--log-file`, `--config`) — blocks `curl -o /home/user/exfil` patterns. | `createWorktreeIsolationHook`, `WorktreeIsolationDeps` |

## Agent Personality System

Source: `src/main/agent-system/personality/`

Defines voice, role frame, constraints, and communication patterns for each agent type. Personalities are injected into prompts during agent initialization via `buildAgentPrompt()` in `prompt-composer.ts`.

| Personality | Purpose |
|---|---|
| `pipeline-personality.ts` | Concise, action-oriented pipeline executor — focused on spec compliance and test verification |
| `assistant-personality.ts` | BDE Project Assistant — pipeline-aware advisor for sprint health, task status, and diagnostics. Leads with answers, checks state before responding, recommends based on actual data |
| `adhoc-personality.ts` | BDE Dev Agent — conversational coding partner with full tool access, working directly in the user's repository. Emphasizes execution, commits frequently, and suggests sprint pipeline tasks for autonomous work |
| `copilot-personality.ts` | Text-only spec drafting helper — conversational, non-tool-using (no file access, no exploration) |
| `synthesizer-personality.ts` | Structured spec generator from user intent — single-turn, receives code snippets + file tree, outputs markdown with heading sections |
| `bde-advisor-personality.ts` | BDE Advisor — always-available floating agent. Concise and diagnostic; leads with real data. Full read access; does NOT make code changes without explicit request. Suggests Adhoc session for coding work. |
| `types.ts` | `AgentPersonality` interface: `voice`, `roleFrame`, `constraints[]`, `patterns[]` |
