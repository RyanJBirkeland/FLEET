# Agent Manager

Pipeline agent lifecycle orchestration — drain loop, worktree management, watchdog, completion handling.
Source: `src/main/agent-manager/`

| Module | Purpose | Key Exports |
|--------|---------|-------------|
| [`opencode-wire.ts`](opencode-wire.md) | Pure translation layer: opencode `--format json` stdout lines → Anthropic SDK wire message objects. Event type mapping: `text`→assistant text, `tool`→assistant tool_use + user tool_result, `step_finish(stop)`→result, `error`→assistant error text. No I/O, no side effects — every function is a pure value-in/value-out transform. **T-43:** `parseOpencodeEvent` validates the `JSON.parse` result through `isOpencodeEvent` before use — `null`, arrays, numbers, and objects without a string `type` field are rejected with `undefined`. **T-44:** `isOpencodeTextPart`/`isOpencodeToolPart`/`isOpencodeStepFinishPart` now use `in`-operator + `typeof` guards (no intermediate `as` cast) so the TypeScript compiler enforces the check rather than lying about it. | `translateOpencodeEvent`, `extractOpencodeSessionId` |
| `resolve-git.ts` | Git binary resolution for packaged Electron builds. Probes well-known install locations (Apple Silicon Homebrew, Intel Homebrew, Xcode CLT, system `/usr/bin/git`) in priority order and returns the first usable absolute path. Mirrors `resolve-node.ts`. **Epic 5 (T-25):** `hasNoCommitsAheadOfMain(revListOutput)` extracts the `parseInt(...) === 0` zero-check so all rev-list callers parse consistently. | `resolveGitExecutable`, `hasNoCommitsAheadOfMain` |
| `sdk-message-protocol.ts` | SDK wire protocol type guards and field accessors. `asSDKMessage` rejects messages whose outer shape contradicts the declared type — `message` must be an object if present, and `message.content` must be an array if present — so consumers never receive a value that lies about its container shape. | `SDKWireMessage`, `asSDKMessage`, `getNumericField`, `getSessionId`, `isRateLimitMessage` |
| `sdk-policy.ts` | Centralised SDK `settingSources` policy. Exports `INTERACTIVE_AGENT_SETTINGS_SOURCES = ['user', 'local']` (pipeline, adhoc, assistant, reviewer) and `TEXT_HELPER_SETTINGS_SOURCES = []` (copilot, synthesizer, spec validators). All agent spawn sites import from here — policy changes require editing exactly one file. Rationale: `'project'` is excluded everywhere because FLEET conventions are injected via `buildAgentPrompt()`; `'user'` and `'local'` give interactive agents the user's hooks, file-based MCP servers, and permissions. See #712 for managed-connector limitation. | `INTERACTIVE_AGENT_SETTINGS_SOURCES`, `TEXT_HELPER_SETTINGS_SOURCES` |
| `spawn-sdk.ts` | SDK-based agent spawn — session ID extraction, abort wiring, steer stub. Exports `MAX_TURNS = 20` used by both SDK config and message consumer. **T-79:** the SDK-mode steer warn log reports only `message length: <n>` — the message body is never logged, since steer content is user-supplied and project policy keeps prompts/specs out of `~/.fleet/fleet.log`. **EP-12:** emits a structured `logger.event('agent.spawn', { taskId, tickId, agentType, model, maxBudgetUsd, cwd })` at spawn time. **EP-6 (T-78):** `agent.spawn` event now includes `backend: strategy.type` from the `SpawnStrategy` parameter — callers (sdk-adapter) resolve the strategy before calling `spawnViaSdk` and pass it explicitly. **contract-test fix:** `wrapMessages` now uses a `sessionIdResolved` flag so only the first message carrying a `session_id` updates `resolvedSessionId`; subsequent messages with a different `session_id` are ignored. **Epic 3 (T-32):** `spawnViaSdk` opts accepts `mainRepoPaths?: readonly string[]` — removes direct `getRepoPaths()` call; caller (via `spawn-and-wire.ts`) passes resolved paths at spawn time. | `spawnViaSdk`, `MAX_TURNS` |
| `spawn-cli.ts` | CLI fallback spawn — stream-json protocol, V8 heap cap, stdin steer. Accepts optional `maxBudgetUsd` and passes it via `CLAUDE_MAX_COST_USD` env var to the CLI process. **T-34:** `spawnViaCli` validates `opts.model` against `/^claude-[a-z0-9-]+$/` before spawn — rejects flag-like values (e.g. `--print /etc/passwd`), empty strings, and non-claude identifiers with a clear `Invalid model ID` error. **T-35:** `_logger` parameter typed as `Logger | undefined` (was `unknown`) — eliminates an `any`-equivalent footgun on the public API surface. | `spawnViaCli`, `withMaxOldSpaceOption`, `AGENT_PROCESS_MAX_OLD_SPACE_MB` |
| `opencode-session-mcp.ts` | Per-session MCP HTTP server for opencode agents. Binds to 127.0.0.1 on a random port with a per-session token; enforces `Authorization: Bearer <token>` using `crypto.timingSafeEqual` (prevents timing side-channel). Exposes the same tasks/epics/meta tools as the in-process planner server used by Claude agents. Torn down when the session closes. | `startOpencodeSessionMcp`, `OpencodeSessionMcpHandle` |
| `opencode-worktree-config.ts` | Writes `.opencode/opencode.json` into the worktree with the per-session MCP server URL + token, and builds the opencode-specific first-turn prompt (branch name + commit rules prepended to the raw user task). | `writeOpencodeWorktreeConfig`, `buildOpencodeFirstTurnPrompt` |
| `spawn-opencode.ts` | opencode CLI spawn adapter — spawns `opencode run --format json`, pipes stdout line-by-line through `translateOpencodeEvent`, captures `sessionID` from the first event, and returns an `AgentHandle`. Steer is intentionally unsupported (one-shot CLI). **T-33:** prompt is now placed after a `--` separator in the argument list so CLI option parsers cannot interpret flag-like prompt content (e.g. `--model`, `--session`) as opencode flags. | `spawnOpencode`, `OpencodeSpawnOptions` |
| `sdk-adapter.ts` | (T-64 silent-failure) When the local backend throws and we fall back to Claude, `manager:warning` is broadcast once per `agentType:reason` per 5 minutes so the user is informed without spam. Thin facade over spawn-sdk, spawn-cli, spawn-opencode, and local-adapter. `spawnAgent` reads `loadBackendSettings()`+`resolveAgentRuntime()` and routes to the correct backend. The opencode branch: starts a per-session MCP server (`startOpencodeSessionMcp`), writes worktree config (`writeOpencodeWorktreeConfig`), builds a lightweight branch-prefixed first-turn prompt (`buildOpencodeFirstTurnPrompt`) when `branch` is provided, spawns opencode, and wraps the messages iterable to close the MCP server on exhaustion. If `spawnOpencode` throws before returning a handle, the MCP server is closed immediately in a catch block to prevent port leaks. `spawnWithTimeout` wraps `spawnAgent` with a `SPAWN_TIMEOUT_MS` deadline. **T-77/T-27:** `assertCwdIsInsideWorktreeBase` now distinguishes two refusal reasons: when `resolvePhysicalPath(cwd)` returns null the error says "does not exist or is not accessible" (ENOENT / worktree never set up); when the physical path resolves outside the base the error says "not inside the configured worktree base" (path traversal attempt). Both cases refuse the spawn. Symlink defense: `realpathSync` on both cwd and base closes the lexical-prefix bypass. **Logger fix:** `resolvePhysicalPath` falls back to a module-scope `createLogger('sdk-adapter')` instance so failure diagnostics always reach `~/.fleet/fleet.log`. **EP-6 (T-73/74):** `resolveSpawnStrategy()` resolves SDK availability and returns a `SpawnStrategy` discriminated union. **extraEnv:** `spawnAgent` and `spawnWithTimeout` accept an optional `extraEnv?: Record<string, string>`. **Epic 5 (T-26):** `spawnWithTimeout` accepts `SpawnWithTimeoutOpts` object instead of 12 positional parameters. **Epic 7 (T-72):** `spawnWithTimeout` destructuring spread across one field per line for readability. `withMcpCleanup` typed as `AsyncGenerator<SDKWireMessage>` now that `AgentHandle.messages` is typed. | `spawnAgent`, `spawnWithTimeout`, `SpawnWithTimeoutOpts`, `SpawnStrategy`, `SDKWireMessage`, `asSDKMessage`, `getNumericField`, `getSessionId`, `isRateLimitMessage`, `AGENT_PROCESS_MAX_OLD_SPACE_MB`, `withMaxOldSpaceOption` |
| `pipeline-abort-error.ts` | Typed sentinel thrown by Phase 1 / Phase 2 helpers after completing their own recovery. `runAgent` catch blocks check `instanceof PipelineAbortError` to distinguish an expected early exit (no double cleanup) from an unexpected error that needs claim-release and terminal notification. `message` defaults to `'pipeline aborted'` so callers signalling an already-handled abort can throw with no args. Single canonical definition — `success-pipeline.ts` re-exports it from here so all `instanceof` checks resolve to the same class. | `PipelineAbortError` |
| `prompt-assembly.ts` | Task validation + prompt context prep — upstream context, scratchpad, prompt build | `validateTaskForRun`, `assembleRunContext`, `fetchUpstreamContext`, `readPriorScratchpad` |
| `message-consumer.ts` | SDK message stream iteration, OAuth refresh on auth error, playground path accumulation. Hard turn limit via `maxTurns` param (aborts with `max_turns_exceeded`); per-turn budget enforcement aborts if `agent.costUsd >= agent.maxCostUsd`. `pendingPlaygroundPaths` typed as `PlaygroundWriteResult[]` (includes `contentType`). **T-40:** `consumeMessages` decomposed into named sub-functions — `buildStalledStreamResult` (stall handling), `enforceMaxTurns` (turn budget), `enforceCostBudget` (cost budget), `handleAuthError` (OAuth refresh path), `handleStreamError` (catch-block orchestration); `MessageLoopContext` carries shared mutable state so each function reads at one level of abstraction. **EP-12:** stream-error path emits `logger.event('agent.stream.error', ...)`. **EP-6 (T-70):** per-message stream deadline via `nextOrStall()`. **T-55, T-102, T-103, T-41, T-42:** OAuth, perf, and error improvements. **Epic 3 (T-29):** `stallTimeoutMs` is now a parameter of `consumeMessages` (default `MESSAGE_STALL_TIMEOUT_MS`) — the module-level mutable and `__setStallTimeoutMsForTesting`/`__resetStallTimeoutForTesting` exports removed; tests pass the timeout directly. **Epic 5 (T-28):** auth-error detection in `handleStreamError` delegates to `classifyFailureReason` instead of 5 inline `.includes()` checks — single source of truth for auth patterns. **Epic 1:** `consumeMessages` accepts a single `ConsumptionContext` object instead of 9 positional parameters. | `consumeMessages`, `ConsumptionContext`, `ConsumeMessagesResult`, `OAuthRefreshFailedError`, `MESSAGE_STALL_TIMEOUT_MS` |
| `agent-telemetry.ts` | Cost/token tracking from SDK messages, SQL persistence of run telemetry. `computeTokenCost` estimates cost from token totals when the SDK subprocess exits before yielding the `result` message (e.g. maxTurns abort or OS signal). `persistAgentRunTelemetry` uses the SDK-reported cost when non-zero and falls back to `computeTokenCost` otherwise. | `trackAgentCosts`, `persistAgentRunTelemetry`, `computeTokenCost` |
| `metrics.ts` | Closure-based `MetricsCollector` implementation. Counters: `agentsCompleted`, `agentsFailed`, `watchdogKills`, and `duration_<bucket>` (five time buckets: <30s, <2m, <10m, <30m, >30m). `snapshot()` returns a plain object copy; `reset()` zeros all counters. `recordAgentDuration(durationMs)` buckets finished-agent runtime; called from `run-agent.ts` finalizeAgentRun after every completion. | `createMetricsCollector`, `MetricsCollector` |
| `agent-initialization.ts` | Agent record creation, stderr wiring, activeAgents registration, agent:started event, and structured `agent.spawned` log emitted via `logger.event('agent.spawned', { taskId, agentRunId, model, timestamp })` — parseable NDJSON, joinable by `taskId`/`agentRunId`. `createAgentRecord` failure is logged at `error` level (fire-and-forget: function is synchronous; untracked run is surfaced loudly so operators can investigate). `task.max_runtime_ms` is clamped to `[60_000, 86_400_000]` before being stored in `ActiveAgent.maxRuntimeMs` — zero or negative values fall back to `null` (uses config default); values above 24 hours are capped. | `initializeAgentTracking` |
| `spawn-and-wire.ts` | Spawn orchestration and error recovery — calls `credentialService.refreshCredential('claude')` immediately before `spawnWithTimeout` (V0.6 race fix) and surfaces the `CREDENTIAL_GUIDANCE` actionable message in the task's `notes` on failure, then `initializeAgentTracking` on success. Calls `deps.onAgentRegistered?.()` after `initializeAgentTracking` so `AgentManagerImpl` can decrement `_pendingSpawns` the moment the agent enters `activeAgents`. Passes `worktree.branch` to `spawnWithTimeout` so the opencode backend can build a lightweight branch-prefixed first-turn prompt. **EP-5 (T-55):** `handleSpawnFailure` calls `deps.onSpawnFailure(task.id, errReason)` with the task ID and error message so callers receive structured context for circuit-breaker accounting. **extraEnv:** calls `getRepoEnvVars(task.repo)` just before `spawnWithTimeout` and forwards the result as the 11th argument so per-repo env vars from Settings reach the agent process. `catch` block uses `return await handleSpawnFailure(...)` — the `never` return type narrows out the dead branch; unreachable `throw` statements removed. | `spawnAndWireAgent`, `handleSpawnFailure` |
| `playground-sanitize.ts` (`src/main/`) | DOMPurify-based HTML sanitizer. `<meta>` and `<link>` removed from allowlist (prevented HTTP-refresh redirect and resource-fetch vectors). `afterSanitizeAttributes` hook strips `url(...)` from inline `style` attributes to block CSS-based data exfiltration. `ALLOWED_URI_REGEXP` added to DOMPurify config: permits safe schemes (https, http, mailto, tel, data URIs, relative paths) while blocking `javascript:` and other execution schemes in `<img src>` and `<a href>` attributes. | `sanitizePlaygroundHtml` |
| [`playground-handler.ts`](playground-handler.md) | Detects playground file writes from agents (.html, .svg, .md, .markdown, .json), reads and sanitizes the file via DOMPurify, emits `agent:playground` events through `emitAgentEvent` (so they persist to SQLite AND reach the renderer via the batched broadcast path — the old direct `broadcast('agent:event', …)` call dropped events because the renderer only listens to `agent:event:batch`). Detection is session-scoped: `createPlaygroundDetector().onMessage(msg)` pairs each Write `tool_use` block with its matching `tool_result` block (by `tool_use_id`) so hits only fire after the file is confirmed written. Supports three tool naming conventions: Claude SDK `Write` (snake_case `file_path`), opencode `edit` (camelCase `filePath`), and opencode `apply_patch` (`*** Add File:` lines in `patchText`). `tryEmitPlaygroundEvent` takes a `PlaygroundEmitRequest` object; pass `allowAnyPath: true` for adhoc/assistant sessions where the user may render files outside the worktree. Uses `realpath()` (not `resolve()`) to follow symlinks before containment check, preventing traversal via symlinks. **T-8:** `stat()` and `readFile()` both use `resolvedPath` (not `absolutePath`) to close the TOCTOU race window between path resolution and I/O. **T-9:** All `as string | undefined` casts on SDK wire message inputs replaced with `typeof x === 'string'` guards in `matchLegacyTopLevelToolResult` and `resolvePlaygroundWriteForTool`. **T-10:** `PlaygroundEmitRequest` accepts an optional `resolvedWorktreePath` — callers that spawn many playground events per session can pre-resolve once and pass it here to skip a redundant `realpath()` syscall per emit. | `createPlaygroundDetector`, `PlaygroundDetector`, `detectPlaygroundWrite` (legacy top-level format only), `detectHtmlWrite` (backward-compat alias), `tryEmitPlaygroundEvent`, `PlaygroundEmitRequest`, `PlaygroundWriteResult`, `PlaygroundContentType` |
| `oauth-checker.ts` | Thin boolean adapter over `CredentialService` — delegates to `getDefaultCredentialService(...).getCredential('claude')`. Returns `true` on `status: 'ok'`, `false` otherwise, logging the service's canonical `actionable` message ("Run: claude login" etc.) on the warning. All caching, file reads, and proactive Keychain refresh logic now lives inside the service. TTL constants stay exported for test expectations. | `checkOAuthToken`, `invalidateCheckOAuthTokenCache`, `OAUTH_CHECK_CACHE_TTL_MS`, `OAUTH_CHECK_FAIL_CACHE_TTL_MS` |
| `prompt-sections.ts` | Shared prompt section builders and constants. `CODING_AGENT_PREAMBLE` includes a "Data vs. Instructions" directive listing all XML boundary tags as user-provided data. `escapeXmlContent` escapes closing tags (`</`) as `&lt;/` (standard XML entity encoding — unambiguous regardless of tokenizer) and opening tags (`<[a-zA-Z]`) with a backslash prefix; `>` → `&gt;`; `<` before digits/spaces left untouched to preserve diff output. **T-42:** closing-tag escape changed from non-standard `<\/` to `&lt;/` for unambiguous encoding. `buildUpstreamContextSection` wraps `upstream.title` in `<upstream_title>` XML tags with `escapeXmlContent` applied. `buildCrossRepoContractSection` applies `escapeXmlContent` to the contract. `buildRetryContext` auto-detects structured `RevisionFeedback` JSON; falls back to legacy `<failure_notes>`. `PLANNER_TOOLS_INSTRUCTIONS` advertises the in-process FLEET MCP tools. | `CODING_AGENT_PREAMBLE`, `SPEC_DRAFTING_PREAMBLE`, `PLANNER_TOOLS_INSTRUCTIONS`, `PLAYGROUND_INSTRUCTIONS`, `buildPersonalitySection`, `buildUpstreamContextSection`, `buildCrossRepoContractSection`, `buildBranchAppendix`, `buildRetryContext`, `buildScratchpadSection`, `truncateSpec`, `escapeXmlContent` |
| `prompt-pipeline.ts` | Pipeline agent prompt builder. Section ordering: upstream context → task spec → prior scratchpad → retry context. Prior scratchpad content (from `progress.md`) is wrapped in `<prior_scratchpad>…</prior_scratchpad>` boundary tags with `escapeXmlContent` applied before insertion — prevents a malicious `progress.md` from breaking XML containment. `MUST_COMMIT_BEFORE_EXIT` directive injected before `DEFINITION_OF_DONE` — instructs agents to run `git add -A && git commit` as their final action and verify with `git log -1`. `PUSH_FAILURE_GUIDANCE` injected for all task classes; `PIPELINE_JUDGMENT_RULES` (test failure rules) only for fix/refactor. Passes `revisionFeedback` from `BuildPromptInput` to `buildRetryContext` so re-spawned agents see human revision requests. | `buildPipelinePrompt`, `classifyTask`, `TaskClass` |
| `prompt-assistant.ts` | Assistant and adhoc agent prompt builders. `renderTaskSection` wraps truncated content in `escapeXmlContent()` before inserting it into `<user_task>` tags — prevents boundary-tag injection from user-supplied task strings. | `buildAssistantPrompt`, `buildAdhocPrompt` |
| _moved → `services/planner-mcp-server.ts`_ | T-19 (Phase 2) relocated this file out of `agent-manager/` because its sole responsibility is service orchestration (sprint + epic services), not pipeline lifecycle. See `docs/modules/services/index.md`. | — |
| `prompt-synthesizer.ts` | Synthesizer agent prompt builder (single-turn spec generation) | `buildSynthesizerPrompt` |
| `prompt-copilot.ts` | Copilot agent prompt builder. All user-supplied interpolation sites (`title`, `repo`, `spec`, `msg.content`) are now wrapped in `escapeXmlContent()` before insertion into XML boundary tags. | `buildCopilotPrompt` |
| `prompt-constants.ts` | Truncation limits for all prompt builders. Also exports `MAX_NO_COMMITS_RETRIES=3` — hard cap on the `no_commits` retry branch so agents that never commit are not requeued forever. | `PROMPT_TRUNCATION`, `MAX_NO_COMMITS_RETRIES` |
| `prompt-composer-reviewer.ts` | Reviewer-specific prompt builders for Code Review Station. `buildStructuredReviewPrompt` emits a single JSON object; `buildInteractiveReviewPrompt` builds multi-turn conversational prompt. All user-supplied content (`taskContent`, `diff`, `messages[].content`, `reviewSeed.openingMessage`) is wrapped with `escapeXmlContent` to prevent boundary-tag injection. `reviewSeed.openingMessage` is wrapped in `<opening_message>` XML tags. | `buildStructuredReviewPrompt`, `buildInteractiveReviewPrompt`, `buildReviewerPrompt` |
| `pr-operations.ts` | PR find/create operations via gh CLI — check existing, create new with retry/backoff, sanitize titles. `createNewPr` now calls `ensureGithubCredential()` before invoking `gh` so tasks fail fast with the canonical "Run: gh auth login" guidance (or the opted-out message) instead of burning through three retries against a missing CLI. Broadcasts `manager:warning` toast when PR creation fails after all retry attempts. **Epic 5 (DI):** `createNewPr` and `findOrCreatePR` accept an optional `broadcast?` trailing parameter; no longer import the framework adapter directly. | `generatePrBody`, `sanitizeForGit`, `checkExistingPr`, `createNewPr`, `findOrCreatePR` |
| `worktree.ts` | Worktree setup and cleanup orchestration for pipeline agents. `setupWorktree` acquires a per-repo lock, asserts main-repo cleanliness, fast-forwards local `main` to `origin/main` (skipped with a warning if the main repo is not on branch `main` — prevents ff-merging into an unrelated branch), creates the worktree and branch, symlinks `node_modules`, writes `node_modules` to the worktree's common gitdir `info/exclude` via `git rev-parse --git-common-dir` (prevents the symlink from being staged — `.gitignore`'s trailing slash only matches directories, not symlinks; using `--git-common-dir` instead of `.git/info/exclude` fixes ENOTDIR in worktrees where `.git` is a pointer file), and reserves disk space. `cleanupWorktree` removes the worktree, deletes the branch, and releases the reservation. `pruneStaleWorktrees` removes orphaned entries using **async directory enumeration** (`fs.promises.readdir`) — no blocking `readdirSync` calls on the main-thread event loop. `TASK_ID_HEX_PATTERN` (`/^[0-9a-f]{32}$/i`) is the guard constant — matches FLEET's 32-char hex task IDs (`lower(hex(randomblob(16)))`); directories whose names don't match are never pruned. **T-68:** Optional `logger` parameters now default to `createLogger('worktree')` (never to `console`); helper signatures narrowed from `Logger \| Console` to `Logger`. **T-33:** every cleanup-failure log line includes `worktreePath=<path>` so operators can diagnose orphaned trees without scraping surrounding context. **EP-4:** `SetupWorktreeOpts` accepts an optional `appendToNotes` callback; when `git fetch origin main` fails the failure message is appended to the task's notes field via this callback so operators see the context on retry. **T-5:** `enumeratePruneCandidates` and `enumerateRepoCandidates` are async functions returning `Promise<PruneCandidate[]>`. **Epic 5 (T-38):** `setupWorktree` decomposed into named helpers — `assertRepoExists`, `fetchLatestMainOutsideLock`, `fastForwardMainIfOnDefaultBranch`, `symlinkNodeModules`, `excludeNodeModulesFromGitTracking`, `createWorktreeUnderLock` — each owns one concern; `setupWorktree` reads as a 10-line sequence of named calls. **fork-on-approve:** `SetupWorktreeOpts` accepts an optional `baseBranch` field; when set, `createWorktreeUnderLock` passes it to `addWorktree` so the new branch starts from the specified commit rather than HEAD. | `setupWorktree`, `SetupWorktreeOpts`, `SetupWorktreeResult`, `cleanupWorktree`, `CleanupWorktreeOpts`, `pruneStaleWorktrees`, `branchNameForTask`, `branchNameForTaskId`, `branchNameForTaskGroup` |
| `preflight-check.ts` | Pre-spawn toolchain detection, binary probing, and npm registry credential check. `detectToolchain` inspects the repo root for known signal files (`turbo.json`, `pnpm-lock.yaml`, `gradlew`, `pom.xml`, `pyproject.toml`, `Cargo.toml`) and returns `ToolchainSignal` objects. `detectNpmrcMissingVars` reads `.npmrc` and returns `${VAR_NAME}` references not present in the supplied env (used to detect missing `NODE_AUTH_TOKEN` and similar for private registries). `runPreflightChecks` runs both checks. Returns `{ ok: true }` when all pass; `{ ok: false, missing: string[], missingEnvVars: string[] }` otherwise. Fail-open on any detection error. | `detectToolchain`, `detectNpmrcMissingVars`, `runPreflightChecks`, `ToolchainSignal`, `PreflightResult` |
| `preflight-gate.ts` | IPC promise bridge for pre-flight user confirmation. `createPreflightGate(emit)` accepts an injected emit callback (replacing direct `broadcast` import — Epic 3 T-30) and returns a `PreflightGate` that calls `emit('agent:preflightWarning', payload)` and awaits user response via a `Map<taskId, Promise>`. Auto-resolves `false` after 5-minute timeout when renderer is unavailable. | `createPreflightGate`, `PreflightGate` |
| `disk-space.ts` | Pre-spawn disk-space gating for worktree setup. `ensureFreeDiskSpace` throws `InsufficientDiskSpaceError` when free bytes are below the threshold and `DiskSpaceProbeError` when the underlying `statfs` call itself fails (ENOENT, EACCES, ENOSYS). **T-99:** probe failures used to be swallowed and treated as a pass — now they log at WARN with the underlying error and propagate so callers can refuse the spawn instead of silently over-committing on an unknown filesystem. `reserveDisk`/`releaseDisk` track in-flight reservations so concurrent spawns do not all race past the same "5 GB free" check. | `ensureFreeDiskSpace`, `InsufficientDiskSpaceError`, `DiskSpaceProbeError`, `reserveDisk`, `releaseDisk`, `getPendingReservation`, `MIN_FREE_DISK_BYTES`, `DISK_RESERVATION_BYTES` |
| `file-lock.ts` | Per-repo PID-based advisory lock used by `setupWorktree` to serialize the conflict-sensitive worktree-add/branch-create/ff-merge operations. `acquireLock` is **async** (`Promise<void>`) — all filesystem operations use `fs.promises.*` so the main-thread event loop is never blocked. Checks liveness via `process.kill(pid, 0)` and reclaims stale locks using a write-then-verify pattern: after `rename` claims the lock it re-reads the file and throws `LockContestedError` if another process won the race (closes the TOCTOU window). `releaseLock` is non-throwing — fires `rm().catch()` fire-and-forget; ENOENT and all other errors log WARN. Both functions accept an optional `logger`; when omitted they fall back to `createLogger('file-lock')` so warnings always reach `~/.fleet/fleet.log`. **T-77 (Phase C):** `acquireLock` async — stale-lock I/O no longer blocks the main thread. | `acquireLock`, `releaseLock`, `LockContestedError` |
| `worktree-lifecycle.ts` | Worktree add/remove/prune and branch delete operations. All `execFileAsync` calls carry `timeout: GIT_EXEC_TIMEOUT_MS` (30 s) so a stalled git subprocess cannot hang the main process indefinitely. **T-48:** `GIT_EXEC_TIMEOUT_MS` is now exported so other modules (`resolve-success-phases.ts`, `pre-review-advisors.ts`, `completion.ts`) can import from this single source instead of redefining the same constant. **fork-on-approve:** `addWorktree` accepts an optional `baseBranch` parameter — when supplied the new branch starts at that commit via `git worktree add -b <branch> <path> <baseBranch>`, allowing a downstream agent's worktree to be forked from an approved parent's branch instead of the repo's current HEAD. | `listWorktrees`, `removeWorktreeForce`, `pruneWorktrees`, `deleteBranch`, `forceDeleteBranchRef`, `addWorktree`, `cleanupWorktreeAndBranch`, `GIT_EXEC_TIMEOUT_MS` |
| `spawn-registry.ts` | Owns the four mutable spawn-tracking collections (`activeAgents`, `processingTasks`, `agentPromises`, `pendingSpawns`) behind a verb-shaped API. All consumers (`run-agent.ts`, `task-claimer.ts`, `watchdog-loop.ts`, `shutdown-coordinator.ts`) receive `SpawnRegistry` instead of raw Map/Set refs. Verb methods: `registerAgent`, `removeAgent`, `getAgent`, `hasActiveAgent`, `allAgents`, `activeAgentCount`, `asActiveAgentsMap`, `markProcessing`, `unmarkProcessing`, `isProcessing`, `trackPromise`, `forgetPromise`, `allPromises`, `incrementPendingSpawns`, `decrementPendingSpawns` (floors at 0), `pendingSpawnCount`. | `SpawnRegistry` |
| `terminal-guard.ts` | Idempotency wrapper for `onTaskTerminal` calls. Owns the `taskId → in-flight Promise<void>` map; `guardedCall(taskId, fn)` returns the in-flight promise when a call for the same task is already running, otherwise executes `fn` and stores the promise. Map entry deleted in `finally` so subsequent calls for the same task always proceed. | `TerminalGuard` |
| `lifecycle-controller.ts` | Owns the four periodic `setInterval` handles that drive the agent-manager loop family. Single responsibility: `startTimers(pollIntervalMs, callbacks, stagger?)` and `stopTimers()`. Extracted from `AgentManagerImpl` so timer management has its own SRP boundary. **T-83 (EP-2):** `startTimers` accepts an optional `TimerStaggerOptions` argument with per-timer `initialDelayMs` values. When set, a `setTimeout` defers the interval start so drain/watchdog/orphan/prune don't all fire at t=0 on startup. `stopTimers()` cancels pending stagger handles. `AgentManagerImpl.start()` passes staggered offsets: drain=0ms, watchdog=100ms, orphan=175ms, prune=250ms. | `LifecycleController`, `LoopCallbacks`, `TimerStaggerOptions` |
| `circuit-breaker.ts` | Spawn-phase circuit breaker. **Epic 7 (T-55):** `recordFailure` called while circuit is open does NOT extend `openUntil` — the pause window is fixed at trip time. After `SPAWN_CIRCUIT_FAILURE_THRESHOLD` (5) consecutive spawn-phase failures the drain loop pauses for `SPAWN_CIRCUIT_PAUSE_MS` (5min). `recordFailure(taskId, reason)` accumulates a `recentFailures` history; `recordSuccess()` resets both counter and history. **EP-5:** scope tightened to spawn phase only — stream/cleanup failures must not call `recordFailure`. The `circuit-breaker.open` structured event includes `triggeringTask`, `failureCount`, and the full `recentFailures` array for diagnostics. **T-98 (DI):** `broadcast` is replaced by an optional `CircuitObserver` interface injected via the constructor; the composition root wires a concrete `CircuitObserver` object literal that calls `broadcast`. `CircuitBreaker` no longer imports any framework adapter. **T-100:** `isOpen()` auto-reset log enriched to include `failureCount` and `openDurationMs`. | `CircuitBreaker`, `CircuitObserver`, `SPAWN_CIRCUIT_FAILURE_THRESHOLD`, `SPAWN_CIRCUIT_PAUSE_MS` |
| `error-registry.ts` | Groups circuit-breaker state, per-task drain-failure counts, and fast-fail sliding-window tracking behind a single named collaborator. **T-2 (EP-2):** `ErrorRegistry` wraps `CircuitBreaker` + `drainFailureCounts` map. `AgentManagerImpl` holds one instance and delegates all spawn-failure and drain-failure accounting through it. Methods: `isCircuitOpen()`, `circuitOpenUntil`, `recordSpawnSuccess/Failure(taskId?, reason?)`. **Verb API (g1):** `incrementFailure(taskId)`, `clearFailure(taskId)`, `failureCountFor(taskId)` replace the old `getDrainFailureCount/setDrainFailureCount/clearDrainFailureCount()` accessors; `drainFailureCounts` Map is now `private`. **EP-5 (T-57):** also holds `fastFailTracker: FastFailTracker` — timestamped per-task failure window. `FastFailTracker.record(taskId, reason, now?)` stores a failure entry; `recentCount(taskId)` evicts stale entries (older than `FAST_FAIL_THRESHOLD_MS`) and returns the live count; `isExhausted(taskId)` returns `true` at `MAX_FAST_FAILS` threshold. | `ErrorRegistry`, `FastFailTracker` |
| `agent-manager-test-internals.ts` | Stable typed view over the private members of `AgentManagerImpl` that tests reach into. Tests import via `mgr.__testInternals.<name>` rather than `mgr._<name>`; renames inside `AgentManagerImpl` only need to update the mapping in this file instead of 35+ test sites. Private fields are accessed via `(mgr as any)` inside the seam. **Spying caveat**: `vi.spyOn(manager, '_processQueuedTask')` still uses the underscore name and the assertion needs to use the captured spy variable (the view's delegating wrapper isn't the spy). Use the seam for state read/write and direct method invocation; capture spies as variables for assertion. **T-2/T-6 (EP-2):** exposes `wipTracker`, `errorRegistry`, and `started` via the test seam. **g1:** adds `terminalGuard: TerminalGuard` and `spawnRegistry: SpawnRegistry` to the seam. | `AgentManagerTestInternals` |
| `index.ts` | `AgentManagerImpl` class — drain loop, watchdog, shutdown, `_validateAndClaimTask` (fresh-status guard), `onTaskTerminal`. Timer handles delegated to `LifecycleController`. **T-58 (G2):** holds `_drainLoopInstance: DrainLoop` (constructed in constructor); `_drainLoop()` delegates to `_drainLoopInstance.runDrain()`. Watchdog `getConcurrency` delegates to `_drainLoopInstance`. **T-98 (G2):** `CircuitBreaker` receives a `CircuitObserver` object literal. **g1:** `_spawnAgent` decomposed into `incrementSpawnAccounting`, `recordCircuitBreakerFailure`, `releaseClaimAsLastResort` named helpers. Startup orphan recovery serialized through `_scheduleInitialDrain` (single site). `TerminalGuard` owns `onTaskTerminal` deduplication. `waitForAgentsToSettle` polls `spawnRegistry.activeAgentCount() === 0`. Mutable fields (`_running`, `_shuttingDown`, `_started`, etc.) are TypeScript `private` — accessible only through `__testInternals`. **T-10 (Phase A):** `AgentManager` interface adds `cancelAgent(taskId): Promise<void>`. **T-55 (Phase A):** `AgentManager` interface adds `awaitOAuthRefresh(): Promise<void>`. **T-8:** `_drainQueuedTasks` deleted — the DrainLoop's `drainQueuedTasksWithMap` is the canonical implementation. **Epic 3 (T-3/T-4/T-5/T-6/T-7):** `createAgentManager` factory accepts `CreateAgentManagerOptions` (replaces 8 positional args). Options include optional infrastructure callbacks (`emit`, `getSettings`, `resolveRepoPath`, `getRepoConfigs`, `resolveGhRepo`) — each falls back to direct module calls when omitted, enabling gradual DI migration. `AgentManager.onTaskTerminal` status param narrowed from `string` to `TaskStatus`. `_` prefixed fields made `private` (except `_wipTracker` which remains `readonly` for test seam access). `_watchdogDeps` built once at construction (not rebuilt per invocation). | `AgentManagerImpl`, `createAgentManager`, `CreateAgentManagerOptions`, `AgentManager`, `AgentManagerStatus` |
| `run-agent.ts` | Orchestrator — delegates to prompt-assembly, message-consumer, agent-telemetry, spawn-and-wire; re-exports public symbols from extracted modules for backward compat. **UX:** after `runSetupPhase` resolves a non-null prompt, calls `deps.repo.persistRenderedPrompt(task.id, prompt)` fire-and-forget so spec authors can inspect the exact prompt passed to the SDK. `cleanupWorktreeWithRetry` retries up to 4 times (100ms/500ms/2000ms backoff). **Epic 7 (T-46):** `cleanupOrPreserveWorktree` test coverage — preserves worktree at `review` status, cleans up at `done`, and defaults to preserve on null DB result (conservative). **Epic 7 (T-51):** `cleanupWorktreeWithRetry` test coverage — 4 total attempts (3 backoff + 1 final), surfaces error note to task via `repo.updateTask`. **T-17:** `RunAgentDataDeps` requires `unitOfWork: IUnitOfWork`. **PR-3:** `resolveNormalExit` calls `detectMissingSpecFiles` for re-queue on missing spec paths. `detectMissingSpecFiles` diff and pre-spawn worktree status check both carry 30 s timeouts via `GIT_EXEC_TIMEOUT_MS`. **dogfood-fix-1:** `detectMissingSpecFiles` now exempts new files — any spec-listed path absent from `origin/<defaultBranch>` (checked via `git cat-file -e`) is skipped from enforcement since the agent is free to name new files with any convention. **T-11:** `filterToExistingBaseFiles` now validates each path via `isRelativeSafePath` before calling `git cat-file` — rejects paths containing `..` (traversal) or starting with `/` (absolute); logs a warning and excludes the offending path. `filterToExistingBaseFiles` is now exported for direct testing. **Diag logging:** `filterToExistingBaseFiles` emits `warn` when a path is rejected or `git cat-file` fails. **EP-12:** `finalizeAgentRun` emits `logger.event('agent.completed', { taskId, status, durationMs, model, costUsd })`. **T-16/T-32 (EP-2):** `runAgent` is a pure 5-phase orchestrator. **EP-5 (T-55/57):** `onSpawnFailure` receives `(taskId, reason)`. **Phase A:** `abortPhaseUnexpectedly` routes through `TaskStateService`. **Epic 5 (T-12):** `finalizeAgentRun` accepts `FinalizeAgentRunOpts` (replaces 9 positional args). **Epic 5 (T-13):** `ResolveAgentExitContext` slimmed to `{ task, exitCode, lastAgentOutput, agent, exitedAt, worktree, repoPath, deps }` — `RunAgentDeps` passed directly instead of re-spread. **Epic 6 perf — T-14:** `resolveAgentExit` now returns `Promise<TaskStatus>` — each exit path returns the status it transitioned the task to. `finalizeAgentRun` uses the returned status directly for the `agent.completed` event log, eliminating the `repo.getTask` round-trip that previously followed `resolveAgentExit`. The normal-exit success path reads the DB once via `repo.getTask` to handle the case where the success pipeline internally caught a `PipelineAbortError` and transitioned to a non-review status. **Epic 7 (T-34):** removed duplicate debug log in `detectMissingSpecFiles` that was labelled `'missing files'` but logged `existingBaseFiles` — the accurate log line is retained. **Epic 7 (T-35):** `resolveNormalExit` now uses `isTaskStatus` guard on the `repo.getTask` result; null/undefined path logs a warn and returns `'error'` explicitly rather than silently short-circuiting via `??`. **Epic 7 (T-36):** inline truncation replaced with `truncateToNoteLimit` from `lib/async-utils.ts`; `NOTES_MAX_LENGTH` import removed. **Epic 7 (T-37):** `agentExitedNormally(result)` predicate extracted — replaces the `ffResult === 'normal-exit'` sentinel check and its explanatory comment at the call site. | `runAgent`, `cleanupWorktreeWithRetry`, `consumeMessages`, `validateTaskForRun`, `assembleRunContext`, `fetchUpstreamContext`, `filterToExistingBaseFiles`, `RunAgentDeps`, `RunAgentFastFailDeps`, `AgentRunClaim`, `ConsumeMessagesResult`, `FinalizeAgentRunOpts` |
| `revision-feedback-builder.ts` | Parses raw verification tool output (typecheck stderr, test runner output) into structured `RevisionFeedback` objects. `buildVerificationRevisionFeedback(kind, stderr)` handles `'compilation'` (modern + legacy tsc formats) and `'test_failure'` (FAIL suite headers, vitest ● blocks). `buildMissingFilesRevisionFeedback` and `buildNoCommitsRevisionFeedback` cover the other retry paths. `parseRevisionFeedback(notes)` safely deserialises `task.notes` — returns `null` for legacy freeform strings. `renderRevisionFeedbackBlock(feedback)` formats a `RevisionFeedback` as a `<revision_feedback>` XML block for injection into retry prompts; uses a local `escapeXmlContent` (equivalent to the canonical one in `prompt-sections.ts`) to escape both opening-tag and closing-tag sequences as well as `>` — kept local to avoid a circular import. | `buildVerificationRevisionFeedback`, `buildMissingFilesRevisionFeedback`, `buildNoCommitsRevisionFeedback`, `parseRevisionFeedback`, `renderRevisionFeedbackBlock` |
| `turn-tracker.ts` | Per-agent token/turn tracking — writes turn records via injected `TurnTrackerDeps.insertTurn(record)` callback. Default deps use `insertAgentRunTurn(getDb(), record)`; tests pass a spy directly, no db/module mocking required. **T-147:** `TurnTrackerDeps` accepts an optional `logger`; default falls back to `createLogger('turn-tracker')` so DB-write failures land in `~/.fleet/fleet.log`. **T-25 (perf):** `InsertTurnFn` now typed `(record) => Promise<void> \| void`; the write is fired as a non-blocking microtask via `void Promise.resolve().then(...)` so the event loop can breathe between consecutive assistant messages without losing any turns. | `TurnTracker`, `TurnTrackerDeps`, `InsertTurnFn` |
| `agent-event-mapper.ts` (`src/main/`) | Maps SDK wire-protocol messages to AgentEvents and batches them to SQLite. Broadcasts via `broadcastCoalesced` (agent:event:batch channel only — no separate unbatched emit). `flushAgentEventBatcher(db?)` accepts optional db for injection. `tool_result` output field capped at `TOOL_RESULT_OUTPUT_MAX_CHARS` (50 000 chars) before persisting. Recognizes two SDK tool-result formats: (a) the modern `user` message with a `tool_result` content block keyed by `tool_use_id` — the tool name is resolved from the preceding assistant `tool_use` block via a per-agent, bounded (`MAX_TRACKED_TOOL_USE_IDS_PER_AGENT = 1000`) id→name map (EP-15); and (b) legacy top-level `tool_result` messages. `mapRawMessage(raw, agentId?)` accepts an optional `agentId` so concurrent runs do not share tool-name state — callers omitting it fall back to a shared bucket. **EP-15 DLQ sentinel:** when the batch circuit breaker hits `MAX_CONSECUTIVE_FAILURES`, instead of silently dropping the batch the mapper logs a structured WARN (`event-batcher: permanent failure — dropping events {droppedCount, sampleAgentIds, reason}`), increments `getDroppedEventCount()`, and resets the failure counter so a later recovery is not blocked. **EP-15 per-run isolation:** `emitAgentEvent` clears that agent's tool-name map on `agent:started` so stale tool-use IDs from a prior run cannot mis-label the new run. **T-88:** each unrecognized `msgType` is logged at most once per process lifetime via a module-scoped sentinel set — prevents log flooding when the SDK introduces a new control frame. `__resetUnknownMessageTypeSentinel`, `__resetAllToolNameTracking`, and `__resetDroppedEventCount` are exported for test-only fixture reset. | `mapRawMessage`, `emitAgentEvent`, `flushAgentEventBatcher`, `resetAgentToolNames`, `getDroppedEventCount`, `__resetUnknownMessageTypeSentinel`, `__resetAllToolNameTracking`, `__resetDroppedEventCount` |
| `terminal-handler.ts` | Metrics recording, structured event log, and dependency resolution on task terminal events. **Epic 7 (T-45):** `__tests__/terminal-handler.test.ts` now covers `resolveDependents` invocation for hard-dependency success, hard-dependency failure, and soft-dependency (queued) completion — verifying task ID, status, and repo callbacks are forwarded correctly. **T-17 (Phase 1b):** Transactional atomicity comes from an injected `IUnitOfWork`; `wrapTransactionWithLogging` adds the failure-logging wrapper. **EP-12:** emits `logger.event('agent.terminal', { taskId, status, source: 'terminal-handler' })`. **EP-1:** `createAgentTerminalDispatcher(onTaskTerminal, deps)` wraps `handleTaskTerminal` as a `TerminalDispatcher` for injection into `TaskStateService`. **T-54:** `TerminalHandlerDeps` gains optional `terminalResolution?: TerminalResolutionStrategy`; when provided, its `onStatusTerminal` is called instead of the default `resolveTerminalDependents` path (replacing the old `config.onStatusTerminal` check). **Epic 3 (T-36):** `TerminalHandlerDeps.getSetting` removed; replaced with `cascadeCancellation?: { enabled: boolean }`. Callers resolve the cascade-cancellation setting once and pass the typed policy; `buildGetSettingFromPolicy` translates it internally for `resolveDependents`. **Epic 5 (T-37):** dep-resolution failure note update simplified from nested `try/catch` + `Promise.resolve().catch` to a single `void repo.updateTask(...).catch(...)` fire-and-forget. **Epic 7 (T-36):** inline note truncation replaced with `truncateToNoteLimit` from `lib/async-utils.ts`; `NOTES_MAX_LENGTH` import removed. | `handleTaskTerminal`, `TerminalHandlerDeps`, `createAgentTerminalDispatcher` |
| `orphan-recovery.ts` | Detects tasks stuck in `active` status without a live agent. Re-queues tasks under `MAX_ORPHAN_RECOVERY_COUNT` (3) with the counter incremented; transitions tasks at the cap to `error` with `failure_reason = 'exhausted: orphan recovery cap reached'` via `TaskStateService.transition` (Phase A invariant `orphan-recovery-via-state-service`). Returns `OrphanRecoveryResult { recovered: string[], exhausted: string[] }`. **T-53:** removed module-level `prClearingAlreadyLogged` flag — the log now fires on every task with a PR/branch_only status (called once per drain tick so not noisy in practice). **EP-3**. **dogfood-fix-3:** requeue sets `next_eligible_at` to 30s from now (`ORPHAN_REQUEUE_GRACE_MS`) instead of null — gives the original agent's finally-block time to finish worktree cleanup before the drain loop claims the task again. | `recoverOrphans`, `MAX_ORPHAN_RECOVERY_COUNT`, `ORPHAN_REQUEUE_GRACE_MS`, `OrphanRecoveryResult` |
| `commit-message.ts` | Pure function that builds a FLEET-convention commit message from a `SprintTask`. Format: `${type}(${scope}): ${subject}\n\nTask-Id: ${task.id}`. Type is mapped from `spec_type`; scope is the basename (no extension, lowercase) of the first path in `## Files to Change`, falling back to `agent`; subject is the task title with any leading `T-N [PN]` / `PR-N [PN]` prefix stripped. | `buildCommitMessage` |
| `completion.ts` | **Barrel re-export** (T-50) — public API surface for agent task completion. All implementation moved to focused modules: `success-pipeline.ts`, `pre-review-advisors.ts`, `verification-gate.ts`. Retains thin wrappers for `findOrCreatePR`, `deleteAgentBranchBeforeRetry`, and `resolveFailure`. **T-45:** re-exports `DEFAULT_PRE_REVIEW_ADVISORS` (replaced `preReviewAdvisors`). **T-48:** `GIT_EXEC_TIMEOUT_MS` imported from `worktree-lifecycle.ts`. All existing callers continue to import from this file without changes. | `resolveSuccess`, `resolveFailure`, `findOrCreatePR`, `deleteAgentBranchBeforeRetry`, `DEFAULT_PRE_REVIEW_ADVISORS`, `runPreReviewAdvisors`, `ResolveSuccessContext`, `ResolveFailureContext`, `ResolveFailureResult` |
| `success-pipeline.ts` | Success pipeline for agent task completion (T-50). **Epic 7 (T-52):** unexpected subprocess throw from `failTaskIfNoCommitsAheadOfMain` (not a structured `{committed: false}`) propagates out of `verifyCommitsPhase` as a non-`PipelineAbortError` — `resolveSuccess` re-throws it, and `onTaskTerminal` is NOT called inside `resolveSuccess` (caller `handleResolveSuccessFailure` in `run-agent.ts` owns that notification). Defines `ResolveSuccessContext`, `SuccessPhaseContext`, `SuccessPhase`, all 10 named phase objects, the `successPhases` array, and `resolveSuccess`. The `noOpGuard` phase calls `detectNoOpAndFailIfSo` (private to this module). Re-exports `PipelineAbortError` from `pipeline-abort-error.ts` — not defined locally — so callers importing from either file get the same class and `instanceof` checks in `run-agent.ts` work correctly. **Epic 5 (T-2):** `buildAutoMergeStrategy(ctx)` extracted from `reviewTransitionPhase.run` — returns either a real `evaluateAutoMerge` closure or a no-op depending on whether both optional deps are present. **Epic 7 (T-40):** `AutoMergeStrategy` named type alias added — `(opts: Omit<AutoMergeContext, 'getAutoReviewRules' | 'resolveRepoLocalPath'>) => Promise<void>` — replaces the anonymous inline `Omit<...>` in `buildAutoMergeStrategy`'s return type. | `resolveSuccess`, `successPhases`, `PipelineAbortError`, `ResolveSuccessContext`, `SuccessPhaseContext`, `SuccessPhase` |
| `pre-review-advisors.ts` | Pluggable advisory checks run before the review transition (T-50). `untouchedTestsAdvisor` flags source files changed without a matching test update. `unverifiedFactsAdvisor` scans the diff for invented package names, URLs, and pipe-to-shell patterns; uses `timeout: GIT_EXEC_TIMEOUT_MS` on the `git diff HEAD~1 HEAD` call and logs a specific first-commit message (not a generic skip) when the error contains `'unknown revision'` or `'ambiguous argument'` — distinguishes single-commit branches from genuine git failures. `runPreReviewAdvisors` accepts an explicit `advisors` list (defaults to `DEFAULT_PRE_REVIEW_ADVISORS`) so tests can inject stubs without mutating global state. **T-45:** replaced mutable exported `preReviewAdvisors` array with `DEFAULT_PRE_REVIEW_ADVISORS` (frozen) + injected `advisors` parameter. | `PreReviewAdvisor`, `PreReviewAdvisorContext`, `DEFAULT_PRE_REVIEW_ADVISORS`, `runPreReviewAdvisors` |
| `verification-gate.ts` | Pre-review guard functions (T-50). `appendAdvisoryNote` appends a warning to a task's notes field (fire-and-forget). `verifyBranchTipOrFail` validates the branch tip references this task — routes tip-mismatch to `failed`; emits `logger.warn('[verification-gate] branch-tip check skipped — repoPath absent')` when `repoPath` is undefined so silent skips are visible in the log. `verifyWorktreeOrFail` runs typecheck + tests in the worktree; persists structured `VerificationResults` to `task.verification_results` (fire-and-forget) before success/failure branching; requeues the task with structured revision feedback on failure. **F10:** `capOutput(text, cap)` and `toVerificationRecord(result)` are exported pure helpers for capping and converting gate output; `buildVerificationResults(output)` is private. **T-52:** removed unused `_onTaskTerminal` parameter from `verifyBranchTipOrFail`. | `appendAdvisoryNote`, `verifyBranchTipOrFail`, `verifyWorktreeOrFail`, `capOutput`, `toVerificationRecord` |
| `unverified-facts-scanner.ts` | Pure heuristic scan of a `git diff HEAD~1 HEAD` output for concrete-looking values agents may have invented. Flags: brew tap installs (slash in name), `npm install -g` packages absent from `package.json`, URLs with unapproved hostnames, and pipe-to-shell patterns. Returns `string[]` of warning messages; never throws; no I/O. Advisory only — `completion.ts` appends each warning to `task.notes`. | `scanForUnverifiedFacts` |
| `resolve-success-phases.ts` | Success-path phase functions: worktree verification, branch detection, auto-commit, rebase, commit check, review transition. `autoCommitPendingChanges` takes a `SprintTask` and calls `buildCommitMessage` for a FLEET-convention message. When `failTaskIfNoCommitsAheadOfMain` detects zero commits it calls `logUncommittedWorktreeState` (runs `git diff HEAD` + `git status --porcelain` at WARN level under the task id) before requeuing; notes use `NO_COMMITS_NOTE` from `failure-messages.ts`. Caps the `no_commits` retry loop at `MAX_NO_COMMITS_RETRIES` — once the cap is reached the task short-circuits to `status='failed'` with `failure_reason='no-commits-exhausted'`. **T-16:** renamed `hasCommitsAheadOfMain` → `failTaskIfNoCommitsAheadOfMain` (CQS fix — command name, returns `{ committed: boolean }` result type). **T-48:** `GIT_EXEC_TIMEOUT_MS` imported from `worktree-lifecycle.ts` (deduplicated). **EP-1, EP-5, T-87:** structured logging, injected state service, no direct adapter imports. **Epic-1/T-23:** `failTaskWithError` signature trimmed to `(taskId, message, notes, logger, taskStateService, broadcastCoalesced?)` — the formerly `_`-prefixed `repo` and `onTaskTerminal` parameters were never used and have been removed; `verifyWorktreeExists` and `detectAgentBranch` updated accordingly. **Epic 5 (T-24):** `BranchTipMismatchError`, `extractTaskIdFromBranch`, `branchMatchesTask`, `assertBranchTipMatches`, `ReadTipCommit` extracted to `branch-tip-verification.ts` and re-exported here for backward compatibility. **Epic 5 (T-25):** `parseInt(diffOut.trim(), 10) === 0` replaced by `hasNoCommitsAheadOfMain(diffOut)` from `resolve-git.ts`. **Epic 1:** `transitionTaskToReview` accepts a single `ReviewTransitionContext` object instead of 13 positional parameters. | `verifyWorktreeExists`, `detectAgentBranch`, `autoCommitPendingChanges`, `performRebaseOntoMain`, `failTaskIfNoCommitsAheadOfMain`, `transitionTaskToReview`, `ReviewTransitionContext`, `failTaskWithError`, `extractTaskIdFromBranch`, `branchMatchesTask`, `assertBranchTipMatches`, `BranchTipMismatchError`, `RebaseOutcome`, `ReadTipCommit` |
| `branch-tip-verification.ts` | Validates that the agent's branch tip commit legitimately belongs to the task being completed. Extracted from `resolve-success-phases.ts` (Epic 5 T-24). `assertBranchTipMatches` uses the branch name as the primary signal (short-circuits before spawning a subprocess), falling back to commit-message matching via `buildExpectedTipTokens`. `branchMatchesTask` checks both UUID prefix via the 8-char hex suffix and legacy `t-N` slug tail. Throws `BranchTipMismatchError` only when neither signal matches. | `BranchTipMismatchError`, `extractTaskIdFromBranch`, `branchMatchesTask`, `assertBranchTipMatches`, `ReadTipCommit` |
| `resolve-failure-phases.ts` | Failure-path phase: requeue with exponential backoff or mark task permanently failed. Tail-truncates notes to `NOTES_TAIL_CHARS=1500` before DB write so root causes (at end of stack traces) are preserved. **T-92 (write-failure guard):** `resolveFailure` returns `ResolveFailureResult` (a discriminated union) — never throws. Callers must check `writeFailed` and skip `onTaskTerminal` when true to prevent unblocking dependents against a task still `active` in SQLite. **EP-1:** `resolveFailure` is now `async` and routes all status writes through `TaskStateService.transition()`. Both the requeue (`queued`) and terminal (`failed`) writes use callers `'resolve-failure:requeue'` and `'resolve-failure:terminal'`. All callers must `await` the result. **Orphan-race fix:** catches `InvalidTransitionError(queued→queued)` — the case where the orphan-recovery loop already requeued the task before the failure handler ran — and falls back to `repo.updateTask` to write retry fields in-place without a status change, returning `{ isTerminal: false }` instead of `writeFailed:true`. This restores normal `onTaskTerminal` flow so dependency resolution and worktree cleanup are not silently skipped. | `resolveFailure`, `calculateRetryBackoff`, `ResolveFailureContext`, `ResolveFailureResult` |
| `auto-merge-coordinator.ts` | Evaluates and executes automatic merges after agent completion. Best-effort — failures leave task in `review` for human action. **T-17 (Phase 1b):** Post-merge atomicity now comes from `AutoMergeContext.unitOfWork` (an injected `IUnitOfWork`) rather than a direct `getDb` import. **T-83:** `finalizeAutoMergeStatus` catch block emits `logger.event('auto-merge.status-update-failed', { taskId, error })` on DB failure after a successful merge. **Epic 3 (T-40):** `finalizeAutoMergeStatus` fallback raw `repo.updateTask` bypass removed — on transition failure the function now logs and re-throws, letting `evaluateAutoMerge`'s outer catch leave the task in `review` for human action. `unitOfWork` and `_unitOfWork` params removed from `finalizeAutoMergeStatus` since they were only used by the removed fallback. | `evaluateAutoMerge`, `AutoMergeContext` |
| `auto-merge-policy.ts` | Pure rule evaluation for auto-merge. Computes the CSS-only flag (every changed path is `.css`/`.scss`) so downstream callers can skip the full pre-push test suite. Returns `AutoMergeDecision` (merge with matched rule name) or `AutoMergeSkip`. | `evaluateAutoMergePolicy`, `isCssOnlyChange`, `AutoMergePolicyResult`, `AutoMergeDecision`, `AutoMergeSkip` |
| `diff-snapshot.ts` | Captures a full diff snapshot of a completed agent worktree for persistence to `sprint_tasks.review_diff_snapshot`, enabling Code Review UI to display changes after the worktree is cleaned up. **T-24 (perf):** replaced N+1 per-file `git diff -- <path>` subprocess loop with a single combined `git diff <base>...HEAD` call; `splitCombinedDiffByFile` splits the output on `diff --git` headers and distributes patches to file entries by path — reduces subprocess count from N+2 to 3 regardless of file count. Budget accounting (skipping patches that exceed `MAX_SNAPSHOT_CHARS`) is preserved. **Epic 6 perf — T-56:** `fetchAndDistributePatches` no longer mutates the input `files` array — it builds and returns a new `patchedFiles` array, keeping the original inputs unchanged. **UX:** `captureDiffSnapshot` now captures `HEAD` SHA via `git rev-parse HEAD` and stores it as `branchTip` in the snapshot — enables the Code Review "Since last review" incremental diff feature. | `captureDiffSnapshot` |
| `partial-diff-capture.ts` | Captures partial diffs from failed/cancelled agents for diagnostic notes | `capturePartialDiff` |
| `review-transition.ts` | Transitions a completed task to `review` status, preserving worktree path and branch. Before the state transition, calls `reviewRepo.invalidate(taskId)` (wrapped in try/catch) to purge any stale cached review result from the DB — ensures a revised task always receives fresh AI analysis rather than showing the prior revision's score. Stamps `promoted_to_review_at` so the Code Review nav badge can watermark tasks that entered review since the user last looked. Resets `fast_fail_count` to 0 so a task that fast-failed on earlier attempts does not carry a stale counter. **Epic 3 (T-31):** `TransitionToReviewOpts` gains `onMutation: (event, task) => void` — replaces direct `notifySprintMutation` import; the post-transition notification is now injected from the caller so the module no longer depends on the sprint-mutation-broadcaster. **EP-7:** emits `logger.event('completion.review_transition', …)`. All `execFileAsync('git', …)` calls carry `timeout: GIT_EXEC_TIMEOUT_MS` (30s). | `transitionToReview`, `TransitionToReviewOpts` |
| `task-mapper.ts` | Maps raw sprint task rows to `MappedTask` shape (used as `AgentRunClaim`) and evaluates hard-dependency blocking. Parses `revision_feedback` JSON from DB rows. `checkAndBlockDeps` routes `rawDeps` through the shared `sanitizeDependsOn` helper, which tolerates string-or-array JSONB shapes and silently discards malformed entries — an unparseable string or fully-invalid array is treated as "no deps" rather than marking the task `error`. | `mapQueuedTask`, `checkAndBlockDeps`, `MappedTask` |
| `dependency-refresher.ts` | Rebuilds the in-memory dependency index from SQLite; debounced on task mutations. **EP-9:** accepts an optional `dirtyTaskIds?: Set<string>` hint — when provided, tasks outside the set whose fingerprint is already cached skip the per-task fingerprint recompute. Deletion sweep and terminal eviction always run over the full set so a caller-supplied dirty hint can never leak stale fingerprints. **T-49:** `refreshDependencyIndex` now accepts the narrower `DependencyTaskReader` interface (only requires `getTasksWithDependencies`) instead of the full `IAgentTaskRepository`, eliminating the double-cast in `task-terminal-service.ts`. **T-26 (perf):** When `dirtyTaskIds` is defined and empty AND the global fingerprint hash matches the post-last-scan hash (tracked in a module-level WeakMap keyed on the fingerprints instance), the DB read is skipped entirely — the common path on quiet drain ticks with no task mutations. **Epic 6 perf — T-41:** `computeGlobalFingerprintHash` called at most once per invocation — the post-scan hash is computed once, stored in `postUpdateHash`, and passed to `lastGlobalHash.set` in one step, eliminating the second call that previously existed at the bottom of the dirty path. **Epic 7 (T-48):** global hash cache replaced with a `GlobalHashEntry { hash, valid }` structure stored in `globalHashCache: WeakMap<DepsFingerprint, GlobalHashEntry>`. `invalidateGlobalFingerprintHash` marks the entry invalid on every mutation (add/remove/update); `readGlobalFingerprintHash` recomputes and re-validates only when the cache is invalid. On quiet drain ticks with an empty dirty set the check is now `entry?.valid` (O(1)) instead of re-sorting the full fingerprint map (O(n log n)). | `refreshDependencyIndex`, `computeDepsFingerprint`, `DependencyTaskReader` |
| `types.ts` | Shared type definitions for agent manager internals. `ActiveAgent` now includes `worktreePath` and `branch` fields populated at spawn time. `DRAIN_PAUSE_ON_ENV_ERROR_MS` (30s) configures how long the drain pauses after an environmental failure. **EP-6 (T-73):** `SpawnStrategy` discriminated union added — `{ type: 'sdk' }` or `{ type: 'cli'; claudePath: string }`. **T-54:** `TerminalResolutionStrategy` interface extracted from `AgentManagerConfig` — behavior hook for external terminal-status routing. `AgentManagerConfig.onStatusTerminal` removed; callers pass a `TerminalResolutionStrategy` to `createAgentManager` instead. **Epic 7 (T-62):** `AgentHandle.messages` typed as `AsyncIterable<SDKWireMessage>` (was `AsyncIterable<unknown>`) — the declared shape matches the runtime protocol; consumers that need to narrow further use `asSDKMessage()` in `sdk-message-protocol.ts`. Spawn adapters (`spawn-cli.ts`, `spawn-sdk.ts`, `spawn-opencode.ts`) cast their generators to this type since the actual messages are validated at consumption time. | `ActiveAgent`, `AgentHandle`, `AgentManagerConfig`, `TerminalResolutionStrategy`, `ResolveDependentsParams`, `SpawnStrategy`, `DRAIN_PAUSE_ON_ENV_ERROR_MS` |
| `drain-loop.ts` | Polling orchestration — precondition checks, dep-index refresh, queued-task fetching and processing. **Epic 7 (T-47):** `quarantineStatusFor` behavior covered via public `applyQuarantine` path — `queued`→`cancelled`, `active`/`blocked`/`review`→`error`. **Epic 7 (T-53):** env-failure `pausedUntil` timestamp is within ±500ms of `Date.now() + DRAIN_PAUSE_ON_ENV_ERROR_MS`. **T-58:** `DrainLoop` class owns all mutable drain state (`drainFailureCounts`, `drainPausedUntil`, `lastTaskDeps`, `_recentlyProcessedTaskIds`, `_concurrency`). Constructor accepts the slimmed `DrainLoopDeps` (read-only collaborators only — no mutable setters). `runDrain()` is the main entry. `getConcurrency()`/`setConcurrency()` are public accessors used by the watchdog. `recentlyProcessedTaskIds` getter exposes the dirty-set hint for task-claimer. `initializeFingerprintsFrom(tasks)` seeds fingerprint cache at startup. Environmental failures are classified via `classifyFailureReason`. Spec-level failures quarantine after `DRAIN_QUARANTINE_THRESHOLD` (3) throws. **EP-12:** `runDrain` generates a tick correlation ID via `generateTickId()` (private file-scope helper that returns an 8-char hex string); emits `logger.event('drain.tick.idle', { tickId, queuedCount: 0 })` when no tasks are available. **T-31:** `generateTickId()` extracted from the inline `Math.random().toString(16).slice(2, 10)` expression. **T-1 (EP-2):** `drainQueuedTasksWithMap` wraps `repo.getQueuedTasks()` in `Promise.race` with `DRAIN_TICK_TIMEOUT_MS` (10s). On timeout, emits `logger.warn('[drain-loop] tick <tickId> timed out after <ms>ms — DB may be under pressure')` at WARN level alongside the existing `logger.event('drain.tick.timeout', ...)`. **T-55 (Phase A):** `DrainLoopDeps` adds optional `awaitOAuthRefresh?(): Promise<void>`. **(Epic 7 perf)** `buildTaskStatusMap` passes `new Set(_recentlyProcessedTaskIds)` as a hint to `repo.getTasksWithDependencies` when the dep index is dirty and processed IDs are available — scoping the DB query to changed tasks. Full scan used when the hint set is empty (startup or force-rebuild). **T-54:** `taskStatusMap` typed as `Map<string, TaskStatus>` (was `Map<string, string>`). **T-29:** `applyQuarantine` is now `async` and awaits the `taskStateService.transition()` call so the drain loop does not re-process a task before its quarantine transition commits. **T-18 (Epic 4):** both `Map<string, TaskStatus>` cast sites replaced with `filterToValidTaskStatuses` — a private helper that drops entries with unrecognised status values and logs a warning for each one, so bad DB rows never propagate into the dependency index as trusted statuses. **Epic 6 perf — T-20:** `DrainLoopDeps.activeAgents: ReadonlyMap` replaced with `activeAgentCount: () => number` — decouples the drain loop from the registry's internal representation. **Epic 6 perf — T-21:** `runDrain` start, tick skip, and "Fetching queued tasks" logs downgraded from `logger.info` to `logger.debug`; found-queued-tasks log retained at INFO only when `queued.length > 0`; no-slot stop log downgraded to DEBUG. **Epic 6 perf — T-19:** deprecated pure-function wrappers (`validateDrainPreconditions`, `buildTaskStatusMap`, `drainQueuedTasks`, `runDrain`) deleted; callers use `DrainLoop` directly. **Epic 7 (T-30):** `buildTaskStatusMap` split into `rebuildTaskStatusMap()` (full rebuild, filters to non-terminal tasks via `TERMINAL_STATUSES`) and `refreshTaskStatusMapIncrementally()` (incremental refresh); `buildTaskStatusMap` remains as the public selector that delegates to one of them. **Epic 7 (T-31):** full rebuild filters `getTasksWithDependencies` results to exclude terminal statuses (`done`, `cancelled`, `failed`, `error`) before rebuilding the dep index — historical tasks no longer participate in the rebuild. **Epic 7 (T-36):** inline truncation in `formatQuarantineNote` replaced with `truncateToNoteLimit` from `lib/async-utils.ts`. | `DrainLoop`, `DrainLoopDeps`, `DRAIN_QUARANTINE_THRESHOLD`, `DRAIN_TICK_TIMEOUT_MS`, `DrainTimeoutError` |
| `watchdog-loop.ts` | Agent health checks — idle/timeout/rate-limit/cost verdicts, kill helpers. **Epic 7 (T-48):** DB-write failure (via `taskStateService.transition` throw) blocks `onTaskTerminal` and triggers `broadcastToRenderer` warning — prevents unblocking dependents against an un-persisted state change. **Epic 7 (T-54):** rate-limit-loop requeue routes through `taskStateService.transition` with `'queued'` (not `'error'`). **T-32:** `softKillAgent` issues `handle.abort()`; `forceKillAgent` escalates via `handle.forceKill()` / subprocess SIGKILL / abort fallback with explicit "forceKill applied" log; `killAgentWithEscalation` wires both with `FORCE_KILL_DELAY_MS` (5s) grace window. `killAgentImmediately` (renamed from `abortAgent`) sends soft-abort + SIGKILL unconditionally without the escalation timer. `runWatchdog` (async): escalation kill → flush → DB write (via `TaskStateService.transition` when wired, raw `repo.updateTask` fallback) → remove from registry → worktree cleanup (after DB write succeeds) → flush → notify terminal. **T-1 (P0 orphan window fix):** worktree cleanup moved to AFTER the DB write succeeds. **T-2 (P1):** `WatchdogLoopDeps` adds optional `taskStateService?: TaskStateService`; when provided the DB write routes through `TaskStateService.transition()`. **T-5 (P2):** `cleanupWorktreeIfNotInReview` receives `targetStatus` directly instead of doing a `repo.getTask` read. **EP-12:** kill path emits `logger.event('agent.watchdog.kill', ...)`. **EP-5 (T-29):** Idempotency guard at the top of the kill loop. **T-35 (write-failure guard):** DB write wrapped in `withRetryAsync`; on persistent failure `deps.broadcastToRenderer?.('manager:warning', ...)` called and function returns early without calling `onTaskTerminal`. **await fix:** `onTaskTerminal` is now `await`ed inside a `try/catch` so dependency resolution completes before the watchdog moves on. **T-16 (Epic 4):** both `result.taskUpdate.status` cast sites replaced with `isTaskStatus` guard — invalid status strings log a warning and skip the transition instead of being blindly passed to `taskStateService.transition`. **Epic 6 perf — T-17:** `killActiveAgent` accepts only `SpawnRegistry` (the `Map<string, ActiveAgent>` branch and `removeAgentFromMap` removed). **Epic 7 (T-44):** `narrowToTaskStatus(raw: unknown): TaskStatus | undefined` helper extracted — replaces the two duplicated `typeof rawStatus === 'string' && isTaskStatus(rawStatus) ? rawStatus : undefined` patterns in the watchdog kill path. | `runWatchdog`, `killActiveAgent`, `killAgentImmediately`, `softKillAgent`, `forceKillAgent`, `killAgentWithEscalation`, `FORCE_KILL_DELAY_MS`, `removeAgentFromRegistry`, `WatchdogLoopDeps` |
| `already-done-check.ts` | Pre-claim guard helper for the drain loop. `taskHasMatchingCommitOnMain(task, repoPath, logger)` scans the last 200 commits on `origin/main` and returns the first SHA whose subject or body references the task via any of three criteria (OR'd): `(T-<taskId>)` token in subject or body, subject equal to `task.title`, or `agent-run-id: <runId>` trailer in subject or body. Returns `{ sha, matchedOn }` or `null`. The `git log` invocation passes `maxBuffer: 16 MiB` and `timeout: GIT_FETCH_TIMEOUT_MS` (30 s) so a stalled subprocess cannot block the drain tick. Parsed commits are memoized per `repoPath` for `ALREADY_DONE_CACHE_TTL_MS` (5 s). Errors from `git log` are swallowed (empty list, not cached). `__resetAlreadyDoneCache` is test-only. | `taskHasMatchingCommitOnMain`, `AlreadyDoneTask`, `AlreadyDoneMatch`, `ALREADY_DONE_CACHE_TTL_MS`, `__resetAlreadyDoneCache` |
| `task-claimer.ts` | Task claim pipeline — dep blocking, repo path resolution, pre-claim already-done check (via `already-done-check.ts`), atomic CAS claim, worktree setup, agent spawn. **Epic 7 (T-49):** `skipIfAlreadyOnMain` test coverage — auto-completes to `done` and fires `onTaskTerminal` on commit match; does NOT fire `onTaskTerminal` when the transition itself fails. **Epic 7 (T-50):** double-failure in `prepareWorktreeForTask` (worktree setup throws + `transition` throws) falls back to `repo.updateTask({ claimed_by: null })` release — no stale claim. The pre-claim `getTask` status read was removed (T-13); status-change and concurrent-claim detection is now handled entirely by `claimTask`'s CAS transaction, which returns `null` for any non-queued status or WIP-limit violation. When `taskHasMatchingCommitOnMain` returns a match, the task is auto-completed (`status='done'`) via `taskStateService.transition(..., caller: 'task-claimer:auto-complete')` rather than being claimed — prevents re-execution of work that has already landed. After a successful auto-complete transition, `deps.onTaskTerminal(task.id, 'done')` is called so `DrainLoop.notifyRecentlyProcessedTask` fires and the dep index is marked dirty, unblocking downstream `blocked` tasks. Passes `config.maxConcurrent` to `claimTask` for atomic DB-level WIP enforcement. `spawnAgent` callback typed `Promise<void>`. **EP-9:** post-claim status-map refresh now uses `repo.getTask(claimedId)` (single PK lookup). **T-95 (write-failure guard):** `skipIfAlreadyOnMain` returns `false` on transition failure without calling `onTaskTerminal`. **EP-1:** `TaskClaimerDeps` requires `taskStateService: TaskStateService`. **T-96/T-97:** structured log events on auto-complete and spawn. **dogfood-fix-4:** `processQueuedTask` guards against concurrent agents — if `spawnRegistry.hasActiveAgent(taskId)` is true at entry, the spawn is skipped with a WARN log to prevent two agents racing on the same worktree path. **Epic 3 (T-40):** no-repo-path error path routes through `taskStateService.transition(taskId, 'error', { caller: 'task-claimer:repo-not-configured' })` instead of raw `repo.updateTask` — ensures dependency resolution, audit trail, and renderer broadcast fire on this path. **#708:** pre-flight "move to backlog" path (`proceed = false`) now routes through `taskStateService.transition(taskId, 'backlog', { caller: 'preflight:move-to-backlog' })` instead of `repo.updateTask` — the direct `repo.updateTask` path was bypassing the state machine, causing the invalid `queued→backlog` write to throw, which the drain loop quarantined as `cancelled` after 3 retries. **fork-on-approve:** `prepareWorktreeForTask` accepts an optional `rawTask: SprintTask` parameter and calls `resolveBaseBranch` before `setupWorktree`. `resolveBaseBranch` walks the task's hard dependencies, finds the first with `status='approved'` and a live worktree, reads its current branch via `git branch --show-current`, and returns it as `baseBranch`. When a parent qualifies, `stacked_on_task_id` is written to the task (fire-and-forget) and the worktree is forked from the parent's branch instead of HEAD. | `validateAndClaimTask`, `prepareWorktreeForTask`, `processQueuedTask`, `resolveRepoPath` |
| `worktree-manager.ts` | Worktree prune pass and review-status check helper | `runPruneLoop`, `checkIsReviewTask`, `WorktreeManagerDeps` |
| `shutdown-coordinator.ts` | Graceful shutdown — waits for drain, aborts agents, re-queues active tasks, flushes event batcher. **T-34 (EP-2):** re-queue step now checks task status before writing; tasks already in `review` status are skipped (their agent work is complete and awaiting human review — a shutdown must not discard that). | `executeShutdown`, `ShutdownCoordinatorDeps` |
| `config-manager.ts` | Hot-reload settings — updates maxConcurrent (valid range 1–16, matching Settings UI max), maxRuntimeMs, maxTurns in place; flags worktreeBase as restart-required. `AgentManagerImpl.reloadConfig()` triggers an immediate drain tick when maxConcurrent is raised so new slots are filled without waiting for the next 30-second poll. | `reloadConfiguration`, `ConfigManagerDeps` |
| `wip-tracker.ts` | Thin facade over ConcurrencyState for readable slot queries, plus `WipTracker` class. **T-2 (EP-2):** `WipTracker` class wraps the active-agent count behind named operations: `count` (live read via callback), `isFull(max)`. Used by `AgentManagerImpl` as `_wipTracker`. | `createConcurrencyState`, `getAvailableSlots`, `updateMaxSlots`, `WipTracker` |
| `failure-classifier.ts` | Maps agent error notes to a `FailureReason` via a frozen keyword-based pattern set. Patterns cover environmental, auth, no_commits, timeout, test_failure, compilation, spawn, incomplete_files, and unknown — searched in that priority order. `classifyFailureReason` accepts optional `logger`, `taskId`, and `additionalPatterns` for structured debug logging and test-safe extension without mutating global state. **EP-5 (T-58):** debug log on match. **T-47:** removed mutable `registerFailurePattern`/`resetRegistryToBuiltins`; use `additionalPatterns` parameter instead. **Epic 5 (T-28):** `auth` pattern expanded to include bare `'authentication'` so `message-consumer.ts` can delegate to this classifier and cover the same set of errors the former inline checks covered. **Epic 7 (T-39):** all `as FailureReason` casts removed from `BUILTIN_FAILURE_PATTERNS` entries — the `readonly FailurePattern[]` type annotation on the array provides contextual inference that enforces valid `FailureReason` values at the type level without per-entry casts. | `classifyFailureReason`, `BUILTIN_FAILURE_PATTERNS`, `FailurePattern` |
| `failure-messages.ts` | Centralised user-visible failure notes written to the task `notes` column. `FAST_FAIL_EXHAUSTED_NOTE` surfaces on triple fast-fail (directs users to `~/.fleet/fleet.log` and the Retry button in the Task Detail drawer). `NO_COMMITS_NOTE` surfaces when the agent exits without committing. `NOOP_RUN_NOTE` surfaces when the agent committed only scratch files. | `FAST_FAIL_EXHAUSTED_NOTE`, `NO_COMMITS_NOTE`, `NOOP_RUN_NOTE` |
| `spec-parser.ts` | Pure parser for sprint task spec markdown. Extracts the list of file paths declared in the `## Files to Change` section, handling backtick-quoted, bare, parenthetical-annotated, and em-dash-suffixed bullet variants. Returns `[]` when the section is absent. No I/O — safe to import anywhere. | `extractFilesToChange` |
| `resolve-node.ts` | Resolves an absolute path to a usable `node` binary for packaged `.app` bundles whose PATH lacks fnm/nvm/Homebrew install locations. Probes fnm default alias, highest nvm version, then Homebrew — called by `env-utils.buildAgentEnv()` so all agent spawn paths inherit the correct node dir. Note: Electron's `process.execPath` is intentionally skipped — it's the app binary, not a `node` shim, so prepending its directory to PATH does not help `#!/usr/bin/env node` shebang resolution. | `resolveNodeExecutable` |
| `turn-budget.ts` | Pipeline agent SDK tuning: returns `maxTurns` (75 default; 100 when the spec opts in via `## Multi-File: true`) and exports the `PIPELINE_DISALLOWED_TOOLS` list. The 30-turn default and the 50-turn mixed-stack auto-detection were removed in the Phase-B 2026-04-24 change; the multi-file ceiling was raised from 75 to 100 in the follow-up after measurement showed 75 was the binding operating point, not a margin. Recon blocks: `Bash(git log:*)`, `Bash(git status:*)`, `Bash(git ls-remote:*)`, `Bash(git diff:*)`, `Bash(git reflog:*)`, `Bash(git log --grep:*)`. Network/exfil blocks: `Bash(curl:*)`, `Bash(wget:*)`, `Bash(ssh:*)`, `Bash(scp:*)`, `Bash(nc:*)`, `Bash(gh:*)`, `WebFetch`, `WebSearch`. Pipeline-only — other agent types use their own spawn paths. | `computeMaxTurns`, `PIPELINE_DISALLOWED_TOOLS` |
| `test-touch-check.ts` | Advisory pre-review heuristic — lists files the agent changed via `origin/main..<agentBranch>` (corrected range; previously reversed), then flags any changed `.ts/.tsx/.js/.jsx` source file whose sibling test exists but was not updated. Never blocks the review transition; warning appended to task `notes`. `listChangedFiles` carries a 30s timeout on the git subprocess. | `detectUntouchedTests`, `listChangedFiles`, `formatAdvisory` |
| `backend-selector.ts` | Per-agent-type runtime resolution. `resolveAgentRuntime(agentType, settings) → { backend, model }` reads from a SQLite-backed JSON setting under `agents.backendConfig`. Every agent type (Pipeline, Synthesizer, Copilot, Assistant, Adhoc, Reviewer) routes through this helper; backends are `claude` or `opencode`. `DEFAULT_SETTINGS.opencodeExecutable` is `'opencode'` (PATH lookup); `mergeWithDefaults` fills the field from defaults when the stored value is absent. | `BackendSettings`, `AgentBackendConfig`, `BackendKind`, `loadBackendSettings`, `saveBackendSettings`, `resolveAgentRuntime`, `DEFAULT_SETTINGS`, `SETTING_BACKEND_CONFIG` |
| `noop-detection.ts` | Post-commit guard that classifies an agent run as a no-op when the diff vs `origin/main` contains only Aider scratch artefacts (`.aider*` files and a `.gitignore` whose lines all start with `.aider`). Called from `completion.resolveSuccess` after `hasCommitsAheadOfMain`; on `true`, routes the task through `resolveFailure` with notes `"produced only scratch files"` rather than transitioning to `review`. Empty change lists are treated as not-a-no-op so `hasCommitsAheadOfMain` remains the single authority for the empty-commit path. | `detectNoOpRun`, `NoOpDetectionDeps` |
| `verify-worktree.ts` | Pre-review verification gate. Runs `npm run typecheck` then `npm test --run` inside the worktree after the agent commits. Returns `WorktreeVerificationOutput` — both verification steps carry raw `stdout`, `stderr`, and `durationMs` on both success and failure so downstream consumers (Task 4 / verification-gate) can persist and surface the full output. `CommandResult` is a discriminated union on `ok: boolean` with all three output fields present on both branches. `VerificationResult` / `VerificationFailure` are retained for backward compat. Accepts an injectable `runCommand` so the module can be unit-tested without spawning real npm processes. | `verifyWorktreeBuildsAndTests`, `WorktreeVerificationOutput`, `CommandResult`, `VerificationResult`, `VerificationFailure`, `RunCommand`, `VERIFICATION_STDERR_LIMIT` |
| `worktree-isolation-hook.ts` | SDK `CanUseTool` gate used by pipeline + adhoc agents. Checks absolute paths in Bash command tokens and now also checks paths that follow flag tokens (`-o`, `--output`, `-f`, `--file`, `--log-file`, `--config`) — blocks `curl -o /home/user/exfil` patterns. | `createWorktreeIsolationHook`, `WorktreeIsolationDeps` |

## Agent Personality System

Source: `src/main/agent-system/personality/`

Defines voice, role frame, constraints, and communication patterns for each agent type. Personalities are injected into prompts during agent initialization via `buildAgentPrompt()` in `prompt-composer.ts`.

| Personality | Purpose |
|---|---|
| `pipeline-personality.ts` | Concise, action-oriented pipeline executor — focused on spec compliance and test verification |
| `assistant-personality.ts` | FLEET Project Assistant — pipeline-aware advisor for sprint health, task status, and diagnostics. Leads with answers, checks state before responding, recommends based on actual data |
| `adhoc-personality.ts` | FLEET Dev Agent — conversational coding partner with full tool access, working directly in the user's repository. Emphasizes execution, commits frequently, and suggests sprint pipeline tasks for autonomous work |
| `copilot-personality.ts` | Text-only spec drafting helper — conversational, non-tool-using (no file access, no exploration) |
| `synthesizer-personality.ts` | Structured spec generator from user intent — single-turn, receives code snippets + file tree, outputs markdown with heading sections |
| `fleet-advisor-personality.ts` | FLEET Advisor — always-available floating agent. Concise and diagnostic; leads with real data. Full read access; does NOT make code changes without explicit request. Suggests Adhoc session for coding work. |
| `types.ts` | `AgentPersonality` interface: `voice`, `roleFrame`, `constraints[]`, `patterns[]` |
