# Data

Repository and query layer. All SQLite access lives here.
Source: `src/main/data/`

| Module | Purpose | Key Exports |
|--------|---------|-------------|
| [sprint-pr-ops](sprint-pr-ops.md) | PR lifecycle queries — mark done/cancelled, update mergeable state, list open PRs. `transitionTasksByPrNumber` validates each task's transition via `validateTransition` before bulk-updating; invalid-transition tasks are skipped with a warning. All functions accept optional `db?: Database.Database` for injection. | `markTaskDoneByPrNumber`, `markTaskCancelledByPrNumber`, `updateTaskMergeableState`, `listTasksWithOpenPrs` |
| `sprint-task-crud.ts` | CRUD operations for sprint tasks. `listTasks` takes either a bare status string (legacy) or a `ListTasksOptions` object — `{ status?, repo?, epicId?, tag?, search?, limit?, offset? }` — and pushes every filter and the pagination window into SQL (tag match uses `json_each`; search is case-insensitive `LIKE` on title + spec). `updateTask` accepts an optional `UpdateTaskOptions` argument (`{ caller? }`); `caller` lands in the `task_changes` audit trail as `changed_by` so MCP-originated writes (`'mcp'`) surface distinctly from IPC/UI ones instead of collapsing to `'unknown'`. All exported functions accept optional `db?: Database.Database`. | `getTask`, `listTasks`, `ListTasksOptions`, `listTasksRecent`, `createTask`, `updateTask`, `UpdateTaskOptions`, `deleteTask`, `createReviewTaskFromAdhoc` |
| `sprint-queue-ops.ts` | Queue claim/release/WIP operations. `claimTask` and `releaseTask` validate transitions via `validateTransition` before updating — invalid transitions log a warning and return null instead of silently no-oping. All exported functions accept optional `db?: Database.Database`. | `claimTask`, `releaseTask`, `getActiveTaskCount`, `getQueuedTasks` |
| `sprint-agent-queries.ts` | Agent-specific health and dependency queries. All exported functions accept optional `db?: Database.Database`. | `getQueueStats`, `getOrphanedTasks`, `clearStaleClaimedBy`, `clearSprintTaskFk`, `getHealthCheckTasks`, `getAllTaskIds`, `getTasksWithDependencies` |
| `sprint-planning-queries.ts` | Sprint CRUD queries. All exported functions accept optional `db?: Database.Database`. | `createSprint`, `getSprint`, `getAllSprints`, `updateSprint`, `deleteSprint`, `getSprintTasks` |
| `task-changes.ts` | Audit trail — field-level change recording. `getTaskChanges` accepts either a numeric limit (legacy) or `{ limit?, offset? }` for SQL-level `LIMIT ? OFFSET ?` pagination — upstream callers no longer fetch `limit + offset` rows and slice in memory. Functions accept optional `db?: Database.Database`. | `recordTaskChanges`, `recordTaskChangesBulk`, `getTaskChanges`, `GetTaskChangesOptions`, `pruneOldChanges` |
| `task-group-queries.ts` | Task group (epic) CRUD and dependency management. Functions accept optional `db?: Database.Database`. `CreateGroupInput.goal` / `UpdateGroupInput.goal` are typed `string \| null \| undefined`: `null` writes SQL NULL (create) or clears the column (update); `undefined` on update is filtered out of the SET clause so callers can omit fields without accidentally wiping existing values. | `createGroup`, `listGroups`, `getGroup`, `updateGroup`, `deleteGroup`, `addTaskToGroup`, `removeTaskFromGroup`, `getGroupTasks`, `queueAllGroupTasks`, `reorderGroupTasks`, `addGroupDependency`, `removeGroupDependency`, `updateGroupDependencyCondition`, `getGroupsWithDependencies` |
| `webhook-queries.ts` | Webhook CRUD. All exported functions accept optional `db?: Database.Database`. | `listWebhooks`, `createWebhook`, `updateWebhook`, `deleteWebhook`, `getWebhookById`, `getWebhooks` |
| `dashboard-queries.ts` | Dashboard analytics queries — hourly completions and recent agent events. Transforms raw DB rows to camelCase `DashboardEvent` (including JSON-parsed `payload`) before returning. Functions accept optional `db?: Database.Database`. | `getCompletionsPerHour`, `getRecentEvents` |
| `reporting-queries.ts` | Analytics queries for sprint tasks. All functions accept optional `db?: Database.Database`. | `getDoneTodayCount`, `getFailureReasonBreakdown`, `getTaskRuntimeStats`, `getSuccessRateBySpecType`, `getDailySuccessRate` |
| `sprint-maintenance.ts` | SQLite maintenance ops for sprint_tasks: diff-snapshot pruning, test-artifact cleanup | `pruneOldDiffSnapshots`, `cleanTestArtifacts`, `DIFF_SNAPSHOT_RETENTION_DAYS` |
| `sprint-maintenance-facade.ts` | Stable re-export path for maintenance utilities (snapshot pruning, FK cleanup, update allowlist, test cleanup) | `cleanTestArtifacts`, `pruneOldDiffSnapshots`, `DIFF_SNAPSHOT_RETENTION_DAYS`, `UPDATE_ALLOWLIST`, `clearSprintTaskFk` |
| `data-utils.ts` | Shared error-handling utility for the data layer. Eliminates the repeated try/catch log-and-return pattern. | `withDataLayerError` |
| `migrations/loader.ts` | Loads all migration files via `import.meta.glob` (bundler-safe) and validates the full sequence is contiguous 1..N. Also exports `getPendingMigrations(migrations, currentVersion)` which filters to versions above `currentVersion`, sorts, and throws on any version gap before migrations run. | `loadMigrations`, `getPendingMigrations`, `Migration` |
| `sprint-task-repository.ts` | Repository abstraction layer — `IAgentTaskRepository`, `ISprintPollerRepository`, `IDashboardRepository`, and the composite `ISprintTaskRepository`. Factory `createSprintTaskRepository()` delegates to sprint-queries. `claimTask` accepts optional `maxActive` for atomic DB-level WIP enforcement. `IAgentTaskRepository` also exposes `getQueueStats()` so the drain loop can report queue depth in drain-paused broadcasts. `IDashboardRepository.listTasks` accepts either a bare status string (legacy) or a `ListTasksOptions` object — the filter and pagination push-down that closed the MCP `tasks.list` "load everything then slice" anti-pattern. `IAgentTaskRepository.updateTask` accepts an optional third `UpdateTaskOptions` argument — `{ caller? }` — so MCP-originated writes end up as `'mcp'` in `task_changes.changed_by` instead of collapsing to `'unknown'`. | `ISprintTaskRepository`, `IAgentTaskRepository`, `ISprintPollerRepository`, `IDashboardRepository`, `createSprintTaskRepository`, `ListTasksOptions`, `UpdateTaskOptions` |
