# Components

React UI components, organized by domain group.
Source: `src/renderer/src/components/`

| Module                        | Group          | Purpose                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Key Exports                                                                                                                                                                    |
| ----------------------------- | -------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `CommandPalette.tsx`          | layout         | Command palette with fuzzy search and action execution. Spawns assistant using the first configured repository path.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | `CommandPalette`                                                                                                                                                               |
| `Sidebar.tsx`                 | layout         | V1/V2 feature-flag dispatcher. Renders `SidebarV1` (default) or `SidebarV2` based on `featureFlags.v2Shell`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | `Sidebar`                                                                                                                                                                      |
| `SidebarV1.tsx`               | layout         | V1 icon-only sidebar (52px). Original implementation preserved for feature-flag rollback.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | `SidebarV1`                                                                                                                                                                    |
| `SidebarV2.tsx`               | layout         | V2 sidebar (200px). Shows workspace/branch header, labeled nav rows with active rail, live agents block (up to 3 active pipeline tasks), and footer with model + version. Collapses to 52px icon-only via container query at <1024px.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | `SidebarV2`                                                                                                                                                                    |
| `UnifiedHeader.tsx`           | layout         | V1/V2 feature-flag dispatcher. Renders `UnifiedHeaderV1` (default) or `UnifiedHeaderV2` based on `featureFlags.v2Shell`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | `UnifiedHeader`                                                                                                                                                                |
| `UnifiedHeaderV1.tsx`         | layout         | V1 header (44px). B logo button, tab strip, HealthStrip + NeonBadge (tokens) + NotificationBell + theme toggle. Preserved for feature-flag rollback; rendered when `featureFlags.v2Shell = false`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | `UnifiedHeaderV1`                                                                                                                                                              |
| `UnifiedHeaderV2.tsx`         | layout         | V2 header (38px). Brand block (F mark + FLEET wordmark, 200px), tab strip, `+` add-tab button (opens command palette), CommandPill + HealthChip + TokenChip + NotificationBell + theme toggle. Header has `-webkit-app-region: drag`; interactive zones opt out. Rendered when `featureFlags.v2Shell = true`.                                                                                                                                                                                                                                                                                                                                                                                                                                                               | `UnifiedHeaderV2`                                                                                                                                                              |
| `CommandPill.tsx`             | layout         | Search/command pill button that opens the command palette. Collapses to icon-only at <1280px via container query.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | `CommandPill`                                                                                                                                                                  |
| `HealthChip.tsx`              | layout         | Pill-style agent health indicator. Shows pulse when agents running; failures count in `--st-failed` only when >0. V2 replacement for `HealthStrip`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | `HealthChip`                                                                                                                                                                   |
| `TokenChip.tsx`               | layout         | Pill showing total token count. Reads from `useCostDataStore`. V2 replacement for `NeonBadge` in header.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | `TokenChip`                                                                                                                                                                    |
| `LiveAgentRow.tsx`            | layout         | Single row in the V2 sidebar live agents block. Shows fleet-pulse, task title, and indeterminate progress bar.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | `LiveAgentRow`                                                                                                                                                                 |
| `AgentCard.tsx`               | agents         | Compact card showing agent status, cost, and duration. Uses `useBackoffInterval` for the live duration ticker. Outer button has `aria-label` combining task and status. `STATUS_ACCENTS` typed as `Record<AgentMeta['status'], NeonAccent>` for exhaustiveness. `KILL_REFRESH_DELAY_MS = 500` replaces the magic literal.                                                                                                                                                                                                                                                                                                                                                  | `AgentCard`                                                                                                                                                                    |
| `AgentRow.tsx`                | agents         | V2 compact list-row for a single agent in the agents sidebar. Shows `fleet-pulse` (running) or `fleet-dot--{status}` (terminal), agent id, relative age, repo prefix + current step or task, and a progress bar with `data-testid="progress-bar"` (running only). Selected state uses V2 design tokens. Props: `agent`, `selected`, `onClick`, `currentStep?`, `progressPct?`. | `AgentRow`, `AgentRowProps` |
| `ScratchpadBanner.tsx`        | agents         | Dismissible informational banner explaining that agents in the Agents view run in isolated worktrees outside the sprint pipeline. Left accent border uses `--accent`. Dismiss button carries `aria-label="Dismiss scratchpad notice"`. | `ScratchpadBanner`, `ScratchpadBannerProps` |
| `AgentConsole.tsx`            | agents         | Terminal-style detail pane for a single agent session. Virtualised event list (tanstack-virtual). Search bar (Cmd+F) with `Set<number>`-based O(1) per-row match lookup. Waiting-for-output placeholder has `role="status"` and decorative Loader icon is `aria-hidden`. | `AgentConsole` |
| `AgentList.tsx`               | agents         | V2 left-panel agent list. Header band: FLEET eyebrow + agent count + Spawn button + 4px composition strip (proportional status segments). Filter chips (all/live/review/failed/done) with status dots and counts. Restyled search input (28px, `--surf-1`, mono 12px). ScratchpadBanner when `showBanner` is true. Scrollable rows grouped into "Live · {n}" (running) and "Recent" (all others) using `AgentRow`. Filter chips switch to a flat list when a non-all filter is active. Empty/loading (5 skeletons)/error states. `hasMore`/`onLoadMore` are V1-compat no-ops. | `AgentList`, `AgentListProps`, `groupAgents`, `AgentGroups` |
| `CollapsibleBlock.tsx`        | agents         | Shared collapsible wrapper used in tool cards. Accepts optional `label` prop; when set, aria-label reads "Expand/Collapse \<label\>" for screen-reader context. | `CollapsibleBlock` |
| `cards/ConsoleCard.tsx`       | agents         | Router component dispatching to type-specific card components. | `ConsoleCard` |
| `cards/McpDisclosureCard.tsx` | agents         | Renders the MCP server disclosure line in the agent console immediately after spawn — lists file-based servers loaded and notes that managed connectors are not available. | `McpDisclosureCard` |
| `cards/TextCard.tsx`          | agents         | Renders agent text output as markdown. Removed dead `timestamp` and `searchClass` props. | `TextCard` |
| `cards/ThinkingCard.tsx`      | agents         | Reasoning block with preview/expanded toggle. `THINKING_PREVIEW_MAX_CHARS = 120` replaces magic literal; emoji header is `aria-hidden`; removed dead `timestamp`/`searchClass` props. | `ThinkingCard` |
| `cards/ErrorCard.tsx`         | agents         | Error message card. Removed dead `timestamp` and `searchClass` props. | `ErrorCard` |
| `cards/ToolCallCard.tsx`      | agents         | Tool call card with collapsible expanded content. `renderExpandedContent` memoised; `input` guarded before cast; accepts `onPlaygroundClick` forwarded from `ToolGroupCard`. | `ToolCallCard` |
| `cards/ToolPairCard.tsx`      | agents         | Paired tool-call + tool-result card with collapsible expanded content. Same memoisation and input-guard improvements as `ToolCallCard`. | `ToolPairCard` |
| `cards/ToolGroupCard.tsx`     | agents         | Groups multiple tool calls into one collapsible block. `onPlaygroundClick` now forwarded through `renderToolBlock` to child cards. | `ToolGroupCard` |
| `cards/CompletionCard.tsx`    | agents         | Agent completion stats card. Status icon (✓/✗) is `aria-hidden` to avoid duplicate announcement. | `CompletionCard` |
| `ConsoleHeader.tsx`           | agents         | 56px glass header for AgentConsole — status dot, task name, model badge, duration, phase, cost, and ctx token counter. Ctx counter calls `window.api.agents.getContextTokens` once on mount and polls every 3s while running; displays the latest-turn context window size live, and the peak across the run for finished agents. Action buttons: open terminal, stop (running), promote to Code Review (adhoc with worktree), copy log.                                                                                                                                                                                                                                                                                                                                    | `ConsoleHeader`                                                                                                                                                                |
| `CreateEpicModal.tsx`         | planner        | Form modal for creating new epics (task groups). Fields: name (required), icon, goal, accent color. Template selection supported — templates populate fields and their tasks. Repo selection defaults to the first configured repo via `useRepoOptions()` — no hardcoded repo list.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | `CreateEpicModal`                                                                                                                                                              |
| `EpicHeader.tsx`              | planner        | Header region of the epic detail panel. Renders epic icon, name, goal, Ask AI button, overflow menu (edit/ready/pause-resume/complete/delete), and a 3px progress stripe (`doneCount/totalCount`). Props: `group`, `isReady`, `isCompleted`, `doneCount`, `totalCount`, `onOpenAssistant`, `onEdit`, `onToggleReady`, `onMarkCompleted`, `onDelete`, `onTogglePause`. Overflow menu item count depends on `isCompleted`: 4 items when completed (no "Mark as Completed"), 5 items otherwise.                                                                                                                                                                                                                                                                                 | `EpicHeader`, `EpicHeaderProps`                                                                                                                                                |
| `EpicDetail.tsx`              | planner        | Full detail panel for a selected epic. Computes `doneCount`/`totalCount` from tasks and passes them to `EpicHeader`. Accepts `onOpenAssistant: () => void` prop and threads it through to `EpicHeader`. Optional `onTogglePause?: () => void` is threaded to `EpicHeader` (defaults to a no-op so call sites that haven't wired it yet remain valid). Also renders `EpicProgress`, `EpicDependencySection`, `TaskList`, and the queue bar.                                                                                                                                                                                                                                                                                                                                  | `EpicDetail`, `EpicDetailProps`                                                                                                                                                |
| `TaskWorkbenchModal.tsx`      | task-workbench | Wraps the shared `Modal` primitive (size `lg`) to host `TaskWorkbench` for create/edit. Mounted once at app root; opened from Pipeline (Edit) and Planner (Add/Edit) via `useTaskWorkbenchModalStore`. Dismissal funnels through `requestDismiss()`, which calls the form-store `isDirty()` check and prompts via `useConfirm` before discarding. Modal title is "New Task" in create mode and `Edit: <task title>` (truncated to 80 chars) in edit mode.                                                                                                                                                                                                                                                                                                                   | `TaskWorkbenchModal`                                                                                                                                                           |
| `Modal.tsx`                   | ui             | Shared modal primitive: backdrop, focus trap (`useFocusTrap`), portal to `document.body`, ESC + backdrop-click dismissal, size presets `sm` (≤440px), `md` (720px), `lg` (1200×800), `fullscreen` (95vw / 90vh). Header renders an optional title and close button; body is the children. Animates in/out via framer-motion's `scaleIn` variant. Use for any new modal — don't reimplement backdrop/focus-trap. `ConfirmModal` and `TaskWorkbenchModal` already wrap it.                                                                                                                                                                                                                                                                                                    | `Modal`, `ModalSize`                                                                                                                                                           |
| `PlannerAssistant.tsx`        | planner        | AI planning assistant drawer that slides over the epic detail panel. Streams chat via `workbench:chatStream`; parses `[ACTION:...]` markers from assistant replies to surface create-task, create-epic, and update-spec action cards inline. `update-spec` cards embed a `SpecDiffViewer` toggle. When 2+ pending `update-spec` cards exist for a message, an "Apply All" button appears; it is disabled while in-flight (`isApplyingAll` state). Exported `parseActionMarkers` is unit-tested. System prompt explicitly bans exploratory verbs in `## Implementation Steps`.                                                                                                                                                                                                | `PlannerAssistant`, `PlannerAssistantProps`, `parseActionMarkers`, `ParseResult`                                                                                               |
| `SpecDiffViewer.tsx`          | planner        | Collapsible before/after diff viewer for update-spec action cards. Renders collapsed ("Show changes +N / -N") by default; expands to show line-level diff using `buildSyntheticDiff` + `parseDiff`. Reuses `.edit-diff-card__row--add/del/ctx` CSS classes from `EditDiffCard.css`. When `oldSpec` is null, del lines are suppressed so the diff reads as all-additions.                                                                                                                                                                                                                                                                                                 | `SpecDiffViewer`, `SpecDiffViewerProps`                                                                                                                                        |
| `WorkbenchForm.tsx`           | task-workbench | Task creation/edit form with AI copilot, dependency picker, and validation checks. Repo dropdown is populated dynamically via `useRepoOptions()` — no hardcoded repo list. Priority and Dependencies are always visible; Advanced section contains cost, playground, and cross-repo contract. Per-task model selection was removed once Settings → Models became the single routing source.                                                                                                                                                                                                                                                                                                                                                                                 | `WorkbenchForm`                                                                                                                                                                |
| `SpecEditor.tsx`              | task-workbench | Spec textarea with template buttons (Feature, Bug Fix, Refactor, Test) sourced from `DEFAULT_TASK_TEMPLATES` in `src/shared/constants.ts`, Generate Spec, Research Codebase, quality hint indicators, and a hint explaining verbatim execution (## heading requirement).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | `SpecEditor`                                                                                                                                                                   |
| `DependencyPicker.tsx`        | task-workbench | Dependency selection and management UI. Renders a list of selected dependencies with Hard/Soft type toggle, cascade semantics dropdown ("Unblock when"), and remove buttons. Includes a dropdown search picker for adding tasks from the available list. Help text explains Hard (blocks on failure) vs Soft (unblocks regardless).                                                                                                                                                                                                                                                                                                                                                                                                                                    | `DependencyPicker`                                                                                                                                                             |
| `BatchActionsToolbar.tsx`     | code-review    | Renders the batch action buttons (Merge All, Ship All, Create PRs, Discard All, Clear). In-flight spinner state via `BatchActionKey`. Extracted from `TopBar` to eliminate JSX duplication. Count span has `aria-live="polite"`; each batch action button has an `aria-label` that includes the selection count (the Clear button does not).                                                                                                                                                                                                                                                                                                                                                                                                                                | `BatchActionsToolbar`                                                                                                                                                          |
| `TaskRunMetrics.tsx`          | code-review    | Inline cost + duration display for a task in the Code Review top bar. Renders `$0.97 · 5m 50s` (and appends `· N retries` when `retry_count > 0`). Returns null when cost or duration data is unavailable. Props: `task: SprintTask`. Uses `useTaskCost` hook and `formatDurationMs` utility.                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | `TaskRunMetrics`                                                                                                                                                               |
| `RepositoriesSection.tsx`     | settings       | Settings section for repository CRUD. Lists configured repos as `SettingsCard` entries. Each card footer has an "Env vars" button that opens an inline key-value editor for per-repo environment variables. Manual add form validates the `localPath` via `window.api.git.detectRemote` at save time: non-git paths show an inline error and block save; directory-name/repo-name mismatches show an inline warning but allow saving. Includes `RepoDiscoveryModal` for discovery-based add, and a color picker.                                                                                                                                                              | `RepositoriesSection`                                                                                                                                                          |
| `ConnectionsSection.tsx`      | settings       | Settings section for auth status, GitHub credential management, webhooks, and local MCP server. Displays Claude Code CLI auth status (CLI found, token status, expiry). Shows system encryption availability via green/red encryption status banner. Includes a "Read-only mode (skip GitHub)" checkbox that toggles `settings.githubOptedOut` so users can enter or leave opted-out mode without re-running onboarding. Embeds `WebhooksSection` and `LocalMcpServerSection` at the bottom.                                                                                                                                                                                                                                                                                | `ConnectionsSection`                                                                                                                                                           |
| `LocalMcpServerSection.tsx`   | settings       | Settings card for the local MCP server — enable/disable toggle, port input, bearer token display/copy/regenerate, and "Copy Claude Code config" button that writes the JSON snippet to clipboard. Reads `mcp.enabled` and `mcp.port` from settings on mount; fetches the bearer token via `mcp:getToken` and the last rotation timestamp via `mcp:getTokenRotatedAt`. After enabling the server, waits 800 ms then re-fetches the token (which may have been freshly minted) and the rotation timestamp so the displayed value stays current. A "Rotated at …" hint appears below the token whenever a rotation timestamp is available. | `LocalMcpServerSection`                                                                                                                                                        |
| `AgentManagerSection.tsx`     | settings       | Settings section for pipeline configuration (concurrency, worktree base, runtime, auto-start) and agent permissions. Embeds `AgentPermissionsSection` below the pipeline config card. Model selection lives in Settings → Models — not here.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | `AgentManagerSection`                                                                                                                                                          |
| `AgentPermissionsSection.tsx` | settings       | Agent tool permission editor — consent banner, preset buttons (Recommended/Restrictive/Permissive), allow-tool grid with names + descriptions, and custom deny-rule list. Reads/writes `~/.claude/settings.json` via `claudeConfig` IPC.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | `AgentPermissionsSection`                                                                                                                                                      |
| `AppearanceSection.tsx`       | settings       | Settings section for theme, accent color, tear-off window behavior, notifications, and keyboard shortcuts. Embeds `NotificationsSection` and `KeybindingsSettings`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | `AppearanceSection`                                                                                                                                                            |
| `AboutSection.tsx`            | settings       | Settings section for app version, source link, and API usage history. Embeds `CostSection`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | `AboutSection`                                                                                                                                                                 |
| `DocumentationSection.tsx`    | settings       | Settings → Documentation tab. Two-column layout: left sidebar nav (five topics, active state via IntersectionObserver) + right scrollable content area. Renders `DOCUMENTATION_TOPICS` from `documentation-data.ts` — Epics, Writing a Task Spec, Task Dependencies, Agent Types, MCP Integration. Supports code blocks (with filename labels), tables, and pass/fail badge lists. | `DocumentationSection` |
| `TaskTemplatesSection.tsx`    | settings       | Settings section for managing task prompt templates. Renders one `SettingsCard` per template — built-ins show a `Built-in` badge and a Reset action; custom templates show an editable name input and a Delete action. Long markdown lives in a `rows={12}` textarea with `min-height: 280px` and `resize: vertical` (per `TaskTemplatesSection.css`), so multi-paragraph prompts are scannable without truncation. The redundant disabled name input is hidden on built-in cards — the title + badge already communicate the name. Saves are debounced 500ms after the last edit.                                                                                                                                                                                          | `TaskTemplatesSection`                                                                                                                                                         |
| `ReviewActionsBar.tsx`        | code-review    | Review action buttons (Ship It/Connect GitHub →, Merge Locally, Create PR, Shipped Outside FLEET, Revise, Discard) plus freshness badge, rebase button, and "Open in Finder" button. Renders in `full` or `compact` variant (compact uses a render-prop). When GitHub is not configured, the Ship It button is replaced by a "Connect GitHub →" button that navigates to Settings → Connections. When `freshness.status === 'conflict'` a conflict-resolution banner appears with an "Open in IDE" button. "Open in Finder" appears in the rebase-status row when `worktreePath` is set — calls `window.api.window.openWorktreePath()`. "Shipped Outside FLEET" marks the task done without a git operation. Revise button shows "Max revisions (5/5)" and is disabled once 5 revision requests have been made.               | `ReviewActionsBar`, `ReviewActionCallbacks`                                                                                                                                    |
| `ReviewMetricsRow.tsx`        | code-review    | Three metric cards (Quality, Issues, Files) showing AI review statistics. Quality score displays a rubric tooltip (via `title` attribute) that explains the three rating bands: <50 (significant issues), 50–74 (minor issues), 75+ (good quality). Quality card shows score or `—` when loading/undefined. Issues and Files cards display counts or `—` when unavailable. All three cards have semantic `role="status"` and descriptive `aria-label` attributes.                                                                                                                                                                                                                                      | `ReviewMetricsRow`                                                                                                                                                             |
| `ReviewQueue.tsx`             | code-review    | Two-section sidebar: "Pending Review" (`status='review'`) and "Approved" (`status='approved'`). j/k keyboard navigation traverses both sections. Approved section header shows a "Build PR" button (opens `PrBuilderModal`) when approved tasks exist. Select-all batch checkbox covers pending review tasks only. Scoped store subscription via `useShallow`. | `ReviewQueue` |
| `PrBuilderModal.tsx`          | code-review    | Full-screen PR group composer. Two-panel layout: left pool of unassigned approved tasks (draggable via HTML5 DnD), right list of `PrGroup` cards. Each card accepts dropped tasks, edits title/branch/description on blur, and triggers `buildGroup` to push a stacked PR. Uses `usePrGroups(repo)` for data. Props: `open`, `repo`, `onClose`. | `PrBuilderModal` |
| `VirtualizedDiffContent.tsx`  | diff           | Virtualized rendering of diff rows (file headers, hunk headers, code lines, and comment rows). Manages scroll/viewport via ResizeObserver and binary-search visibility window. Comment rows (`CommentRow`) are injected by `buildFlatRows` after each code line that has associated `PrComment` or `PendingComment` entries, so comments are visible without disabling virtualization. Exports shared row types (`FlatRow`, `HunkAddress`) and height constants used by `DiffViewer`.                                                                                                                                                                                                                                                                                       | `VirtualizedDiffContent`, `FlatRow`, `HunkAddress`, `CommentRow`, `buildFlatRows`, `rowHeight`, `ROW_HEIGHT`, `FILE_HEADER_HEIGHT`, `HUNK_HEADER_HEIGHT`, `COMMENT_ROW_HEIGHT` |
| `VirtualizedDiffBanner.tsx`   | diff           | Banner shown above large diffs in virtualized mode, with a "Load full diff" button to disable virtualization and unlock new-comment authoring.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | `VirtualizedDiffBanner`                                                                                                                                                        |
| `GhStep.tsx`                  | onboarding     | Onboarding step that checks gh CLI availability and authentication via `onboarding:checkGhCli` IPC. Shows two check rows: "gh CLI installed" and "gh CLI authenticated". Now surfaces a copy-pasteable `gh auth login` command (with a copy button) when the user is not authenticated, and exposes a "Skip — read-only mode" button that writes `settings.githubOptedOut = 'true'`. The primary "Next" button writes `'false'` so re-running onboarding can recover a user who opted out earlier.                                                                                                                                                                                                                                                                          | `GhStep`                                                                                                                                                                       |
| `AuthStep.tsx`                | onboarding     | Onboarding step that checks Claude Code CLI auth via `auth.status` IPC. Wraps the check in a 10 s `withTimeout` — on timeout shows a `role="alert"` fallback. When checks fail, shows a preflight note (`role="note"`) explaining that "Continue Anyway" is safe because agents authenticate via the SDK at spawn time, and naming macOS Keychain ACL as the common cause of false negatives on unsigned bundles. When CLI is missing, surfaces copy-pasteable install command; when CLI is present but token is missing, surfaces `claude login`. | `AuthStep`                                                                                                                                                                     |
| `GitStep.tsx`                 | onboarding     | Onboarding step that checks git availability via `git.checkInstalled` IPC (runs `git --version`). Shows check status icon. Next button disabled while checking or when git is unavailable.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | `GitStep`                                                                                                                                                                      |
| `Onboarding.tsx`              | (root)         | First-launch setup check screen. Runs required checks (CLI, token, expiry, git) and optional checks (repos). Auto-advances when all required checks pass. Bypass button is **always** enabled (not `checking`) — the old "disabled-when-failed" trap is gone. Label becomes "Continue without `<check>`" (or "Continue without N required checks") so users know exactly what they're skipping, and a `role="alert"` risk callout names the skipped items with a "fix in Settings" pointer. Retry button renamed "Re-check" with explicit "after running the command, click Re-check to verify" guidance next to the instruction code block.                                                                                                                                | `Onboarding`                                                                                                                                                                   |
| `PreloadBridgeError.tsx`      | (root)         | Fallback UI rendered from `main.tsx` when `window.api` is undefined at startup (preload script failed to load). Shows actionable recovery steps: right-click → Open, `xattr -dr com.apple.quarantine /Applications/FLEET.app`, or reinstall. Pure presentational — no hooks or IPC.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | `PreloadBridgeError`                                                                                                                                                           |
| `PlaygroundModal.tsx`         | agents         | Full-screen modal for previewing agent-generated playground files (HTML, SVG, Markdown, JSON). HTML and SVG use sandboxed iframe with DOMPurify sanitization; Markdown and JSON rendered as `<pre>` text. Editable source pane with syntax-highlight overlay. Re-sanitizes user edits via `playground:sanitize` IPC before rendering. Supports Split/Preview/Source modes. Scripts-enabled badge shown only for HTML.                                                                                                                                                                                                                                                                                                                                                       | `PlaygroundModal`, `PlaygroundModalProps`                                                                                                                                      |
| `PlaygroundCard.tsx`          | agents         | Compact card for playground previews in agent chat. Shows filename, file size, and content-type badge (HTML/SVG/Markdown/JSON). Click to open PlaygroundModal.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | `PlaygroundCard`, `PlaygroundCardProps`                                                                                                                                        |
| `render-agent-markdown.tsx`   | agents         | Lightweight markdown-to-JSX renderer for agent-authored text. Handles `**bold**`, `` `code` ``, and `##`/`###` headings. Colocated CSS (`render-agent-markdown.css`) provides the `.console-md-*` primitives. Imported wherever agent messages render (agent console, SpecPanel, FLEET Advisor).                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | `renderAgentMarkdown`                                                                                                                                                          |
| `SprintPipeline.tsx`          | sprint         | V1/V2 feature-flag dispatcher. Renders `SprintPipelineV1` (default) or `SprintPipelineV2` based on `featureFlags.v2Pipeline`. | `SprintPipeline` |
| `SprintPipelineV1.tsx`        | sprint         | V1 three-zone pipeline layout (preserved for feature-flag rollback): backlog sidebar, five stage columns, and a conditional task detail drawer. Reads `pollError` from `sprintTasks` store and renders a dismissible `ErrorBanner` with a Retry button. Imports `useSprintPipelineState` for all store subscriptions. | `SprintPipelineV1` |
| `SprintPipelineV2.tsx`        | sprint         | V2 pipeline composition (feature-flagged). Mirrors V1's hook set and callback tree, swaps V1 components for V2 variants: `PipelineHeaderV2`, `PipelineFilterBarV2`, `PipelineBacklogV2`, `PipelineStageV2`, `TaskDetailDrawerV2`. Replaces inline banners with `PollErrorBanner`, `OrphanRecoveryBanner`, `DrainPausedBanner`. Empty states use flat token-based divs instead of `NeonCard`. | `SprintPipelineV2` |
| `PipelineHeaderV2.tsx`        | sprint         | V2 52px pipeline header. Stat chips (count + label), right-side controls (conflict badge, health badge, density toggle, DAG toggle, export dropdown, drain trigger, New Task button). `ExportDropdown` is an internal component. | `PipelineHeaderV2`, `PipelineHeaderV2Props` |
| `PipelineFilterBarV2.tsx`     | sprint         | V2 38px filter strip. Token-based search input, repo chips, preset chips, "Save View" button. Same filter store bindings as V1; renders null when repos ≤ 1 and no active filters. | `PipelineFilterBarV2` |
| `primitives/PriorityChip.tsx` | sprint         | V2 primitive: priority badge (`P1`/`P2`/`P3`) with `--st-failed` / `--st-blocked` / `--fg-3` tint derived from priority number. | `PriorityChip` |
| `primitives/EyebrowTitle.tsx` | sprint         | V2 primitive: stacked eyebrow + title block with optional right slot. Eyebrow uses `.fleet-eyebrow`, title at 13px/500 weight. | `EyebrowTitle` |
| `primitives/DrawerSection.tsx`| sprint         | V2 primitive: padded section container with eyebrow + title header row and bottom divider. Used inside the V2 task detail drawer. | `DrawerSection` |
| `primitives/MiniStat.tsx`     | sprint         | V2 primitive: small stat cell with eyebrow label and mono value. Used inside the V2 task detail drawer for cost, duration, etc. | `MiniStat` |
| `StatusDot.tsx`               | ui             | Shared status indicator dot using `fleet-dot fleet-dot--{kind}` CSS classes. Six kinds: `running`, `queued`, `review`, `blocked`, `failed`, `done`. Configurable `size` prop (default 6px). | `StatusDot`, `StatusDotKind` |
| `Tag.tsx`                     | ui             | Mono-font pill tag with `--surf-2` background and `--line` border. Used for repo/label tags on task cards. | `Tag` |
| `PipelineFilterBar.tsx`       | sprint         | Search and repo-chip filter bar for the Sprint Pipeline. The search input uses a 150ms debounced local state so keystrokes respond instantly while store updates (and the resulting re-partition) are deferred. Debounce timer is cleaned up on unmount via `useRef`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | `PipelineFilterBar`                                                                                                                                                            |
| `PipelineStage.tsx`           | sprint         | Named-status column within the Sprint Pipeline. Renders a capped visible slice of tasks (`STAGE_VISIBLE_LIMIT = 20`) with a "Show more / Show less" toggle. The visible slice is computed by `useMemo([tasks, expanded])` so the slice is only recomputed when the task list or expansion state changes — not on every parent render. Wrapped in `React.memo` via `PipelineStageInner`.                                                                                                                                                                                                                                                                                                                                                                                     | `PipelineStage`                                                                                                                                                                |
| `PipelineStageV2.tsx`         | sprint         | V2 stage column. Token-based layout with `--surf-1` background, `--r-lg` border radius, 40px head bar (StatusDot + label + count), and scrollable body. Count turns `--st-failed` when `active` stage exceeds `WIP_LIMIT_IN_PROGRESS`. Empty state is a dashed `--line` box with an em dash. Shows `TaskPillV2` (default) or `TaskRowV2` (compact density). Wrapped in `React.memo`. | `PipelineStageV2` |
| `TaskPill.tsx`                | sprint         | Single task card rendered inside a `PipelineStage`. Wrapped in `React.memo`. Uses `useNow()` for the stale-task calculation so the 10-second tick (not every parent render) is what triggers re-evaluation — keeping memo effective between ticks. Shows a failure category chip (from `failureCategoryForReason`) on failed/error/cancelled tasks that have a `failure_reason`. Shows a pulsing "starting…" label on queued tasks with `claimed_by` set (agent spawn window). When blocked, displays a hover tooltip ("Blocked by: <task names>") by querying the sprint tasks store for blocking task titles from `depends_on` IDs. The status dot and activity pulse span are `aria-hidden="true"` (decorative).                                                                                                                                      | `TaskPill`                                                                                                                                                                     |
| `TaskPillV2.tsx`              | sprint         | V2 task card. Column layout: top meta row (id + PriorityChip + contextual right tag), title, optional `LiveAgentBlock` for active tasks (the only place `.fleet-pulse` appears in this file), bottom row (tags + repo). `LiveAgentBlock` renders a time-elapsed label, percent-complete derived from `started_at` / `max_runtime_ms`, and a 2px progress bar. Multi-select via shift/meta/ctrl. Wrapped in `React.memo`. | `TaskPillV2` |
| `TaskRow.tsx`                 | sprint         | Compact single-line task row for the `compact` density view in `PipelineStage`. Ticks every 10s via `useBackoffInterval` when active to keep elapsed time fresh. | `TaskRow` |
| `TaskRowV2.tsx`               | sprint         | V2 compact row (28px height). Inline flex: StatusDot + id (mono) + title (truncated, flex-1) + PriorityChip + elapsed for active tasks. No fleet-pulse. Wrapped in `React.memo`. | `TaskRowV2` |
| `PipelineHeader.tsx`          | sprint         | Pipeline view header with task stat badges, density toggle, export menu, WIP slot capacity badge, and drain-loop trigger button. Renders agent concurrency status (active/max slots) polled every 5s from agent manager via `useBackoffInterval`. The ↻ "Check now" button calls `agent-manager:triggerDrain` IPC immediately and spins for 1.5s so users get instant feedback after queuing a task. Stat badges are clickable to filter the pipeline.                                                                                                                                                                                                                                                                                                                      | `PipelineHeader`                                                                                                                                                               |
| `TaskDetailDrawer.tsx`        | sprint         | Task detail side panel for the Sprint Pipeline. Shows status, dependencies, spec/prompt links, agent activity preview, and PR info. The failure block is a labelled `<section>` ("Failure details" / "Cancellation details") shown for `failed`/`error`/`cancelled` tasks — renders a `failureCategoryForReason` chip (pre-computed as `failureChip`), `failure_reason` as a `<pre>`, a watchdog verdict callout when `failure_reason === 'timeout'` and notes mention "watchdog", structured `VerificationDiagnostics` when `task.notes` is a valid `RevisionFeedback` JSON, falling back to a plain `<pre>`, plus the last three agent error events (pre-computed as `recentErrorItems`). Override section: "Force Release" (active tasks with `claimed_by`), "Mark Failed" (`queued`/`active`/`blocked`), "Force Done" (any non-`done` status). All three override handlers are wrapped in try/catch and call `toast.error` on failure — matching the `handleForceRelease` pattern. | `TaskDetailDrawer`, `TaskDetailDrawerProps`                                                                                                                                    |
| `TaskDetailActionButtons.tsx` | sprint         | Status-aware action bar rendered inside `TaskDetailDrawer`. Renders Retry (primary) for any `failed`/`error`/`cancelled` task when `onRetry` is provided; otherwise renders the status-appropriate mix of Launch/Edit/Stop/Unblock/Clone/Delete/Export buttons. All buttons carry `aria-busy` + spinner during async handlers; Retry carries a task-named `aria-label`.                                                                                                                                                                                                                                                                                                                                                                                                     | `TaskDetailActionButtons`                                                                                                                                                      |
| `TaskDetailDrawerV2.tsx`      | sprint         | V2 task detail panel. Fixed 340px width, token-based styling, no CSS class dependencies. Contains sticky header (id prefix, priority chip, status dot with `fleet-pulse` only for active, title, tags/repo), action row, and `DrawerSection` blocks: BRIEF (spec with edit button), LIVE (pulse + progress bar + 3-up MiniStat grid, active/claimed only), GRAPH (dependency status dots), TRACE (last 8 events), META (timestamps/PR/agent link), FAIL (failure chip + notes, terminal statuses), PR (branch-only fallback), OPS (force overrides). Reuses all V1 IPC and modal logic verbatim. | `TaskDetailDrawerV2`, `TaskDetailDrawerV2Props` |
| `TaskDetailActionButtonsV2.tsx` | sprint       | V2 status-aware action row. Three button variants: primary (accent fill), secondary (ghost), danger (red ghost). Status→button mapping: backlog/queued: Launch/Edit/Delete; blocked: Unblock/Edit/Delete; active: Open in Agents/Stop; review: Review Changes/Open Spec/Discard; approved: Open Spec/Delete; done: Open in Agents/Export; failed/error: Retry/Edit/Delete; cancelled: Retry/Delete. Full `handleAction` async wrapper + `loadingAction` state preserved from V1. | `TaskDetailActionButtonsV2`, `ActionButtonsV2Props` |
| `PreflightWarningModal.tsx`   | sprint         | Modal shown when pipeline agent pre-flight detects missing toolchain binaries or missing `.npmrc` env vars. Subscribes to `agent:preflightWarning` broadcasts. Presents missing binary list and/or missing env var list (with a hint to configure them in Settings → Repositories) with "Move to backlog" / "Proceed anyway" actions. Backed by `preflightStore`.                                                                                                                                                                                                                                                                                                                                                                                                           | `PreflightWarningModal`                                                                                                                                                        |
| `PipelineBacklog.tsx`         | sprint         | V1 backlog and failed-tasks sidebar (280px). Preserved for feature-flag rollback. Renders backlog cards with checkbox multi-select and a failed section with requeue/clear actions.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | `PipelineBacklog`                                                                                                                                                              |
| `PipelineBacklogV2.tsx`       | sprint         | V2 backlog and failed-tasks rail (280px). Same props and behavior as V1 but reskinned with V2 design tokens: `TRIAGE` eyebrow, mono task IDs, `PriorityChip`, `Tag` chips, accent rail on selected rows, dashed "+ N more" expand button. Failed section uses `NEEDS ATTENTION` eyebrow and red accent rail. Double-click a backlog row to add to queue. Exported as `React.memo` with `displayName = 'PipelineBacklogV2'`.                                                                                                                                                                                                                                                                                                                                                | `PipelineBacklogV2`, `PipelineBacklogV2Props`                                                                                                                                  |
| `BulkActionBar.tsx`           | sprint         | Floating bar shown when one or more tasks are selected. V2 reskin: outer div uses `--surf-2` background and design-token spacing; count displayed as a rounded chip with accent tint. All action logic (cancel/requeue/delete via `sprint.batchUpdate` IPC) and `AssignEpicPopover` preserved unchanged.                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | `BulkActionBar`                                                                                                                                                                |
| `banners/PollErrorBanner.tsx` | sprint         | Standalone poll-error banner extracted from inline JSX in SprintPipeline. Red left-border alert with message text, Retry, and Dismiss buttons.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | `PollErrorBanner`, `PollErrorBannerProps`                                                                                                                                      |
| `banners/DrainPausedBanner.tsx` | sprint       | Drain-loop paused banner with blocked-color left border. Shows reason, queued task count, and a countdown to resume derived from `pausedUntil - now`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | `DrainPausedBanner`, `DrainPausedBannerProps`                                                                                                                                  |
| `banners/OrphanRecoveryBanner.tsx` | sprint    | Orphan-recovery banner with dynamic left border (running-color when no exhausted, failed-color when exhausted > 0). Shows counts of recovered and exhausted tasks.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | `OrphanRecoveryBanner`, `OrphanRecoveryBannerProps`                                                                                                                            |
| `DoneStep.tsx`                | onboarding     | Final onboarding wizard step. With multiple repos configured, renders a `<select>` labelled "Start in repository" — whatever the user picks is used as the first-task repo (no longer hardcoded to `repoOptions[0]`). With no repos configured, the CTA swaps to "Add a repository" which opens Settings; "Create your first task" is hidden in that state to avoid asking the user to create a task against an empty repo list.                                                                                                                                                                                                                                                                                                                                            | `DoneStep`                                                                                                                                                                     |
| `WelcomeStep.tsx`             | onboarding     | First step of the onboarding wizard — introduces FLEET and links forward. Back button is wired to the `onBack` prop (formerly a no-op stub); it still only renders when `isFirst` is false, so the default first-step render is unchanged.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | `WelcomeStep`                                                                                                                                                                  |
| `OnboardingWizard.tsx`        | onboarding     | Multi-step onboarding wizard container. Backdrop carries `role="dialog" aria-modal="true"`. The progress rail is now an `<ol role="progressbar">` with `aria-valuemin/max/now/text`; each step `<li>` gets `aria-current="step"` when active plus a descriptive `aria-label` (e.g. "Step 2 of 6: Authentication (completed)").                                                                                                                                                                                                                                                                                                                                                                                                                                              | `OnboardingWizard`                                                                                                                                                             |
| `GitHubOptedOutBanner.tsx`    | (root)         | Small warning banner that appears in Task Workbench and Code Review when `settings.githubOptedOut === 'true'`. Session-dismissible via `sessionStorage` (`fleet:github-opted-out-dismissed`) — re-appears on next launch until the user runs `gh auth login` and unsets the setting. Links to the onboarding copy-path to recover.                                                                                                                                                                                                                                                                                                                                                                                                                                          | `GitHubOptedOutBanner`                                                                                                                                                         |
| `FloatingAgentButton.tsx`     | floating-agent | Persistent FAB (bottom-right, z-index 900) that opens/closes the FLEET Advisor panel. Shows neon glow when active.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | `FloatingAgentButton`                                                                                                                                                          |
| `FloatingAgentPanel.tsx`      | floating-agent | Slide-over chat panel for the FLEET Advisor. Spawns adhoc agent sessions via `spawnLocal`, resumes via `steer`. Streams `agent:text` events into message bubbles. ⌘. and ↺ button reset the session. Token usage bar shows ~50k limit.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | `FloatingAgentPanel`                                                                                                                                                           |
| `DashboardErrorCard.tsx`      | dashboard      | Shared error card for dashboard data-load failures. Renders a message string and a Retry button. Imported by `CenterColumn` and `ActivitySection` — the only definition of this markup. Props: `message`, `onRetry`. | `DashboardErrorCard` |
| `CenterColumn.tsx`            | dashboard      | Center column with pipeline Sankey and charts (throughput, success rate, load average, tokens/run). Receives `onRetry` and `onRetryLoad` as props from `DashboardView` — no direct store access for retry logic. Imports `DashboardStats` from `dashboard-types`. | `CenterColumn` |
| `ActivitySection.tsx`         | dashboard      | Right column with activity feed, recent completions, and token metrics. Receives `onRetry` prop for feed error recovery — no direct store access for retry logic. | `ActivitySection` |
| `StatusRail.tsx`              | dashboard      | Left rail with status tiles (active, queued, review, done, tokens). Clicking the Review tile navigates to Code Review view. "New Task" button uses design tokens. Imports `DashboardStats` from `dashboard-types`, `formatTokensCompact` from `format.ts`. | `StatusRail`, `RailFilter` |
| `FiresStrip.tsx`              | dashboard      | Alert strip for dashboard attention items (failed, blocked, stuck, load). Decorative ⚠ emoji wrapped in `aria-hidden` span. Renders null when all counts are zero. | `FiresStrip` |
| `MorningBriefing.tsx`         | dashboard      | Morning briefing card shown after session gap when tasks completed. Task rows use `task.id` as React key. | `MorningBriefing` |
| `SuccessRateChart.tsx`        | dashboard      | 14-day success rate SVG chart. `trendPath` and `fillPath` computed in `useMemo([data, plotW, plotH])` — path strings only rebuild when data changes. | `SuccessRateChart` |
| `MissionBriefBand.tsx`        | dashboard      | V2 dashboard top band (3-col grid): animated headline sentence, sprint progress bar, quick action buttons. Receives props from `useDashboardData`. | `MissionBriefBand` |
| `ActiveAgentsCard.tsx`        | dashboard      | V2 live card showing running pipeline agents (up to 5) with elapsed time and token count. Empty state shows Spawn button. Card head carries `fleet-pulse`. | `ActiveAgentsCard` |
| `PipelineGlanceCard.tsx`      | dashboard      | V2 live card: proportional flow bar + 4-up grid (queued/running/review/done counts + oldest peek). Card head carries `fleet-pulse`. | `PipelineGlanceCard` |
| `ThroughputCard.tsx`          | dashboard      | V2 historical card: 24-bar chart from `CompletionBucket[]` + delta vs yesterday. No pulse — historical data. | `ThroughputCard` |
| `AttentionCard.tsx`           | dashboard      | V2 triage card: aggregated failed/blocked/stale-review items ranked by severity×age. Hides entirely when empty. Red-tinted border. | `AttentionCard` |
| `ReviewQueueCard.tsx`         | dashboard      | V2 triage card: tasks in `pendingReview` partition, capped at 5 with overflow link. Shows "All caught up." when empty. | `ReviewQueueCard` |
| `RecentCompletionsCard.tsx`   | dashboard      | V2 triage card: last 5 done tasks with token count. Hides entirely when empty. QualityChip stubbed (TODO phase-2.5). | `RecentCompletionsCard` |
| `KPIStrip.tsx`                | dashboard      | V2 full-width KPI row: 5 cells (success rate, avg duration, tokens/run, cost/task, failure rate) each with delta indicator and MicroSpark. | `KPIStrip` |
| `PerAgentStats.tsx`           | dashboard      | V2 stats table: agent runs grouped by task title, last 7 days, sorted by runs desc, capped at 6. | `PerAgentStats` |
| `PerRepoStats.tsx`            | dashboard      | V2 stats table: runs grouped by repo, last 7 days, capped at 6. Shows merged/open PR counts. | `PerRepoStats` |
| `primitives/Card.tsx`         | dashboard      | V2 primitive: card shell with `--surf-1` bg and optional attention border (red-tinted). | `Card` |
| `primitives/CardHead.tsx`     | dashboard      | V2 primitive: card header with eyebrow, title, optional right slot, optional live `fleet-pulse`. | `CardHead` |
| `primitives/QualityChip.tsx`  | dashboard      | V2 primitive: quality score chip (q≥90 green, q≥75 amber, q<75 red). Returns null when q is null. | `QualityChip` |
| `primitives/MicroSpark.tsx`   | dashboard      | V2 primitive: inline 18px SVG sparkline. Respects `prefers-reduced-motion`. | `MicroSpark` |
| `hooks/useDashboardData.ts`   | dashboard      | V2 data aggregator hook — single source for all dashboard card props. Derives `activeAgents` (running tasks with elapsed time + token count), `attentionItems` (failed/blocked/stale-review ranked by severity×age), `perAgentStats`/`perRepoStats` (last-7-day cost record aggregations), `briefHeadlineParts` (colorized sentence parts), and `avgCostPerTask`/`failureRate`. Exposes navigation callbacks (`openAgentsView`, `openPipelineView`, `openReviewView`, `openPlannerView`, `openNewTask`, `retryTask`). Fetches `agentManager.maxConcurrent` from settings on mount. | `useDashboardData`, `DashboardData`, `ActiveAgent`, `AttentionItem`, `PerAgentRow`, `PerRepoRow`, `BriefHeadlinePart` |
| `FloatingAgentMessage.tsx`    | floating-agent | Message bubble for user/assistant turns in the floating agent panel. User bubbles right-aligned (plain text), assistant bubbles left-aligned and rendered via `renderAgentMarkdown` for bold/code/headings.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | `FloatingAgentMessage`                                                                                                                                                         |
| `ModelsSection.tsx`           | settings       | Three settings cards: Local backend (endpoint URL + test connection), Opencode backend (executable path), and Active routing (one row per agent type). Each row shows Claude / Local / Opencode radios and a model picker. Local enabled for Pipeline only (`supportsLocal`); Opencode enabled for Pipeline, Assistant, Adhoc, Reviewer (`supportsOpencode`); disabled radios show explanatory tooltips. Loads and saves `agents.backendConfig` (including `opencodeExecutable`) atomically.                                                                                                                                                                                                                                                                                | `ModelsSection`                                                                                                                                                                |
| `LaunchpadGrid.tsx`           | agents         | Agent session launcher (tiles + repo chip + prompt input). The runtime model is resolved from `agents.backendConfig` and configured in Settings → Models — there is no in-launchpad model picker.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | `LaunchpadGrid`                                                                                                                                                                |
| `ToastContainer.tsx`          | layout         | Renders the `useToastStore` queue across two stacked live regions: errors go in `aria-live="assertive"` so screen readers interrupt narration on critical failures; routine info/success notifications stay `aria-live="polite"`. Each toast also carries `role="alert"` (errors) or `role="status"` (others) on the item itself.                                                                                                                                                                                                                                                                                                                                                                                                                                           | `ToastContainer`                                                                                                                                                               |
| `NotificationBell.tsx`        | layout         | Header bell button + dropdown panel for the in-app notification center. The dropdown is rendered through a React portal at `document.body` and pinned to the bell via `getBoundingClientRect()` (re-measured on resize/scroll), so it escapes the unified header's `overflow:hidden` and stacking context. Auto-focuses the first item on open, auto-marks-all-read after 1.5s, supports arrow/enter/escape keyboard navigation, and routes internal `viewLink` paths through `usePanelLayoutStore.setView` plus the matching selection store. Header exposes "Mark all as read" and "Clear all" actions; each item carries an inline X button that dismisses the single notification without triggering navigation.                                                        | `NotificationBell`                                                                                                                                                             |
| `ReviewQuickActions.tsx`      | code-review    | Three suggested-prompt chips (security, performance, improvements) under the AI Review chat. Accepts `disabled` and `disabledReason` — when disabled, every chip carries `title={disabledReason}` so users can hover to learn why (e.g. "Select a review task first").                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | `ReviewQuickActions`                                                                                                                                                           |
| `ReviewChatInput.tsx`         | code-review    | Textarea + send/abort buttons for the AI Review chat. Accepts `disabled` and `disabledReason`; the disabled textarea/send button surface the reason via `title`. Empty-message tooltip on Send falls back to "Type a message to send".                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | `ReviewChatInput`                                                                                                                                                              |
| `WorkbenchActions.tsx`        | task-workbench | Save / Queue / Cancel button row at the bottom of the Task Workbench form. The Save button now carries a `title="Add a title before saving"` tooltip when disabled because the title check has not passed; the Queue button continues to surface the multi-reason `queueDisabledReasons` joined with periods.                                                                                                                                                                                                                                                                                                                                                                                                                                                               | `WorkbenchActions`                                                                                                                                                             |
| `AIAssistantPanel.tsx`        | code-review    | Right-side AI Review panel: header with Re-review/Clear-thread menu, metrics row, message list, and `ReviewQuickActions` + `ReviewChatInput`. Threads `disabledReason` through to the chat input + quick-action chips so users on a no-task or streaming state see why they're disabled. On auto-review failure, shows a compact "AI Review unavailable" notice with a collapsible details toggle (raw error) and Retry button, rather than surfacing the raw error as the primary panel content.                                                                                                                                                                                                                                                                              | `AIAssistantPanel`                                                                                                                                                             |
| `UnifiedHeader.tsx`           | layout         | App-wide top bar: logo column, panel tab strip, health strip, token badge, notification bell, theme toggle. Root element is `<header>` for landmark navigation. Sized to align all action-row items to a 28px visual baseline; tab close buttons reserve only 16px so active and inactive tabs render at consistent widths.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | `UnifiedHeader`                                                                                                                                                                |
| `HealthStrip.tsx`             | layout         | Compact agent-manager pill in the unified header — colored dot + active/queued counters with optional `!failed` count. Clicking opens the Sprint Pipeline. Visual sizing (height, padding, radius) lives in `.health-strip` in `UnifiedHeader.css`; only color tokens stay inline.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | `HealthStrip`, `HealthManagerState`                                                                                                                                            |
| `NeonBadge.tsx`               | neon           | Neon-themed inline pill (color/background/border driven by accent token). Uses `fleet-badge fleet-badge--sm` for consistent padding/font sizing wherever it's rendered (token counter, dashboard chips, etc.).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | `NeonBadge`                                                                                                                                                                    |
| `Spinner.tsx`                 | ui             | Accessible loading spinner. Renders `role="status"` and `aria-label` (defaults to `"Loading"`, overridable via `label` prop). Size variants: `sm`, `md` (default), `lg`. Optional `color` override via inline style. | `Spinner`, `SpinnerProps` |
| `LoadingState.tsx`            | ui             | Accessible loading placeholder for async content. Renders `role="status"` so dynamic insertion is announced by screen readers. Accepts `message` (default `"Loading..."`) and `className` props. | `LoadingState`, `LoadingStateProps` |
| `ErrorBanner.tsx`             | ui             | Conditionally rendered error message bar. Renders `role="alert"` so screen readers announce it on insertion. Returns `null` when `message` is null or empty. Accepts optional `className`. | `ErrorBanner`, `ErrorBannerProps` |
| `Input.tsx`                   | ui             | Accessible text input with optional prefix/suffix decorators and inline error display. Uses `useId()` to auto-generate a stable error element id; wires `aria-describedby` on the `<input>` when `error` is set. Error span carries `role="alert"` and `id` matching the input's `aria-describedby`. Callers may also pass their own `aria-describedby` — both ids are merged. | `Input`, `InputProps` |
| `Tooltip.tsx`                 | ui             | Accessible tooltip primitive. Tooltip text wrapped in `<span role="tooltip" id={id}>` (via `useId()`). Trigger carries `aria-describedby={id}`. Revealed on hover and keyboard focus. Side variants: `top` (default), `bottom`, `left`, `right`. | `Tooltip`, `TooltipProps` |
| `RepositoriesSection.tsx`     | settings       | CRUD UI for configured repositories. Each repo card has a footer with an "Env vars" toggle button that expands an inline key/value editor for per-repo environment variables. Variables are saved as `envVars: Record<string,string>` in the `repos` settings JSON. Also manages scan-directories and clone-directory discovery config.                                                                                                                                                                                                                                                                                                                                                                                                                                     | `RepositoriesSection`                                                                                                                                                          |
| `TopBar.tsx`                  | code-review    | Task switcher, branch bar, review action area, and AI Partner toggle. Accepts both `review` and `approved` tasks (guard updated). For `review` tasks: shows a standalone "Approve" button (`useApproveAction`) and the consolidated `ApproveDropdown`. For `approved` tasks: shows a "Build PR" button that opens `PrBuilderModal`. "View Prompt" button renders the last rendered agent prompt in a `Modal`. | `TopBar` |
| `ChangesTab.tsx`              | code-review    | Diff viewer tab for Code Review Station. **UX:** adds a "Since last review" toggle button when `snapshot.branchTip` is present and `task.retry_count > 0`. Toggle calls `git:diffBetweenRefs` with the snapshot's `branchTip` as `fromRef` and `HEAD` as `toRef`; result rendered in `DiffViewer`. State is paired with the task ID so switching tasks automatically resets the toggle. | `ChangesTab` |
| `VerificationTab.tsx`         | code-review    | Verification tab for Code Review Station (replaces TestsTab). Two sections: **FLEET Verified** — reads `task.verification_results` (structured gate output persisted by `verification-gate.ts`); renders a pass/fail table for typecheck + tests with expand/collapse for stdout/stderr and a truncation notice when capped. **Agent Test Runs** — uses `extractTestRuns` against `useAgentEventsStore` to surface test commands the agent ran (same logic as former TestsTab, with expanded gradle/maven patterns). Shows empty-state messages when either source is unavailable. Styles in `VerificationTab.css`. | `VerificationTab` |
