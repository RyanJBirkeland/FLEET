# Hooks

React hooks for shared logic across components.
Source: `src/renderer/src/hooks/`

| Module | Purpose | Key Exports |
|--------|---------|-------------|
| `useFilteredTasks.ts` | Derives filtered and partitioned task subsets from sprint store + UI filter state. Uses `useShallow` to prevent re-renders when task array contents are unchanged. | `useFilteredTasks` |
| `useIDEFileOperations.ts` | Manages IDE file I/O: loads file content on tab switch, exposes save/change/close/open-folder/open-file handlers. Encapsulates the saving-in-progress guard ref. | `useIDEFileOperations` |
| `useIDEKeyboard.ts` | Registers IDE keyboard shortcuts while the IDE view is active. V1 set: ⌘S/W/O/B/J/P + ⌘/ shortcut overlay + terminal-scoped (⌘T/F/D, ⌘⇧[/], ⌘+/-/0, ⌃L). V2 set (Task 8): ⌘1 (Files), ⌘⇧F/G/O/A (Search/SCM/Outline/Agents), ⌘⌥I (toggle Insight Rail). Three new optional setters route the V2 shortcuts: `setActivity`, `setSidebarOpen`, `toggleInsightRail` — V1 callers without them simply skip the V2 keys. | `useIDEKeyboard` |
| `useIDEStateRestoration.ts` | On mount, reads `ide.state` from settings and restores rootPath, open tabs, active tab, display preferences, and V2 `uiState` to the IDE store. New `insightSectionsOpen` keys added in future versions receive defaults from `DEFAULT_INSIGHT_SECTIONS` via spread-under-saved pattern. Validates paths with `fs.stat` before restoring — stale paths from another machine are silently skipped. | `useIDEStateRestoration` |
| `useIDEUnsavedGuard.ts` | Registers a `beforeunload` handler that blocks page unload when any open IDE tab has unsaved changes. | `useIDEUnsavedGuard` |
| `useSprintPipelineState.ts` | Centralises all store subscriptions for SprintPipeline. Returns tasks, selection (including `toggleTaskSelection` lifted from `useSprintSelection`), UI overlay state, and derived values. | `useSprintPipelineState` |
| `useAgentViewLifecycle.ts` | Registers the view-lifecycle effects for AgentsView: agent-list fetch when the view becomes active, history load on selection change (rejection caught with `console.error`), spawn-modal listener, and scratchpad banner check (rejection caught). The live agent-events subscription is owned by `useAppInitialization` (app-lifetime) so switching panel tabs does not drop events for running agents. | `useAgentViewLifecycle` |
| `useAppInitialization.ts` | App-boot initialization hook mounted once at the root of `App.tsx`: loads saved panel layout, keybindings, pending-review state, filter presets, local-agents cost data, and subscribes the agent-events store so live events are captured for the app's lifetime regardless of which view is mounted. | `useAppInitialization` |
| `useAgentViewCommands.ts` | Registers and unregisters Spawn Agent and Clear Console commands in the command palette while AgentsView is mounted. `onSpawnAgent` replaces the old `handleSpawnAgent` param — callers pass `openLaunchpad` directly. | `useAgentViewCommands` |
| `useAgentSlashCommands.ts` | Handles slash commands in AgentConsole: /stop, /retry, /focus, /checkpoint, /test, /scope, /status. Returns handleCommand. | `useAgentSlashCommands` |
| `useBatchActions.ts` | Wraps useBatchReviewActions with confirm dialogs and in-flight state tracking for batch merge/ship/PR/discard. Used by TopBar. | `useBatchActions`, `BatchActionKey`, `UseBatchActionsResult` |
| `useReviewFreshness.ts` | Fetches and tracks whether the agent branch is fresh, stale, or in conflict with main. Re-fetches on task id or rebased_at change. | `useReviewFreshness`, `Freshness`, `FreshnessStatus` |
| `useReviewActionModals.ts` | Wires confirm dialogs and textarea prompt modals for review actions. Exposes trigger functions and props objects for rendering. | `useReviewActionModals` |
| `useReviewActionState.ts` | Tracks per-action loading state (actionInFlight) and the selected merge strategy. | `useReviewActionState`, `MergeStrategy` |
| `useGitCommands.ts` | Registers git operation commands (stage all, commit, push, switch branch) in the command palette. Extracted from GitTreeView. | `useGitCommands` |
| `useWebhookManager.ts` | Owns all webhook CRUD state and async operations (list, create, update, delete, test). Returns state and handlers for the webhook settings UI. | `useWebhookManager`, `WebhookManager` |
| `useVisibleStuckTasks.ts` | Derives the list of stuck tasks that have not been dismissed. Composes `useHealthCheckStore` + `useSprintTasks` — lives in hooks (not stores) to keep cross-store logic out of the store layer. | `useVisibleStuckTasks` |
| `useBatchReviewActions.ts` | Batch review actions (merge locally, ship it, create PR, discard) over a set of tasks. `batchShipIt` now delegates to `window.api.review.shipBatch` — one server-side call that merges every task and pushes once instead of N per-task pushes. Merge/PR/discard still loop client-side via file-private `executeBatchAction`. | `useBatchReviewActions`, `UseBatchReviewActionsResult` |
| `useSingleTaskReviewActions.ts` | Single-task review actions: shipIt, mergeLocally, createPr, requestRevision, rebase, discard. `requestRevision` opens a TextareaPromptModal so the user can describe what needs to change — feedback is persisted to `revision_feedback` then forwarded to the review orchestration (which appends it to the task spec before re-queuing). Surfaces conflict file list on merge failure, clickable PR URL via toast action, and empty-queue toast. | `useSingleTaskReviewActions`, `UseSingleTaskReviewActionsResult` |
| `useSprintPolling.ts` | Adaptive sprint-task polling plus subscriptions to both `sprint:externalChange` (coarse file-watcher signal) and `sprint:mutation` (granular main-process broadcast from review actions, etc.). Each triggers a full `loadData()` — pragmatic refresh path, leaves room for a later in-place patch optimisation. | `useSprintPolling` |
| `useManagerEventListener.ts` | Surfaces main-process manager + task-terminal events to toasts: `agent-manager:circuit-breaker-open` (error — "drain resumes in N minutes"), `task-terminal:resolution-error` (error — "check blocked tasks"), and `manager:warning` (info — covers the status-server port-conflict message). Single hook called once in `App.tsx` so the three subscriptions stay colocated. | `useManagerEventListener` |
| `useDrainStatus.ts` | Subscribes to `agentManager:drainPaused` broadcasts and returns the current pause state (reason, pausedUntil, affectedTaskCount) or null. Auto-clears when `pausedUntil` elapses. | `useDrainStatus`, `DrainPausedState` |
| `useNow.ts` | Returns the current timestamp updated at a coarse 10-second interval via a module-level singleton `setInterval`. All subscribing components share one timer — use this instead of `Date.now()` in render bodies to keep `React.memo` effective between ticks. | `useNow` |
| `useBackoffInterval.ts` | Drop-in `setInterval` replacement with exponential backoff on errors, random jitter, and automatic pause when the document is hidden. Used by `DashboardView`, `PipelineHeader`, and any component that polls a fallible IPC endpoint. Accepts `baseMs | null` (null disables). Backoff multiplier, max interval, and jitter are configurable via options. | `useBackoffInterval` |
| `useDashboardPolling.ts` | Centralised dashboard polling hook. Calls `fetchAll` on mount + every `POLL_DASHBOARD_INTERVAL` (60s), and `fetchLoad` on mount + every `POLL_LOAD_AVERAGE` (5s). Subscribes to `sprint:externalChange` to refresh `fetchAll` on DB writes. `PollingProvider` mounts this once at app root — views must not add their own polling loops. | `useDashboardPolling` |
| `useDashboardMetrics.ts` | Derives display-ready metrics from sprint tasks and cost data. Exports `DEFAULT_STUCK_MS` (1 hour). Imports `DashboardStats` and `formatTokensCompact` from shared lib modules. | `useDashboardMetrics`, `DEFAULT_STUCK_MS` |
| `useWindowSession.ts` | Encapsulates `fleet:last-window-close` localStorage access. Detects tasks completed since the last window close and exposes `showBriefing`, `briefingTasks`, and `dismissBriefing`. `DashboardView` delegates all session-persistence to this hook. | `useWindowSession` |
| `useTaskAutoSelect.ts` | Auto-selects the first `review` or `approved` task when the current selection becomes stale (e.g. after Ship It marks it `done`). Prevents the TopBar from falling into its empty state. Updated to treat both `review` and `approved` as valid selections. | `useTaskAutoSelect` |
| `useAutoReview.ts` | Debounces an auto-review fire when the user selects a task in `review` status. Rapid task switches cancel the pending fire — only the last stable selection (2 s) triggers a review. If a stale result (`ready` or `error`) is already in the store, calls `invalidate(taskId)` before `autoReview` so revised tasks always receive fresh analysis. | `useAutoReview` |
| `useValidationChecks.ts` | Computes and stores structural validation checks for the Task Workbench on every form change. Pure check functions (`computeStructuralChecks`, `checkAntiPatterns`, `checkTestSection`, `checkWordCount`, `checkFilesToChangeSection`, `checkHandlerCountAwareness`, `checkPreloadSync`, `checkComplexity`, `extractFilePaths`) are exported for direct unit testing. H8 warns when spec exceeds 500 words; H9 warns when `## Files to Change` heading is absent. | `useValidationChecks`, `computeStructuralChecks`, `checkAntiPatterns`, `checkTestSection`, `checkWordCount`, `checkFilesToChangeSection`, `extractFilePaths` |
| `useApproveAction.ts` | Single-task approve action hook. Calls `review:approveTask` IPC, shows success/error toast, and invokes `onSuccess` callback on completion. Tracks in-flight state via `inFlight`. | `useApproveAction`, `UseApproveActionResult` |
| `usePrGroups.ts` | Repo-scoped PR Group facade hook. Loads groups on mount, wraps store actions with repo binding, and handles `buildGroup` toast feedback (success with "Open PR" action link, error). Exposes `unassignedTasksForRepo` derived from `approved` sprint tasks not yet in any group. | `usePrGroups`, `UsePrGroupsResult` |
| `useSprintTaskActions.ts` | All task mutation callbacks for SprintCenter — wraps store actions with confirm dialogs, toast feedback, and coordinates UI state. Exposes per-task IPC wrappers (`unblockTask`, `markTaskFailed`, `forceTaskDone`, `releaseTask`) so presentation components don't reach into `window.api.*` directly. Owns the confirm-modal state via `confirmProps`. | `useSprintTaskActions` |
