// Phase 3 wires real composition here. Skeleton avoids import cycle with V1.
export function SprintPipelineV2(): React.JSX.Element {
  return (
    <div
      style={{
        flex: 1,
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        color: 'var(--fg-3)',
        fontSize: 13,
        fontFamily: 'var(--font-mono)',
      }}
    >
      V2 pipeline — skeleton
    </div>
  )
}
